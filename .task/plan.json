{
  "task_id": "105",
  "title": "Surface errors to users: replace console.error with toast notifications + loading states",
  "files": [
    "gui/src/App.tsx",
    "gui/src/App.module.css",
    "gui/src/editor/Editor.tsx",
    "gui/src/panels/ExportDialog.tsx",
    "gui/src/bridge.ts",
    "gui/src/hooks/useToast.ts",
    "gui/src/__tests__/App.test.tsx",
    "gui/src/__tests__/Editor.test.tsx",
    "gui/src/__tests__/ExportDialog.test.tsx",
    "gui/src/__tests__/useToast.test.ts"
  ],
  "modules": [
    "gui/src",
    "gui/src/panels",
    "gui/src/editor",
    "gui/src/hooks",
    "gui/src/__tests__"
  ],
  "analysis": "The task requires replacing 8+ console.error calls with user-visible toast notifications, adding a loading/error state during app initialization, and integrating Tauri's native save dialog for export. Currently:\n\n1. **App.tsx** has 6 console.error sites: getInitialState failure (line 127), subscribeToEvents failure (line 140), file-change subscription failure (line 158), setParameter failure (line 170), re-evaluate failure (line 102), reload failure (line 207). No loading state during init.\n2. **Editor.tsx** has 4 console.error sites: save failure (line 82), updateSource failure (line 99), LSP didChange failure (line 109), LSP init failure (line 133). The Editor operates internally and doesn't have a toast mechanism.\n3. **ExportDialog.tsx** already handles errors with toasts via handleDoExport in App.tsx but uses a hardcoded path `export.${format}` instead of a native file picker.\n\n**Approach:**\n- Create a `useToast` hook (createToast factory following SolidJS conventions) that provides `showToast(message, type)` with auto-dismiss. This centralizes toast logic currently spread across App.tsx's manual signals.\n- Replace App.tsx console.error calls with toast notifications using `showToast`.\n- Add `initPhase` signal to App.tsx: 'loading' | 'ready' | 'error'. Show a loading spinner/skeleton during init, error state with retry on failure.\n- For Editor.tsx errors (save, LSP init): pass an `onError` callback prop from App.tsx so the Editor can surface errors as toasts without coupling to toast implementation. This follows the existing dependency-injection pattern (onSelect, onSetParameter, etc.).\n- For export: add a `pickSavePath` bridge function that invokes a backend command. Since the Tauri dialog plugin is NOT installed (no @tauri-apps/plugin-dialog in package.json or Cargo.toml), we add the bridge function with graceful fallback to hardcoded path when the backend command doesn't exist yet.\n- LSP didChange/fileSwitch errors stay as console.error since they're frequent recoverable internal operations that would flood the user with noise if toasted.\n\nExisting infrastructure to reuse: Toast component (gui/src/panels/Toast.tsx) is already fully built with data-type styling. App.module.css already has .toastContainer with fixed positioning. The Show component pattern from SolidJS is used throughout for conditional rendering.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Create useToast hook file skeleton at gui/src/hooks/useToast.ts (empty export to enable test-first development)",
      "status": "done",
      "commit": "068025e",
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing tests for useToast hook in gui/src/__tests__/useToast.test.ts: (1) showToast sets message and type signals, (2) dismissToast clears message to null, (3) auto-dismiss fires after 5000ms for success type, (4) auto-dismiss fires after 10000ms for error type, (5) calling showToast again resets the auto-dismiss timer, (6) dismissToast cancels pending auto-dismiss timer. Use createRoot from solid-js for reactive context, vi.useFakeTimers for timer assertions.",
      "status": "done",
      "commit": "1ba7e1e"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Implement createToast() in gui/src/hooks/useToast.ts: createSignal<string|null>(null) for message, createSignal<'success'|'error'|'info'>('info') for type. showToast(msg, type) sets both signals and starts setTimeout (5000ms for success/info, 10000ms for error). dismissToast() clears message and cancels timer. Returns { toastMessage, toastType, showToast, dismissToast }.",
      "status": "done",
      "commit": "2a4ecd3"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing tests for App.tsx loading state in gui/src/__tests__/App.test.tsx (new describe 'App initialization loading state'): (1) shows data-testid='app-loading' while getInitialState is pending (use deferred promise), (2) shows data-testid='app-error' with retry button when getInitialState rejects, (3) clicking retry button calls getInitialState again, (4) after successful getInitialState resolve, data-testid='app-layout' is shown and loading/error elements are gone.",
      "status": "done",
      "commit": "e3d9b89"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Add loading/error state to App.tsx: createSignal<'loading'|'ready'|'error'>('loading') for initPhase. Wrap onMount init logic in try/catch that sets 'ready' on success, 'error' on failure. Add conditional rendering with Show: when loading show spinner div (data-testid='app-loading'), when error show error div with Retry button (data-testid='app-error'), when ready show normal layout. Add retry handler. Add .loading and .errorState CSS classes to App.module.css.",
      "status": "done",
      "commit": "76c2eee"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing tests for App.tsx toast on setParameter failure in gui/src/__tests__/App.test.tsx: modify/replace existing 'handleSetParameter error handling' test. When bridgeSetParameter rejects, a toast element (data-testid='toast' with data-type='error') appears in the DOM containing the error message. Verify console.error is NOT called (behavior replaced).",
      "status": "done",
      "commit": "34fef7e"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Wire createToast() hook into App.tsx, replacing manual toastMessage/toastType/setToastMessage/setToastType signals. Replace console.error in handleSetParameter (line 170) with showToast(`Parameter update failed: ${err.message}`, 'error'). Update Toast rendering in JSX to use toast.toastMessage()/toast.toastType()/toast.dismissToast().",
      "status": "done",
      "commit": "689b9d9"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write failing test for App.tsx toast on re-evaluate failure in gui/src/__tests__/App.test.tsx (new describe 'App re-evaluate error toast'): mock bridgeUpdateSource to reject, simulate keyboard shortcut trigger (or directly call the handler via store interaction), verify error toast appears.",
      "status": "done",
      "commit": "11a7271"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Replace console.error in onReEvaluate callback (App.tsx line 102) with showToast('Re-evaluation failed: ...', 'error').",
      "status": "done",
      "commit": "0cf5777"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write failing test for App.tsx toast on reload failure in gui/src/__tests__/App.test.tsx (new describe 'App reload error toast'): set changedFile, mock bridgeOpenFile to reject, trigger handleReload via ReloadPrompt interaction, verify error toast appears.",
      "status": "done",
      "commit": "87a58af"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Replace console.error in handleReload (App.tsx line 207) with showToast('Reload failed: ...', 'error').",
      "status": "done",
      "commit": "3b12d60"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Write failing test for App.tsx toast on event subscription failure in gui/src/__tests__/App.test.tsx (new describe 'App event subscription error toast'): mock engineStore.subscribeToEvents to reject, verify warning toast appears after mount ('Event subscription failed \u2014 some updates may not appear').",
      "status": "done",
      "commit": "b0f4d34"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "Replace console.error for subscribeToEvents failure (App.tsx line 140) and onFileChanged failure (App.tsx line 158) with showToast warning calls. These are non-fatal: show info/error toast but continue running.",
      "status": "done",
      "commit": "f07f3f4"
    },
    {
      "id": "step-13",
      "type": "test",
      "description": "Write failing tests for Editor.tsx error surfacing via onError prop in gui/src/__tests__/Editor.test.tsx: (1) new describe 'Editor save error callback' \u2014 when saveFile rejects, onError callback is called with message string, (2) new describe 'Editor LSP init error callback' \u2014 when lspClient.initialize rejects, onError is called with LSP failure message.",
      "status": "done",
      "commit": "9e99fce"
    },
    {
      "id": "step-14",
      "type": "impl",
      "description": "Add optional onError?: (message: string) => void to EditorProps in Editor.tsx. Replace console.error in save handler (line 82) with props.onError?.('Failed to save file: ...') call. Replace console.error in LSP init chain (line 133) with props.onError?.('LSP initialization failed \u2014 completions and diagnostics may be unavailable'). Wire onError from App.tsx to Editor component, passing (msg) => showToast(msg, 'error').",
      "status": "done",
      "commit": "7261063"
    },
    {
      "id": "step-15",
      "type": "test",
      "description": "Write failing tests for export with save dialog in gui/src/__tests__/App.test.tsx or gui/src/__tests__/ExportDialog.test.tsx: (1) handleDoExport calls bridge.pickSavePath before bridgeExportGeometry, (2) if pickSavePath returns null (user cancelled), bridgeExportGeometry is NOT called, (3) if pickSavePath throws (not available), falls back to default path and proceeds with export.",
      "status": "done",
      "commit": "9b5b6cc"
    },
    {
      "id": "step-16",
      "type": "impl",
      "description": "Add pickSavePath to bridge.ts: invoke<string|null>('pick_save_path', { format, defaultName }). Update handleDoExport in App.tsx to call pickSavePath first wrapped in try/catch; if result is null, abort (user cancelled); if call throws, fall back to hardcoded 'export.{format}' path. This way the feature degrades gracefully when the backend command doesn't exist yet.",
      "status": "done",
      "commit": "3feab26"
    },
    {
      "id": "step-17",
      "type": "test",
      "description": "Write integration test in gui/src/__tests__/App.test.tsx verifying end-to-end flow: App renders, getInitialState succeeds (ready state), then setParameter fails, verify toast is visible with correct error message. Ensures the createToast hook is properly wired through the full component tree.",
      "status": "done",
      "commit": "b647cb0"
    },
    {
      "id": "step-18",
      "type": "impl",
      "description": "Final cleanup pass: verify all console.error calls in App.tsx are replaced with toast calls, ensure Editor.tsx LSP didChange/fileSwitch errors remain as console.error (intentional), run full vitest suite to confirm no regressions. Clean up any unused imports.",
      "status": "done",
      "commit": "bbd5bd7"
    }
  ],
  "design_decisions": [
    {
      "decision": "Create a reusable createToast() hook rather than adding toast logic inline in App.tsx",
      "rationale": "App.tsx already has manual toastMessage/toastType signals (lines 61-62). Extracting to a hook adds auto-dismiss, centralizes logic, and makes it testable in isolation. The factory-function pattern (createToast) follows the existing SolidJS convention used by createEditorStore, createEngineStore, createSelectionStore."
    },
    {
      "decision": "Surface Editor.tsx errors via an onError callback prop rather than importing toast hook directly",
      "rationale": "The Editor component is a leaf component that shouldn't know about the toast system. Dependency injection via callback prop is consistent with the codebase pattern (onSelect, onSetParameter, onGroupDoubleClick, etc.) and keeps the Editor testable without mocking toast infrastructure."
    },
    {
      "decision": "Keep LSP didChange/fileSwitch errors as console.error (not toasted)",
      "rationale": "These are frequent, recoverable internal operations that fire on every keystroke (debounced). Toasting every LSP sync error would flood the user with noise. Only LSP init failure (which means broken completions/diagnostics for the session) warrants a user-visible warning."
    },
    {
      "decision": "Add pickSavePath to bridge.ts with graceful fallback to hardcoded path",
      "rationale": "The Rust backend doesn't have a pick_save_path command yet \u2014 the Tauri dialog plugin is not in Cargo.toml dependencies. By adding the bridge call with try/catch fallback, we prepare the frontend for the native dialog while maintaining current behavior until the backend implements the command."
    },
    {
      "decision": "App init loading state uses a simple 'loading' | 'ready' | 'error' signal with conditional rendering via Show",
      "rationale": "SolidJS's Show component + signal is the idiomatic pattern. No need for a separate store \u2014 init state is local to App.tsx lifecycle. The three-state enum is sufficient: loading (pending), ready (success), error (failed with retry)."
    },
    {
      "decision": "Auto-dismiss timers: 5s for success/info, 10s for errors",
      "rationale": "Success messages are brief acknowledgements; errors need more reading time and may contain diagnostic information. The timer resets when showToast is called again to prevent stale messages from lingering."
    }
  ],
  "reuse": [
    {
      "what": "Existing Toast component (gui/src/panels/Toast.tsx)",
      "where": "Already renders toast UI with message, type, and dismiss button",
      "how": "Keep as-is. The new createToast hook manages state; Toast component remains a pure presentational component. No changes needed to Toast.tsx itself."
    },
    {
      "what": "Existing toast CSS and container (App.module.css .toastContainer)",
      "where": "Already has fixed positioning, z-index, and width constraints for toast overlay",
      "how": "Reuse directly. Only add .loading and .errorState CSS classes for the new init states."
    },
    {
      "what": "Existing App.test.tsx mock patterns for bridge functions",
      "where": "vi.mock('../bridge') with all bridge functions mocked, waitFor pattern for async assertions",
      "how": "Follow the same mock setup pattern for new tests. Add pickSavePath to the mock list."
    },
    {
      "what": "Existing bridge.ts invoke pattern",
      "where": "All bridge functions follow invoke<T>('command', params) pattern",
      "how": "Add pickSavePath following the same pattern: invoke<string|null>('pick_save_path', { format, defaultName })."
    },
    {
      "what": "Dependency injection callback pattern for component props",
      "where": "Used throughout: EditorProps, PropertyEditor (onSetParameter), ConstraintPanel (onConstraintSelect), Viewport (onSelect)",
      "how": "Add onError?: (message: string) => void to EditorProps following this established pattern."
    },
    {
      "what": "SolidJS Show component for conditional rendering",
      "where": "Used in ExportDialog, App.tsx toast rendering, ReloadPrompt",
      "how": "Use Show with when={} for loading/error/ready state conditional rendering in App.tsx."
    },
    {
      "what": "SolidJS createStore/createSignal factory pattern",
      "where": "createEditorStore, createEngineStore, createSelectionStore all use factory functions returning {state, ...methods}",
      "how": "createToast follows the same convention: factory function returning reactive accessors + mutation methods."
    }
  ],
  "_session_id": "105-3a68578e",
  "_created_at": "2026-03-21T19:48:33.677613+00:00"
}
