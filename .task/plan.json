{
  "task_id": "338",
  "title": "Stdlib: complex number functions \u2014 complex, real, imag, conjugate, magnitude, phase",
  "files": [
    "crates/reify-stdlib/src/lib.rs",
    "crates/reify-lsp/src/completion.rs"
  ],
  "modules": [
    "crates/reify-stdlib",
    "crates/reify-lsp"
  ],
  "analysis": "Task 288 (Complex Value variant) is already merged to main. The core complex functions already exist in reify-stdlib under slightly different names: complex(), re(), im(), conjugate(), phase(), magnitude() (extended for Complex), complex_add(), complex_mul() \u2014 all with comprehensive tests (~60 cases). Task 338 specifies user-facing names that differ from the existing short names: `real()` instead of `re()`, `imag()` instead of `im()`, and `complex_magnitude()` as a dedicated Complex-only magnitude function. The approach is: (1) Add `real`/`imag` as aliases using Rust's `|` pattern in match arms alongside existing `re`/`im` \u2014 e.g. `\"re\" | \"real\" =>`, (2) Add `complex_magnitude` as a separate Complex-only function that rejects non-Complex inputs (unlike generic `magnitude` which also handles Tensors), (3) Add task-spec test cases for Voltage dimensions and V/A mismatch, (4) Update LSP completion list (BUILTIN_FUNCTIONS in completion.rs) with all complex function names. All functions follow the existing eval_builtin match-dispatch pattern with unary() helper and sanitize_value() for NaN/Inf guards. The eval_builtin function in lib.rs is the single dispatch point called from reify-expr/src/lib.rs line 91.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Verify task 288 Complex variant is present and existing complex tests pass with `cargo test -p reify-stdlib`",
      "status": "done",
      "commit": "HEAD",
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing tests for `real()` alias in crates/reify-stdlib/src/lib.rs test module: (1) real_dimensionless_returns_real \u2014 eval_builtin(\"real\", &[Complex{3,4,DIMLESS}]) \u2192 Real(3.0), (2) real_dimensioned_returns_scalar \u2014 eval_builtin(\"real\", &[Complex{5,3,LENGTH}]) \u2192 Scalar{5.0, LENGTH}, (3) real_non_complex_returns_undef \u2014 eval_builtin(\"real\", &[Real(3.0)]) \u2192 Undef. These mirror existing re() tests but use the `real` name.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Add `\"real\"` to the `\"re\"` match arm using Rust's or-pattern: change `\"re\" => unary(args, |v| match v {` to `\"re\" | \"real\" => unary(args, |v| match v {`. This shares the exact same closure for both names with zero code duplication.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing tests for `imag()` alias: (1) imag_dimensionless_returns_real \u2014 eval_builtin(\"imag\", &[Complex{3,4,DIMLESS}]) \u2192 Real(4.0), (2) imag_dimensioned_returns_scalar \u2014 eval_builtin(\"imag\", &[Complex{5,3,LENGTH}]) \u2192 Scalar{3.0, LENGTH}, (3) imag_non_complex_returns_undef \u2014 eval_builtin(\"imag\", &[Real(3.0)]) \u2192 Undef.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Add `\"imag\"` to the `\"im\"` match arm using or-pattern: change `\"im\" => unary(args, |v| match v {` to `\"im\" | \"imag\" => unary(args, |v| match v {`.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing tests for `complex_magnitude()`: (1) complex_magnitude_3_4_returns_5 \u2014 eval_builtin(\"complex_magnitude\", &[Complex{3,4,DIMLESS}]) \u2192 Real(5.0), (2) complex_magnitude_dimensioned_returns_scalar \u2014 eval_builtin(\"complex_magnitude\", &[Complex{3,4,LENGTH}]) \u2192 Scalar{5.0, LENGTH}, (3) complex_magnitude_non_complex_returns_undef \u2014 eval_builtin(\"complex_magnitude\", &[Real(5.0)]) \u2192 Undef (unlike generic magnitude which handles Tensors).",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Add `\"complex_magnitude\"` match arm to eval_builtin: `\"complex_magnitude\" => unary(args, |v| match v { Value::Complex { re, im, dimension } => { let mag = (re*re + im*im).sqrt(); if *dimension == DimensionVector::DIMENSIONLESS { sanitize_value(Value::Real(mag)) } else { sanitize_value(Value::Scalar { si_value: mag, dimension: *dimension }) } } _ => Value::Undef })`. This is the Complex branch extracted from the generic `magnitude` function, without the Tensor fallback.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write Voltage dimension spec tests: (1) complex_voltage_preserves_dimension \u2014 build voltage_dim = MASS\u00b7LENGTH\u00b2\u00b7TIME\u207b\u00b3\u00b7CURRENT\u207b\u00b9, create complex(Scalar{3,voltage}, Scalar{4,voltage}), assert result is Complex with voltage dimension, (2) real_voltage_returns_scalar \u2014 real(complex_voltage) \u2192 Scalar{3, voltage}, (3) imag_voltage_returns_scalar \u2014 imag(complex_voltage) \u2192 Scalar{4, voltage}, (4) complex_magnitude_voltage \u2014 complex_magnitude(complex_voltage) \u2192 Scalar{5, voltage}, (5) conjugate_voltage_preserves_dim \u2014 conjugate flips im sign, preserves voltage dimension. These exercise existing logic with the specific Voltage dimension from the task spec.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-8",
      "type": "test",
      "description": "Write dimension mismatch spec test: complex_voltage_current_mismatch_returns_undef \u2014 build voltage_dim and current_dim (DimensionVector::CURRENT), call eval_builtin(\"complex\", &[Scalar{3, voltage}, Scalar{4, current}]), assert result is Undef because Voltage \u2260 Current dimensions.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write phase degree-equivalent verification test: phase_1_plus_i_approx_45_deg \u2014 create Complex{1,1,DIMLESS}, call phase(), assert result is Scalar with ANGLE dimension and si_value \u2248 \u03c0/4 \u2248 0.7854 radians (which is 45\u00b0). Add comment noting 45\u00b0 equivalence. This verifies the specific task-spec case phase(1+i)\u224845deg.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-10",
      "type": "test",
      "description": "Add LSP completion test: in crates/reify-lsp/src/completion.rs tests, verify that BUILTIN_FUNCTIONS contains complex number function names. Assert BUILTIN_FUNCTIONS.contains(&\"complex\"), .contains(&\"real\"), .contains(&\"imag\"), .contains(&\"conjugate\"), .contains(&\"complex_magnitude\"), .contains(&\"phase\"). This will fail until step-11 adds them.",
      "status": "pending",
      "commit": null
    },
    {
      "id": "step-11",
      "type": "impl",
      "description": "Update BUILTIN_FUNCTIONS array in crates/reify-lsp/src/completion.rs to include complex number functions: add \"complex\", \"real\", \"imag\", \"conjugate\", \"complex_magnitude\", \"phase\" to the existing array. Keep existing entries and add a comment grouping the complex functions.",
      "status": "pending",
      "commit": null
    }
  ],
  "design_decisions": [
    {
      "decision": "Add `real`/`imag` as aliases alongside existing `re`/`im` using Rust `|` match patterns, keeping backward compatibility",
      "rationale": "Existing code and tests using `re`/`im` continue to work. The `|` pattern avoids code duplication. Both short and long names are common conventions in engineering tools (MATLAB uses `real`/`imag`, numpy uses `.real`/`.imag`). Short names `re`/`im` may be preferred in expression-heavy contexts."
    },
    {
      "decision": "`complex_magnitude` is a Complex-only function separate from the existing `magnitude`",
      "rationale": "The generic `magnitude` handles both Tensor (vector norm) and Complex. A dedicated `complex_magnitude` provides a clearer typed API that rejects non-Complex inputs with Undef, matching the task spec's intent of a typed Complex function set. The generic `magnitude` continues to handle both types for backward compatibility."
    },
    {
      "decision": "Voltage dimension is computed in tests as MASS\u00b7LENGTH\u00b2\u00b7TIME\u207b\u00b3\u00b7CURRENT\u207b\u00b9 rather than adding a VOLTAGE constant",
      "rationale": "DimensionVector constants are defined for SI base dimensions only (LENGTH, MASS, TIME, CURRENT, etc.) in reify-types/dimension.rs. Derived dimensions like Voltage are composed from base dimensions using .mul()/.div()/.pow() arithmetic. Adding a VOLTAGE constant would belong in reify-types if needed globally, which is out of scope for this stdlib task."
    },
    {
      "decision": "Steps 7-9 are test-only (no paired impl step) \u2014 they exercise existing implementations with task-spec scenarios",
      "rationale": "The underlying logic for complex constructor dimension matching, conjugate dimension preservation, magnitude on Complex, and phase computation already works correctly from task 288. The task spec requires specific Voltage and mismatch scenarios to be tested as verification. These tests should pass immediately with existing code, serving as regression guards."
    },
    {
      "decision": "LSP BUILTIN_FUNCTIONS gets both alias names (`real`/`imag`) but not the short names (`re`/`im`)",
      "rationale": "The completion list is for user-facing autocomplete. The long names `real`/`imag` are more discoverable and self-documenting. The short names `re`/`im` are kept as implementation aliases but don't need to clutter the completion menu. Users who know the short names can type them directly."
    }
  ],
  "reuse": [
    {
      "what": "Existing `re()` / `im()` implementation logic (closure body)",
      "where": "crates/reify-stdlib/src/lib.rs lines 384-405",
      "how": "Extend the match arms with `| \"real\"` and `| \"imag\"` to share the exact same closure \u2014 zero code duplication"
    },
    {
      "what": "Complex magnitude logic from `magnitude` function",
      "where": "crates/reify-stdlib/src/lib.rs lines 285-291",
      "how": "Extract the Complex branch (if-let Complex pattern, sqrt(re\u00b2+im\u00b2), dimensionless\u2192Real / else\u2192Scalar) into the new `complex_magnitude` match arm, without the Tensor fallback that follows"
    },
    {
      "what": "`sanitize_value()` helper for NaN/Inf guarding",
      "where": "crates/reify-stdlib/src/lib.rs helper function",
      "how": "Apply to `complex_magnitude` result, same pattern as existing magnitude/phase/complex_mul implementations"
    },
    {
      "what": "`unary()` arity validation helper",
      "where": "crates/reify-stdlib/src/lib.rs helper function (line ~633)",
      "how": "Wrap `complex_magnitude` implementation in `unary(args, |v| ...)` for consistent single-argument validation, same as all other unary complex functions"
    },
    {
      "what": "Test macros `assert_real_approx!` and `assert_scalar_approx!`",
      "where": "crates/reify-stdlib/src/lib.rs test module (lines ~825-860)",
      "how": "Use for all numeric assertions in new tests \u2014 assert_real_approx for dimensionless Real results, assert_scalar_approx for dimensioned Scalar results"
    },
    {
      "what": "DimensionVector arithmetic (`.mul()`, `.div()`, `.pow()`) and constants",
      "where": "crates/reify-types/src/dimension.rs",
      "how": "Compose Voltage dimension in tests: `MASS.mul(&LENGTH.mul(&LENGTH)).div(&TIME.pow(3)).div(&CURRENT)`, using the same pattern as the existing complex_impedance_integration test"
    },
    {
      "what": "Impedance integration test pattern",
      "where": "crates/reify-stdlib/src/lib.rs lines 3110-3163",
      "how": "The Voltage dimension tests follow the same structure: build a derived dimension from base constants, create Complex with that dimension, test all accessors. Voltage is simpler than Impedance (V = kg\u00b7m\u00b2\u00b7s\u207b\u00b3\u00b7A\u207b\u00b9 vs \u03a9 = kg\u00b7m\u00b2\u00b7s\u207b\u00b3\u00b7A\u207b\u00b2)"
    }
  ],
  "_session_id": "338-393a2752",
  "_created_at": "2026-03-24T20:55:24.262521+00:00"
}
