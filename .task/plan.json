{
  "task_id": "93",
  "title": "Wire frontend components into resizable app layout",
  "files": [
    "gui/src/App.tsx",
    "gui/src/App.module.css",
    "gui/src/__tests__/App.test.tsx",
    "gui/src/components/Splitter.tsx",
    "gui/src/components/Splitter.module.css",
    "gui/src/__tests__/Splitter.test.tsx"
  ],
  "modules": [
    "gui/src",
    "gui/src/components",
    "gui/src/__tests__"
  ],
  "analysis": "Current App.tsx is a skeleton with 3-panel grid layout using placeholder divs and h3 labels. Existing test checks for placeholder text. All real components exist and are fully implemented:\n\n- Viewport (gui/src/viewport/) \u2014 needs {meshes} prop\n- Editor (gui/src/editor/Editor.tsx) \u2014 needs {store: EditorStore} prop\n- FileTabs (gui/src/editor/FileTabs.tsx) \u2014 needs {store: EditorStore} prop\n- PropertyEditor (gui/src/panels/) \u2014 needs {values, selectedEntity, onSetParameter}\n- ConstraintPanel (gui/src/panels/) \u2014 needs {constraints, values}\n- Toolbar (gui/src/panels/) \u2014 needs {onExport, onFitToView}\n- StatusBar (gui/src/panels/) \u2014 needs {evalStatus, meshes, constraints}\n\nStores also exist: createEngineStore (meshes/values/constraints/evalStatus with initFromState + subscribeToEvents), createEditorStore (files/cursor), createSelectionStore (selected/hovered entity). Bridge has getInitialState() which returns GuiState with meshes, values, constraints, files.\n\nApproach:\n1. Build a reusable Splitter component for draggable resize between panels (TDD).\n2. Rewrite App.tsx to: (a) create all stores, (b) call getInitialState on mount to populate them, (c) subscribe to backend events, (d) compose real components into a resizable layout with Toolbar at top, StatusBar at bottom, (e) set document.title reactively based on active file + eval status.\n3. Rewrite App.module.css for the full resizable layout with grid areas.\n4. Rewrite App.test.tsx with proper mocking of bridge/Tauri APIs and sub-components where needed.\n\nNote: Editor has no index.ts barrel, so import directly from './editor/Editor'. Viewport has an index.ts barrel. Panels have an index.ts barrel.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Create gui/src/components/ directory for shared Splitter component",
      "status": "done",
      "commit": null,
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing tests for Splitter component: renders a draggable splitter bar with data-testid, applies correct orientation (horizontal/vertical), mouse drag updates callback with delta position, respects min/max size constraints, has appropriate cursor styling.",
      "status": "done",
      "commit": "dd292bf"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Implement Splitter component (gui/src/components/Splitter.tsx + Splitter.module.css): a thin draggable bar that fires onResize(delta) on mousemove during drag. Props: orientation ('horizontal'|'vertical'), onResize callback, data-testid. Handles mousedown/mousemove/mouseup lifecycle, sets cursor on document during drag.",
      "status": "done",
      "commit": "73d2466"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing tests for wired App layout \u2014 renders all real components: test that App mounts and contains viewport-canvas (from Viewport), editor-container (from Editor), property-editor (from PropertyEditor), constraint-panel (from ConstraintPanel), toolbar (from Toolbar), status-bar (from StatusBar). Must mock bridge (getInitialState, event listeners) and Three.js/Viewport internals. Verify toolbar is at top and status bar at bottom via DOM order.",
      "status": "done",
      "commit": "46f648c"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Rewrite App.tsx to compose all real components into the layout: create engineStore, editorStore, selectionStore. Render Toolbar at top, 3-column main area (Editor left, Viewport center, PropertyEditor+ConstraintPanel right), StatusBar at bottom. Pass store data as props to each component. Wire setParameter callback through bridge.setParameter. Wire toolbar onExport/onFitToView stubs.",
      "status": "done",
      "commit": "0e52dc6"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing tests for resizable splitters in App: verify two Splitter components exist between left-center and center-right columns. Test that dragging a splitter updates panel widths (via CSS custom properties or inline styles).",
      "status": "done",
      "commit": "1349d5f"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Add resizable panel layout to App: integrate Splitter components between editor/viewport and viewport/side-panel. Track panel widths with createSignal, update on Splitter onResize. Apply widths via grid-template-columns with CSS custom properties. Update App.module.css for the full grid layout with toolbar row, main resizable area, and status bar row.",
      "status": "done",
      "commit": "b140dc6"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write failing test for initial state loading on mount: mock bridge.getInitialState to return test GuiState (meshes, values, constraints, files). Verify that after mount, engineStore is populated (values appear in PropertyEditor), editorStore has files open, and subscribeToEvents was called.",
      "status": "done",
      "commit": "19c920a"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Wire onMount in App.tsx to call getInitialState(), populate engineStore via initFromState(), open files in editorStore, and call engineStore.subscribeToEvents(). Store the unsubscribe function and call it in onCleanup.",
      "status": "done",
      "commit": "bdb951a"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write failing test for dynamic window title: verify document.title updates reactively based on active file name and evaluation status (e.g., 'bracket.ri - Reify' when idle, 'bracket.ri [evaluating] - Reify' during evaluation, 'Reify' when no file is open).",
      "status": "done",
      "commit": "00619cf"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Add createEffect in App.tsx that watches editorStore.state.activeFile and engineStore.state.evalStatus to reactively set document.title. Format: '{basename} - Reify' when idle, '{basename} [{phase}] - Reify' when evaluating/resolving, 'Reify' when no file open.",
      "status": "done",
      "commit": "5f63817"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Write failing test for vertical splitter in side panel: verify the right panel has a vertical Splitter between PropertyEditor (top) and ConstraintPanel (bottom), and that dragging it adjusts the split ratio between the two sub-panels.",
      "status": "done",
      "commit": "c22df16"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "Add vertical Splitter inside the side panel between PropertyEditor and ConstraintPanel. Track the split ratio with createSignal, apply via flex-basis or grid-template-rows. Update App.module.css for the side panel split layout.",
      "status": "done",
      "commit": "403a5da"
    }
  ],
  "design_decisions": [
    {
      "decision": "Create a dedicated Splitter component rather than using a third-party library",
      "rationale": "No resize library in dependencies. A custom Splitter is simple (~50 lines), keeps bundle small, and gives full control over theming and behavior. It needs only mousedown/mousemove/mouseup handlers."
    },
    {
      "decision": "Stores created in App and passed as props, not via SolidJS Context",
      "rationale": "App is the root component. Props are simpler, more explicit, and easier to test. Context can be added later if prop drilling becomes an issue, but with only one level of nesting it's unnecessary."
    },
    {
      "decision": "Import Editor directly from './editor/Editor' not from a barrel index",
      "rationale": "The editor directory has no index.ts barrel file (unlike viewport/ and panels/ which do). Creating one is outside this task's scope."
    },
    {
      "decision": "Use CSS grid-template-columns with dynamic values for resizable panels",
      "rationale": "CSS Grid is already used in the current layout. Using CSS custom properties or inline style for grid-template-columns allows smooth resize without re-rendering children. The pattern: `${leftWidth}px 4px 1fr 4px ${rightWidth}px` with splitter bars as fixed 4px columns."
    },
    {
      "decision": "Mock sub-components at the module level in App tests where needed (Viewport/Editor)",
      "rationale": "Viewport requires WebGL/Three.js which jsdom doesn't support. Editor requires CodeMirror DOM APIs. Mocking these at module level follows the existing pattern in Viewport.test.tsx and Editor.test.tsx. Panels are lightweight enough to render directly."
    },
    {
      "decision": "Window title set via document.title inside a createEffect",
      "rationale": "Tauri maps document.title to the OS window title automatically. A SolidJS createEffect watching the reactive store values ensures the title stays in sync."
    }
  ],
  "reuse": [
    {
      "what": "createEngineStore (stores/engineStore.ts)",
      "where": "App.tsx",
      "how": "Instantiate in App, use state for Viewport meshes, PropertyEditor values, ConstraintPanel constraints, StatusBar props. Call initFromState() and subscribeToEvents() on mount."
    },
    {
      "what": "createEditorStore (stores/editorStore.ts)",
      "where": "App.tsx",
      "how": "Instantiate in App, pass to Editor and FileTabs components. Populate with files from getInitialState()."
    },
    {
      "what": "createSelectionStore (stores/selectionStore.ts)",
      "where": "App.tsx",
      "how": "Instantiate in App, pass state.selectedEntity to PropertyEditor. Selection changes flow from Viewport click events."
    },
    {
      "what": "getInitialState() from bridge.ts",
      "where": "App.tsx onMount",
      "how": "Call on mount to fetch initial GUI state, then distribute to engine and editor stores."
    },
    {
      "what": "setParameter() from bridge.ts",
      "where": "App.tsx callback wiring",
      "how": "Pass as onSetParameter callback to PropertyEditor, wrapping the bridge call."
    },
    {
      "what": "applyTheme() from theme.ts",
      "where": "App.tsx onMount",
      "how": "Already called in current App.tsx onMount. Keep as-is."
    },
    {
      "what": "Existing test patterns (vi.mock for bridge, ResizeObserver stub, module-level mocks)",
      "where": "App.test.tsx, Splitter.test.tsx",
      "how": "Follow the same vi.mock patterns from Viewport.test.tsx and engineStore.test.ts for mocking Tauri APIs and Three.js."
    },
    {
      "what": "CSS custom properties from theme.ts (--reify-* tokens)",
      "where": "App.module.css, Splitter.module.css",
      "how": "Use existing theme variables for splitter colors, panel backgrounds, and borders to maintain visual consistency."
    }
  ],
  "_session_id": "93-46a2856b",
  "_created_at": "2026-03-21T06:44:23.940624+00:00"
}
