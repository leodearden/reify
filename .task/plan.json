{
  "task_id": "75",
  "title": "Constraint domain classification + SolverRegistry dispatch",
  "files": [
    "crates/reify-constraints/src/lib.rs",
    "crates/reify-constraints/src/solver.rs",
    "crates/reify-constraints/src/classifier.rs",
    "crates/reify-constraints/src/registry.rs",
    "crates/reify-constraints/src/decompose.rs",
    "crates/reify-constraints/Cargo.toml",
    "crates/reify-constraints/tests/classifier_tests.rs",
    "crates/reify-constraints/tests/registry_tests.rs",
    "crates/reify-constraints/tests/decompose_tests.rs",
    "crates/reify-constraints/tests/solver_integration.rs",
    "crates/reify-compiler/src/lib.rs"
  ],
  "modules": [
    "crates/reify-constraints",
    "crates/reify-compiler",
    "crates/reify-types"
  ],
  "analysis": "This task has been FULLY IMPLEMENTED. All code, tests, and integration points are in place. The implementation consists of three interlocking pieces:\n\n1. **ConstraintClassifier** (classifier.rs): Walks CompiledExpr trees and determines ConstraintDomain by collecting DomainFlags from leaf value types (Scalar/Real/Int \u2192 numeric, Bool \u2192 logical) and function calls (std::geo::* \u2192 geometric). Classification: numeric-only \u2192 Dimensional, logical-only \u2192 Logical, geometric present \u2192 Geometric, mixed \u2192 CrossDomain.\n\n2. **Connected-component decomposition** (decompose.rs): Uses union-find to build bipartite graph of constraints \u2194 auto params. Each connected component becomes an independent SubProblem with its own auto_params, constraints, and domain classification. Constraints referencing no auto params are excluded.\n\n3. **SolverRegistry** (registry.rs): Implements ConstraintSolver trait as a drop-in replacement for DimensionalSolver. Holds domain-specific solvers (dimensional required, geometric/logical/fallback optional). solve() decomposes the problem, dispatches sub-problems to appropriate solvers, and merges results.\n\n4. **Compiler integration** (reify-compiler/src/lib.rs): CompiledConstraint has `domain: Option<ConstraintDomain>` field (default None), allowing optional pre-classification at compile time.\n\nAll 50+ tests pass. The existing DimensionalSolver is used as both the dimensional and fallback solver, maintaining full backward compatibility.\n\n**POST-REVIEW FIXES (3 blocking issues):**\n\n1. **unnecessary_unsafe** (registry.rs:177-180) \u2014 FIXED in steps 13-14: Removed `unsafe impl Send/Sync for SolverRegistry` blocks.\n\n2. **type_system_inconsistency** (classifier.rs:82-84) \u2014 FIXED in steps 15-16: `Type::String` was incorrectly included in the numeric arm.\n\n3. **silent_correctness_failure** (registry.rs:104-122) \u2014 Steps 17-18: The objective assignment iterates components and assigns the objective to the first component whose auto_params intersect the objective's value refs, then stops. If the objective expression references auto params spread across multiple independent components, only the first matching component gets the objective. The design decision explicitly says params co-referenced by the objective must be merged, but this merge is never performed. Fix: modify `decompose_into_components` to accept an optional objective expression, and union all auto params referenced by the objective in the union-find structure before grouping into components. This ensures all objective-referenced params land in the same component. Then update `registry.rs` to pass the objective expression to `decompose_into_components`, and simplify the objective assignment logic (the objective always goes to the single component containing all its params).",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Add new module files (classifier.rs, registry.rs, decompose.rs) to reify-constraints/src/lib.rs with pub mod declarations and re-exports",
      "status": "done",
      "commit": "49db715",
      "tests": []
    },
    {
      "id": "pre-2",
      "description": "Add optional domain field to CompiledConstraint in reify-compiler: pub domain: Option<ConstraintDomain> (default None, non-breaking)",
      "status": "done",
      "commit": "d140236",
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing tests for ConstraintClassifier: classify `thickness > 2mm` (Scalar comparisons) \u2192 Dimensional, classify `x == true` (Bool literal eq) \u2192 Logical, classify `x > 2mm && y == true` (mixed) \u2192 CrossDomain, classify pure arithmetic on dimensionless Reals \u2192 Dimensional, classify expression with geometry function call \u2192 Geometric. Tests in crates/reify-constraints/tests/classifier_tests.rs",
      "status": "done",
      "commit": "037def6"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Implement ConstraintClassifier in crates/reify-constraints/src/classifier.rs. Walk CompiledExpr tree, track leaf value types and operator types. Classification rules: (1) collect all leaf types via recursive traversal, (2) if any FunctionCall with geometry-related qualified_name \u2192 Geometric flag, (3) if all numeric Scalar leaves \u2192 Dimensional, (4) if all Bool/Int leaves with only logical ops \u2192 Logical, (5) if mixed flags \u2192 CrossDomain. Expose pub fn classify(expr: &CompiledExpr) -> ConstraintDomain",
      "status": "done",
      "commit": "e27f569"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing tests for connected-component decomposition: (1) 3 constraints each referencing a unique auto param \u2192 3 components, (2) 2 constraints sharing one auto param \u2192 1 component, (3) chain: C1 refs {a,b}, C2 refs {b,c} \u2192 1 component with all 3 params, (4) empty constraints \u2192 0 components, (5) constraint referencing no auto params \u2192 excluded from components. Tests in crates/reify-constraints/tests/decompose_tests.rs",
      "status": "done",
      "commit": "add08d5"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Implement connected-component decomposition in crates/reify-constraints/src/decompose.rs. Define SubProblem struct {auto_params, constraints, domain: ConstraintDomain}. Use union-find: assign each auto param an index. For each constraint, collect ValueCellIds via expression tree walk (reuse collect_value_refs pattern), filter to auto param set, union all referenced params. Group constraints by component root. Determine per-component domain from individual constraint classifications (unanimous \u2192 that domain, mixed \u2192 CrossDomain). Return Vec<SubProblem>.",
      "status": "done",
      "commit": "637106a"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing tests for SolverRegistry: (1) construct with DimensionalSolver as dimensional + fallback, no geometric/logical \u2192 dispatches to dimensional solver and gets same results as DimensionalSolver alone for a simple feasibility problem, (2) problem with no geometric solver registered \u2192 fallback used for geometric-classified constraints, (3) problem decomposed into 2 independent sub-problems \u2192 both solved, merged result contains all param values, (4) backward compat: existing auto resolution test cases (from solver_integration.rs) pass through SolverRegistry. Tests in crates/reify-constraints/tests/registry_tests.rs",
      "status": "done",
      "commit": "6a1cbb9"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Implement SolverRegistry in crates/reify-constraints/src/registry.rs. Struct holds dimensional: Box<dyn ConstraintSolver>, geometric: Option<Box<dyn ConstraintSolver>>, logical: Option<Box<dyn ConstraintSolver>>, fallback: Option<Box<dyn ConstraintSolver>>. Implement ConstraintSolver trait: (1) classify each constraint, (2) decompose into sub-problems, (3) for each sub-problem select solver by domain (geometric \u2192 geometric.unwrap_or(dimensional), etc.), (4) construct ResolutionProblem per sub-problem with filtered auto_params and constraints, (5) solve each, (6) merge SolveResult values into single HashMap, (7) if any sub-problem is Infeasible \u2192 whole is Infeasible, (8) handle objective by keeping it on the component that references the objective's expression vars.",
      "status": "done",
      "commit": "d16a0ef"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write test for cross-domain iteration: create a problem with 2 constraints \u2014 one Dimensional referencing params {a, b}, one that would be classified differently but shares param b. Verify SolverRegistry merges them into a single component (since they share b) and solves correctly via fallback.",
      "status": "done",
      "commit": "d990ef9"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Handle cross-domain components in SolverRegistry: when a component has CrossDomain classification, use the fallback (dimensional) solver. This is the correct behavior for now since dimensional solver handles numeric optimization generically. Add any edge-case handling discovered from step-7 test.",
      "status": "done",
      "commit": "d990ef9"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write test for optional domain tagging in CompiledConstraint: verify that CompiledConstraint can be constructed with domain: None (backward compat) and domain: Some(ConstraintDomain::Dimensional). Verify existing compiler tests still pass with the new optional field.",
      "status": "done",
      "commit": "bc36e21"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Add domain: Option<ConstraintDomain> field to CompiledConstraint in reify-compiler/src/lib.rs. Initialize to None in compile_structure(). Update all existing construction sites. This is a pure addition \u2014 no existing behavior changes.",
      "status": "done",
      "commit": "d140236"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Write backward-compatibility integration test: run the full set of existing auto-resolution test scenarios (from solver_integration.rs) through a SolverRegistry wrapping DimensionalSolver. Verify identical results \u2014 Solved with same values, Infeasible for same infeasible problems.",
      "status": "done",
      "commit": "4fac5d2"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "Export all new public types from reify-constraints/src/lib.rs: ConstraintClassifier, SolverRegistry, SubProblem, decompose_into_components. Update lib.rs module declarations. Ensure cargo test passes for entire workspace.",
      "status": "done",
      "commit": "e8f517a"
    },
    {
      "id": "step-13",
      "type": "test",
      "description": "Write a compile-time safety test in crates/reify-constraints/tests/registry_tests.rs that statically asserts SolverRegistry is Send + Sync using fn assert_send_sync<T: Send + Sync>() {} and calling assert_send_sync::<SolverRegistry>(). This test must pass both before AND after removing the unsafe impls, confirming the compiler auto-derives these bounds from the ConstraintSolver: Send + Sync supertrait.",
      "status": "done",
      "commit": "74f2165"
    },
    {
      "id": "step-14",
      "type": "impl",
      "description": "Remove the unnecessary `unsafe impl Send for SolverRegistry {}` and `unsafe impl Sync for SolverRegistry {}` blocks (lines 177-180) and the associated safety comment (line 177) from crates/reify-constraints/src/registry.rs. The compiler auto-derives Send + Sync because all fields are Box<dyn ConstraintSolver> where ConstraintSolver: Send + Sync. Removing these restores the compiler's ability to catch future violations if a non-Send/Sync field is ever added. Verify all tests pass including the new static assertion from step-13.",
      "status": "done",
      "commit": "1932c6d"
    },
    {
      "id": "step-15",
      "type": "test",
      "description": "Write failing regression tests in crates/reify-constraints/tests/classifier_tests.rs for the type_system_inconsistency bug (reviewer: architect_reviewer). Two tests:\n\n1. `classify_string_valueref_is_not_numeric`: Create a constraint with only a `Type::String`-typed ValueRef using `value_ref_typed(\"Part\", \"name\", Type::String)` compared via `eq()` to another String-typed ValueRef. Since String is neither numeric (per `Type::is_numeric()`) nor Bool, the expression should have no domain flags set. Assert the result is NOT `ConstraintDomain::Dimensional`. The current code incorrectly classifies this as Dimensional because classifier.rs line 84 includes `Type::String` in the numeric match arm.\n\n2. `classify_string_does_not_contribute_numeric_flag`: Create an expression that combines a String-typed ValueRef with a Bool-typed ValueRef via `and()`. The String ref must NOT contribute `has_numeric`, so the result should be `ConstraintDomain::Logical` (only the Bool contributes). The current buggy code would set both `has_numeric` (from String) and `has_logical` (from Bool), producing `CrossDomain` instead.\n\nBoth tests will FAIL against the current classifier.rs code, proving the bug exists.",
      "status": "done",
      "commit": "d2d6431"
    },
    {
      "id": "step-16",
      "type": "impl",
      "description": "Fix the type_system_inconsistency in crates/reify-constraints/src/classifier.rs (reviewer: architect_reviewer). In the `CompiledExprKind::ValueRef` arm of `collect_flags` (lines 80-87), replace the manual type match with the type system's canonical `is_numeric()` method. Change from:\n```rust\nmatch &expr.result_type {\n    Type::Bool => flags.has_logical = true,\n    Type::Int | Type::Real | Type::Scalar { .. } | Type::String => {\n        flags.has_numeric = true;\n    }\n}\n```\nTo:\n```rust\nif expr.result_type.is_numeric() {\n    flags.has_numeric = true;\n} else if expr.result_type == Type::Bool {\n    flags.has_logical = true;\n}\n// Type::String is a no-op \u2014 no domain flag set, consistent with Type::is_numeric()\n```\nThis aligns with the canonical `Type::is_numeric()` definition in reify-types/src/ty.rs:42 which returns `matches!(self, Type::Int | Type::Real | Type::Scalar { .. })`, explicitly excluding String. Verify all existing classifier tests plus the new step-15 regression tests pass.",
      "status": "done",
      "commit": "4222aa3"
    },
    {
      "id": "step-17",
      "type": "test",
      "description": "Write failing tests for the silent_correctness_failure bug (reviewer: reviewer_robustness) — objective spanning multiple independent components must merge them.\n\n1. In `crates/reify-constraints/tests/registry_tests.rs`, add `objective_spanning_independent_components_merges_them`: Create params `a` (bounds [1mm, 100mm]) and `b` (bounds [1mm, 100mm]) with INDEPENDENT constraints `a > 5mm` and `b > 5mm` (no shared auto params, so without the fix these are 2 separate components). Add objective `Minimize(a + b)` which references both params. Assert both `a` and `b` are near 5mm (their lower feasible bound), since minimizing `a + b` jointly should push both toward the minimum. With the bug, only the first matching component gets the objective — the other component's param is solved without optimization and will likely be far from 5mm.\n\n2. In `crates/reify-constraints/tests/decompose_tests.rs`, add `objective_merges_independent_params_into_one_component`: Create 2 independent params `a` and `b` with independent constraints, and call `decompose_into_components` with a new third argument: `Some(&objective_expr)` where `objective_expr = a + b`. Assert the result is 1 component containing both params. This test will fail to compile because `decompose_into_components` currently only takes 2 arguments — the compilation failure IS the failing test, proving the API doesn't support objective-aware decomposition.",
      "status": "done",
      "commit": "e8d51fc"
    },
    {
      "id": "step-18",
      "type": "impl",
      "description": "Fix the silent_correctness_failure in decompose.rs and registry.rs (reviewer: reviewer_robustness).\n\n**decompose.rs changes:**\n1. Change `decompose_into_components` signature from `(auto_params, constraints)` to `(auto_params, constraints, objective: Option<&CompiledExpr>)`. Add a new parameter for the optional objective expression.\n2. After the constraint-based union-find loop (line 166) and before grouping by component root (line 173), add: if `objective` is `Some(obj_expr)`, collect its value refs via `collect_value_refs`, filter to auto param indices, and union all those indices together. This forces all objective-referenced auto params into the same component.\n3. Update existing callers: the `decompose_tests.rs` tests that call `decompose_into_components` with 2 args must be updated to pass `None` as the third argument.\n\n**registry.rs changes:**\n1. Extract the objective expression from `problem.objective` before calling `decompose_into_components`, and pass it as the third argument.\n2. Simplify the `objective_component` logic: since the objective's params are now guaranteed to be in one component (thanks to the union-find merge), the existing first-match iteration is correct — but we can simplify by noting it will always find exactly one matching component. No behavioral change needed in this section, only the `decompose_into_components` call site.\n\nVerify all existing tests plus new step-17 tests pass.",
      "status": "done",
      "commit": "18946bb"
    }
  ],
  "design_decisions": [
    {
      "decision": "SolverRegistry implements ConstraintSolver trait",
      "rationale": "This makes SolverRegistry a drop-in replacement for DimensionalSolver in the Engine. The Engine holds Option<Box<dyn ConstraintSolver>> and calls .solve() \u2014 no changes needed in reify-eval. Users can pass either DimensionalSolver or SolverRegistry."
    },
    {
      "decision": "Union-find for connected components instead of BFS/DFS on adjacency list",
      "rationale": "Union-find is O(\u03b1(n)) per operation, simpler to implement correctly for bipartite constraint\u2194param graphs, and avoids building explicit adjacency lists. Well-suited for the merge-by-shared-param pattern."
    },
    {
      "decision": "Classification walks the expression tree rather than relying on compiler-assigned domain tags",
      "rationale": "The classifier needs to work on arbitrary CompiledExpr at solve time, not just at compile time. The optional domain field on CompiledConstraint is for caching/hints, but the authoritative classification happens in the solver pipeline. This decouples the classifier from the compiler."
    },
    {
      "decision": "CrossDomain components use the fallback (dimensional) solver",
      "rationale": "The dimensional solver (Nelder-Mead penalty method) is the most general solver \u2014 it handles arbitrary numeric constraints. When constraints span domains, falling back to it is safe. Future work can add cross-domain iteration strategies."
    },
    {
      "decision": "Objective stays with the component containing its referenced variables",
      "rationale": "An optimization objective (Minimize/Maximize) only makes sense in the context of the sub-problem whose params it references. If the objective references params in multiple components, those components must be merged (they'll share params via the objective expression)."
    },
    {
      "decision": "New files (classifier.rs, registry.rs, decompose.rs) rather than expanding solver.rs",
      "rationale": "solver.rs is already 1600+ lines with the DimensionalSolver. Separate files maintain single-responsibility: classifier classifies, decomposer decomposes, registry orchestrates. Each is independently testable."
    },
    {
      "decision": "Collect ValueCellIds from expressions inline rather than depending on reify-eval::deps",
      "rationale": "The collect_value_refs function in reify-eval/src/deps.rs does exactly what we need, but reify-constraints cannot depend on reify-eval (that would create a circular dependency). We implement a local version of the same walk in decompose.rs."
    },
    {
      "decision": "No changes to reify-types for this task",
      "rationale": "ConstraintDomain and all needed types already exist in reify-types. The SolverRegistry and classifier are implementation concerns that belong in reify-constraints. The only structural change is adding the optional domain field to CompiledConstraint in reify-compiler."
    },
    {
      "decision": "Remove unnecessary unsafe impl Send/Sync for SolverRegistry",
      "rationale": "ConstraintSolver trait has Send + Sync supertraits (reify-types/src/constraint.rs:108), so Box<dyn ConstraintSolver> is automatically Send + Sync. Since all SolverRegistry fields are Box<dyn ConstraintSolver> or Option<Box<dyn ConstraintSolver>>, the compiler auto-derives Send + Sync. The manual unsafe impls bypass future compiler safety checks: if a non-Send field is ever added, the compiler would silently accept it instead of emitting an error. A static assertion test is added first to prove auto-derivation works before removing the unsafe blocks."
    },
    {
      "decision": "Use Type::is_numeric() for ValueRef classification instead of manual match",
      "rationale": "The type system already has a canonical is_numeric() method (reify-types/src/ty.rs:42) that correctly defines numeric types as Int | Real | Scalar { .. }, explicitly excluding String. The classifier's ValueRef arm had a manual match that incorrectly included Type::String as numeric. Using the canonical method ensures the classifier stays consistent with the type system as new types are added, and prevents future divergence bugs. The Literal arm already uses Value variant matching which is inherently correct (Value::String doesn't exist as a numeric variant), so only the ValueRef arm needed fixing."
    },
    {
      "decision": "Objective-aware decomposition: union objective-referenced params in the union-find rather than post-hoc component merging",
      "rationale": "The objective expression acts as an additional linkage between auto params, identical to how constraints link params. Unioning objective-referenced params in the existing union-find structure (before grouping into components) is the simplest and most correct approach: it reuses the existing infrastructure, requires no post-hoc component merging logic, and guarantees all objective-referenced params end up in a single component. The alternative (detect multi-component objectives after decomposition and merge components) would require rebuilding component data structures and is more error-prone."
    }
  ],
  "reuse": [
    {
      "what": "ConstraintDomain enum",
      "where": "crates/reify-types/src/constraint.rs:34-44",
      "how": "Already defines Dimensional, Geometric, Logical, CrossDomain variants. Used directly for classification output."
    },
    {
      "what": "ConstraintSolver trait",
      "where": "crates/reify-types/src/constraint.rs:108-111",
      "how": "SolverRegistry implements this trait, making it a drop-in replacement for DimensionalSolver in Engine."
    },
    {
      "what": "DimensionalSolver",
      "where": "crates/reify-constraints/src/solver.rs:34",
      "how": "Used as both the 'dimensional' and 'fallback' solver in SolverRegistry. No modifications needed."
    },
    {
      "what": "CompiledExprKind tree structure",
      "where": "crates/reify-types/src/expr.rs",
      "how": "Classifier walks this tree to determine leaf types and operator domains. Same traversal pattern as collect_value_refs in reify-eval/src/deps.rs."
    },
    {
      "what": "collect_value_refs pattern (expression tree walk)",
      "where": "crates/reify-eval/src/deps.rs",
      "how": "The decomposer needs the same recursive walk to find which ValueCellIds a constraint references. Cannot import from reify-eval (circular dep), so reimplemented locally in decompose.rs."
    },
    {
      "what": "ResolutionProblem struct",
      "where": "crates/reify-types/src/constraint.rs:87-97",
      "how": "SolverRegistry constructs sub-ResolutionProblems per component, filtering auto_params and constraints to the component's subset."
    },
    {
      "what": "Test helper patterns (mm(), vcid(), cnid(), expression builders)",
      "where": "crates/reify-test-support/src/values.rs and crates/reify-test-support/src/builders.rs",
      "how": "Used extensively in all test files for constructing test expressions. Includes gt(), lt(), and(), eq(), value_ref(), literal(), value_ref_typed(), binop()."
    },
    {
      "what": "Engine::with_solver() builder pattern",
      "where": "crates/reify-eval/src/lib.rs",
      "how": "No changes needed \u2014 Engine already accepts Box<dyn ConstraintSolver>. SolverRegistry can be boxed and passed directly."
    },
    {
      "what": "Type::is_numeric() method",
      "where": "crates/reify-types/src/ty.rs:41-43",
      "how": "Canonical definition of which types are numeric: `matches!(self, Type::Int | Type::Real | Type::Scalar { .. })`. Used in step-16 to fix the classifier's ValueRef arm to be consistent with the type system, replacing the manual match that incorrectly included Type::String."
    },
    {
      "what": "collect_value_refs_pub in decompose.rs",
      "where": "crates/reify-constraints/src/decompose.rs:67-69",
      "how": "Already exists as pub(crate) and is used by registry.rs for extracting objective value refs. The same function is reused in the new objective-aware decomposition logic to collect objective param refs for the union-find."
    },
    {
      "what": "binop() test helper with BinOp::Add",
      "where": "crates/reify-test-support/src/builders.rs:39-41",
      "how": "Used to construct `a + b` objective expressions in step-17 tests. The `binop(BinOp::Add, ...)` helper infers result type (same dimension as left operand for Add)."
    }
  ],
  "_schema_version": 1
}
