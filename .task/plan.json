{
  "task_id": "53",
  "title": "Extend Value/Type enums for M5 types",
  "files": [
    "crates/reify-types/src/value.rs",
    "crates/reify-types/src/ty.rs",
    "crates/reify-types/src/traits.rs",
    "crates/reify-types/src/lib.rs",
    "crates/reify-types/tests/m5_types.rs",
    "crates/reify-types/tests/exports.rs"
  ],
  "modules": ["crates/reify-types"],
  "analysis": "This task is FULLY IMPLEMENTED for the M5 type extensions. However, code review identified a blocking bug: content_hash for Value::Real and Value::Scalar normalizes -0.0 to 0.0 before hashing (lines 84, 95), but PartialEq uses to_bits() comparison (lines 150, 153), making -0.0 != 0.0. This violates the cache invariant: values that are not equal MUST have different content hashes (with high probability), otherwise the incremental caching system will suppress recomputation producing silently stale results. The fix (Option A from review) is to remove -0.0 normalization from content_hash and use to_bits() directly, keeping NaN canonicalization intact. The existing test `neg_zero_normalized` (line 417) asserts the broken behavior and must be updated. Steps 1-11 are the original verification-only steps (all done). Steps 12-15 address the blocking review feedback.",
  "prerequisites": [],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Verify Value::Enum variant exists with Display (Color::Red), PartialEq, Ord, and content_hash — tests already exist in value.rs (value_enum_debug, value_enum_equality, value_enum_ordering, value_enum_content_hash, value_display_enum)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-2",
      "type": "test",
      "description": "Verify Value::List variant with Display ([1, 2, 3]), equality, ordering, nesting, and content_hash — tests already exist in value.rs (value_list_equality, value_list_empty, value_list_nested, value_list_ordering, value_list_content_hash, value_display_list)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Verify Value::Set with BTreeSet operations (insert, contains), equality regardless of insertion order, ordering, content_hash — tests already exist in value.rs (value_set_basic, value_set_equality, value_set_ordering, value_set_content_hash, value_display_set)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-4",
      "type": "test",
      "description": "Verify Value::Map with BTreeMap operations (insert, get), equality, ordering, content_hash — tests already exist in value.rs (value_map_basic, value_map_equality, value_map_ordering, value_map_content_hash, value_display_map)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Verify Value::Option(Some/None) with Display (Some(42)/None), equality, ordering (None < Some), content_hash — tests already exist in value.rs (value_option_some_and_none, value_option_equality, value_option_ordering, value_option_content_hash, value_display_option)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-6",
      "type": "test",
      "description": "Verify Value::Ord cross-type ordering: Undef < Bool < Int < Real < Scalar < String < Enum < List < Set < Map < Option — test already exists in value.rs (value_ord_cross_type_ordering, plus within-type tests)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Verify Type::Enum, List, Set, Map, Option, Function variants with Display output — tests already exist in ty.rs (type_enum_display, type_list_display, type_set_display, type_map_display, type_option_display, type_function_display, type_function_multi_params_display, type_new_variants_not_numeric)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-8",
      "type": "test",
      "description": "Verify EnumDef construction, field access, and contains_variant — tests in tests/m5_types.rs (enum_def_construction_and_lookup, enum_def_debug_clone_eq)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Verify TraitDef, TraitMember (all 6 variants), TraitRef, TraitBound, TypeParam, PortDirection construction and trait bounds (Debug, Clone, PartialEq, Eq) — tests in tests/m5_types.rs (trait_def_full_construction, trait_member_param/port/sub/let_and_constraint/associated_type, trait_ref_construction/equality, trait_bound_wraps_trait_ref, type_param_construction/no_bounds_no_default, port_direction_variants/debug_clone_eq_copy_hash)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-10",
      "type": "test",
      "description": "Verify all M5 types are exported from crate root (lib.rs re-exports) — tests in tests/exports.rs (all_m5_types_exported) and tests/m5_types.rs (all_m5_types_exported_from_crate_root, value_display_via_public_api)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Verify no cross-domain hash collisions between Value and Satisfaction content hashes — tests in value.rs (value_option_none_hash_not_equal_satisfaction_satisfied, value_option_some_hash_not_equal_satisfaction_violated, value_and_satisfaction_content_hash_tags_no_cross_domain_collisions)",
      "status": "done",
      "commit": "0201008"
    },
    {
      "id": "step-12",
      "type": "test",
      "description": "Write failing tests for hash-equality invariant on Real and Scalar -0.0: (1) `real_neg_zero_hash_differs_from_pos_zero` — asserts Value::Real(-0.0).content_hash() != Value::Real(0.0).content_hash(), since PartialEq considers them different (to_bits). (2) `scalar_neg_zero_hash_differs_from_pos_zero` — same for Value::Scalar with si_value -0.0 vs 0.0. (3) `hash_equality_invariant_real` — asserts that for any two Value::Real(a), Value::Real(b), if a != b then a.content_hash() != b.content_hash() (spot-check with -0.0/0.0 pair). Also update existing `neg_zero_normalized` test to assert_ne instead of assert_eq (rename to `real_neg_zero_not_normalized_in_hash`). These tests will FAIL against the current implementation which normalizes -0.0 to 0.0 in content_hash.",
      "status": "done",
      "commit": "7ae369a"
    },
    {
      "id": "step-13",
      "type": "impl",
      "description": "Fix content_hash for Value::Real: remove -0.0 normalization, use to_bits() directly. Change line 84 from `let normalized = if *r == 0.0 { 0.0f64 } else { *r };` to just use `*r` directly (keeping NaN canonicalization). The resulting code: `let bits = if r.is_nan() { f64::NAN.to_bits() } else { r.to_bits() };`. This makes content_hash consistent with PartialEq (both use to_bits). All step-12 tests should now pass.",
      "status": "done",
      "commit": "e46ca1d"
    },
    {
      "id": "step-14",
      "type": "test",
      "description": "Write failing test for Scalar hash-equality invariant: `scalar_neg_zero_hash_consistency` — asserts Value::Scalar { si_value: -0.0, dimension: LENGTH }.content_hash() != Value::Scalar { si_value: 0.0, dimension: LENGTH }.content_hash(). This test will FAIL because the Scalar content_hash (line 95) still normalizes -0.0.",
      "status": "done",
      "commit": "ab5761b"
    },
    {
      "id": "step-15",
      "type": "impl",
      "description": "Fix content_hash for Value::Scalar: remove -0.0 normalization from si_value hashing. Change line 95 from `let normalized = if *si_value == 0.0 { 0.0f64 } else { *si_value };` to use `*si_value` directly (keeping NaN canonicalization). The resulting code: `let bits = if si_value.is_nan() { f64::NAN.to_bits() } else { si_value.to_bits() };`. Run full test suite to confirm all 171+ tests pass with the fix.",
      "status": "done",
      "commit": "d7c2eaf"
    }
  ],
  "design_decisions": [
    {
      "decision": "All work already implemented — plan is verification-only",
      "rationale": "All Value variants (Enum, List, Set, Map, Option), Type variants (Enum, List, Set, Map, Option, Function), trait definition types (EnumDef, TraitDef, TraitMember, TraitRef, TraitBound, TypeParam, PortDirection), Ord for Value, Display, content_hash, and all tests (171 total) are already in place and passing. No code changes are needed."
    },
    {
      "decision": "Value::Ord uses type-tag discriminant for cross-type ordering",
      "rationale": "Undef=0, Bool=1, Int=2, Real=3, Scalar=4, String=5, Enum=6, List=7, Set=8, Map=9, Option=10. This provides a deterministic total order required for BTreeSet<Value> and BTreeMap<Value, Value>."
    },
    {
      "decision": "f64 ordering uses to_bits() for total order consistency",
      "rationale": "NaN and -0.0 need deterministic ordering. Using to_bits() gives a total order consistent with the PartialEq implementation."
    },
    {
      "decision": "Value::Option content_hash uses tag [11] to avoid collision with Satisfaction tag [10]",
      "rationale": "Satisfaction uses tag [10] exclusively. Value::Option originally collided; moved to tag [11] with regression tests to prevent future collisions."
    },
    {
      "decision": "TraitMember::Sub uses trait_ref instead of plain type",
      "rationale": "Sub-structures reference traits (which can have type arguments), not raw types. Using TraitRef allows Sub { name: 'child', trait_ref: TraitRef { name: 'Component', type_args: [] } }."
    },
    {
      "decision": "TraitMember::Port includes PortDirection field",
      "rationale": "Ports need direction information (In, Out, Bidi) for connection checking in M5. Including it in the Port variant keeps port metadata colocated."
    },
    {
      "decision": "Fix hash-equality invariant using Option A: remove -0.0 normalization from content_hash, use to_bits() directly",
      "rationale": "Review identified that content_hash normalizes -0.0→0.0 but PartialEq uses to_bits() (making -0.0 != 0.0). This breaks the cache invariant: unequal values must hash differently. Option A (use to_bits() in hash) is lower risk than Option B (normalize in Eq) because it requires no change to Ord/Eq contracts already relied upon by BTreeSet/BTreeMap. NaN canonicalization is retained since it's consistent (NaN == NaN via to_bits(), and canonical NaN bits are stable)."
    }
  ],
  "reuse": [
    {
      "what": "ContentHash infrastructure",
      "where": "crates/reify-types/src/hash.rs",
      "how": "Used ContentHash::of() and .combine() for all new Value variant content hashes, following existing patterns from Bool/Int/Real/String/Scalar/Undef."
    },
    {
      "what": "DimensionVector",
      "where": "crates/reify-types/src/dimension.rs",
      "how": "Scalar variant's dimension field and Ord implementation comparing dimensions first, then value bits."
    },
    {
      "what": "Existing test patterns",
      "where": "crates/reify-types/src/value.rs (tests module)",
      "how": "New variant tests follow the same structure: construction, equality, ordering, content_hash determinism, Display output."
    },
    {
      "what": "BTreeSet/BTreeMap from std",
      "where": "std::collections",
      "how": "Used directly for Value::Set and Value::Map, enabled by implementing Ord on Value."
    }
  ]
}
