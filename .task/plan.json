{
  "task_id": "66",
  "title": "Trait conformance + compilation",
  "files": [
    "crates/reify-compiler/src/lib.rs",
    "crates/reify-compiler/tests/trait_conformance_tests.rs"
  ],
  "modules": ["crates/reify-compiler"],
  "analysis": "Task 65 (grammar + parser for traits) is done. The parser already produces Declaration::Trait(TraitDecl) with name, is_pub, type_params, refinements, members (Vec<MemberDecl>), and structures have trait_bounds: Vec<String>. The compiler currently skips trait declarations with a comment 'Trait compilation deferred to a later milestone' at line 961. This task implements:\n\n1. **CompiledTrait data structures** — new types: CompiledTrait, TraitRequirement, RequirementKind, TraitDefault, DefaultKind.\n2. **Trait compilation** — a pre-pass that compiles TraitDecl into CompiledTrait, builds a trait registry (HashMap<String, CompiledTrait>).\n3. **Conformance checking** — when compiling a structure with non-empty trait_bounds, resolve each trait, recursively collect all requirements (including from refinement chains), and verify the structure satisfies them.\n4. **Default merging** — if a trait provides defaults and structure doesn't override, inject the defaults into the TopologyTemplate.\n5. **Composition conflict detection** — same name + different types across traits → error.\n\nKey observations from code reading:\n- resolve_type_name (line 333) currently only handles Scalar, Bool, Int, Real, String. For trait conformance with type names like 'Length' and 'Mass' in tests, we extend it to map Length → Scalar{LENGTH}, Mass → Scalar{MASS}.\n- Trait members use the same MemberDecl enum as structures, so we reuse compile_expr and type resolution.\n- The trait registry is module-local (matches existing pattern where cross-module import resolution is noted but not fully wired).\n- The compile() function (line 889) handles pre-passes for enums and functions. We add a similar pre-pass for traits.\n- compile_structure() (line 1038) is the main function to modify for conformance checking.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Extend resolve_type_name() (lib.rs:333) to recognize 'Length' as Type::Scalar{dimension: DimensionVector::LENGTH} and 'Mass' as Type::Scalar{dimension: DimensionVector::MASS}. These are dimensional scalar aliases needed for trait conformance tests.",
      "status": "done",
      "commit": "58cb0ac",
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing test: compile a trait declaration produces CompiledTrait in CompiledModule.trait_defs. Parse `trait Fastener { param thread_pitch : Length }`, verify module.trait_defs has 1 entry with correct name 'Fastener' and 1 required member named 'thread_pitch' with RequirementKind::Param(Scalar{LENGTH}).",
      "status": "done",
      "commit": "a3bb156"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Add CompiledTrait, TraitRequirement, RequirementKind, TraitDefault, DefaultKind structs to lib.rs. Add trait_defs: Vec<CompiledTrait> field to CompiledModule. Implement compile_trait() function that converts TraitDecl into CompiledTrait by iterating members: Param without default → RequirementKind::Param(Type), Param with default → TraitDefault, Let without value → RequirementKind::Let(Type), Constraint → RequirementKind::Constraint, Sub → RequirementKind::Sub(structure_name). Wire into compile() pre-pass replacing the no-op Trait(_) => {} arm. Include trait_defs content hashes in module content_hash.",
      "status": "done",
      "commit": "06f7f0a"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing test: simple conformance — `trait Fastener { param thread_pitch : Length }` and `structure def Bolt : Fastener { param thread_pitch : Length = 20mm }` — compiles with no error-severity diagnostics.",
      "status": "done",
      "commit": "238d31f"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Implement trait registry (HashMap<String, &CompiledTrait>) built in compile() after trait pre-pass. Add trait_registry parameter to compile_structure(). Implement check_trait_conformance() that: resolves trait by name in registry, iterates required_members, checks each against structure members by name and type. Pass diagnostics for unresolvable trait names. For satisfied requirements, no diagnostic.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing test: missing member — `trait Fastener { param thread_pitch : Length }`, `structure def Bolt : Fastener { param length : Length = 10mm }` → error diagnostic containing 'missing required member' and 'thread_pitch'.",
      "status": "done",
      "commit": "6944627"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Ensure check_trait_conformance produces an error diagnostic when a required member name is not found among structure members. Error message includes trait name, missing member name, and expected type.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write failing test: type mismatch — `trait Weighted { param mass : Mass }`, `structure def S : Weighted { param mass : Length = 5mm }` → error diagnostic about type mismatch (expected Mass, got Length).",
      "status": "done",
      "commit": "fcbe483"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Add type compatibility checking in conformance: when a structure has the required member by name but the type doesn't match exactly, emit an error diagnostic with expected vs actual type. Use Type equality (==) for conformance checking.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write failing test: default merging — `trait HasSize { param size : Length = 10mm }`, `structure def S : HasSize { }` → S should compile without errors, and the TopologyTemplate should contain a value cell for 'size' with the trait's default expression (10mm).",
      "status": "done",
      "commit": "c17565b"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Implement default member injection: after conformance checking identifies defaults not overridden by the structure, inject them as additional value cells (for param/let defaults) or constraints (for constraint defaults) into the TopologyTemplate. Compile default expressions using a temporary CompilationScope with the structure's existing members.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Write test: default override — `trait HasSize { param size : Length = 10mm }`, `structure def S : HasSize { param size : Length = 20mm }` → structure's value (20mm) is used, no error, only one 'size' value cell exists.",
      "status": "done",
      "commit": "fe298d5"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "Ensure conformance logic correctly detects that a structure-provided member satisfies the requirement even when a default exists. The member's presence prevents default injection. Verify with test from step-11.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-13",
      "type": "test",
      "description": "Write failing test: multiple trait bounds — `trait A { param a : Length }`, `trait B { param b : Length }`, `structure def X : A + B { param a : Length = 1mm  param b : Length = 2mm }` → no errors, both requirements satisfied.",
      "status": "done",
      "commit": "092b719"
    },
    {
      "id": "step-14",
      "type": "impl",
      "description": "Ensure conformance checking iterates over all entries in structure.trait_bounds, resolving and checking each trait independently. All requirement violations collected before returning.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-15",
      "type": "test",
      "description": "Write failing test: diamond inheritance — `trait C { param x : Length }`, `trait A : C { }`, `trait B : C { }`, `structure def X : A + B { param x : Length = 5mm }` → no errors. The requirement from C, reachable via both A and B, is checked once.",
      "status": "done",
      "commit": "730ccc3"
    },
    {
      "id": "step-16",
      "type": "impl",
      "description": "Implement collect_all_requirements(trait_name, registry) that recursively walks refinement chains, collecting requirements from parent traits. Deduplicate by member name: if the same name appears from multiple paths with the same type, keep one. If same name with different types, it's a conflict (handled in step-18). Use a HashSet<String> to track visited traits to handle diamonds.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-17",
      "type": "test",
      "description": "Write failing test: composition conflict — `trait A { param size : Length }`, `trait B { param size : Mass }`, `structure def X : A + B { param size : Length = 5mm }` → error diagnostic about conflicting requirements for 'size' (Length vs Mass).",
      "status": "done",
      "commit": "b70ef23"
    },
    {
      "id": "step-18",
      "type": "impl",
      "description": "Implement conflict detection: when collecting requirements from multiple traits (including via refinement chains), if two requirements share a member name but have different types, emit a compile error about the conflict. This check happens during requirement collection before per-member checking.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-19",
      "type": "test",
      "description": "Write failing test: deep trait chain — `trait A { param x : Length }`, `trait B : A { param y : Length }`, `trait C : B { param z : Length }`, `structure def S : C { param x : Length = 1mm  param y : Length = 2mm  param z : Length = 3mm }` → S must satisfy requirements from A (x), B (y), and C (z). No errors.",
      "status": "done",
      "commit": "3fe8942"
    },
    {
      "id": "step-20",
      "type": "impl",
      "description": "Verify transitive requirement collection handles deep chains (C→B→A). The collect_all_requirements function should walk the entire chain. Fix if needed.",
      "status": "done",
      "commit": "3fb1b5a"
    },
    {
      "id": "step-21",
      "type": "test",
      "description": "Write failing test: constraint from trait — `trait Safe { constraint x > 0 }`, structure conforms without having a matching constraint → default constraint is injected. Also test: two traits require a labeled constraint 'min_size'. A structure with a single 'min_size' constraint satisfies both.",
      "status": "done",
      "commit": "2c1445b"
    },
    {
      "id": "step-22",
      "type": "impl",
      "description": "Implement constraint requirement and default handling: Constraint members in traits become requirements (matched by label if labeled, or injected as defaults if they have an expression). For labeled constraints, a single constraint in the structure with matching label satisfies requirements from multiple traits.",
      "status": "done",
      "commit": "2c1445b"
    }
  ],
  "design_decisions": [
    {
      "decision": "Extend resolve_type_name to support 'Length' → Scalar{LENGTH} and 'Mass' → Scalar{MASS}",
      "rationale": "The language spec and existing trait parser tests use type names like 'Length' and 'Mass'. These need to resolve to dimensioned scalar types for conformance type checking to work. This is a small, safe addition to an existing function."
    },
    {
      "decision": "Build trait registry as module-local HashMap<String, CompiledTrait>",
      "rationale": "Follows existing pattern where cross-module resolution is noted but not implemented. Import resolution for traits can be added when multi-module compilation is fully wired up."
    },
    {
      "decision": "Requirements vs defaults determined by presence of default expression",
      "rationale": "A trait member with no default is a requirement (must be provided by the structure). A trait member with a default (e.g., param size : Length = 10mm) provides a fallback. For params, the default is ParamDecl.default. For lets, the LetDecl always has a value so all lets are either requirements (value references undefined names) or defaults."
    },
    {
      "decision": "Conformance checking happens inside compile_structure, after first-pass name registration but before reference safety pass",
      "rationale": "Default members need to be injected into the structure's value_cells/constraints before the reference safety analysis runs, so conformance checking and default injection must happen during structure compilation."
    },
    {
      "decision": "Deduplication of requirements by name for diamond patterns",
      "rationale": "When A : C and B : C, and X : A + B, the requirement from C appears via both paths. Deduplication by member name ensures it's only checked once and prevents false 'conflict' errors when types agree."
    },
    {
      "decision": "Type matching for conformance uses exact type equality (==), not type_compatible()",
      "rationale": "For trait conformance, the structure member must have exactly the same type as the trait requirement. The existing type_compatible() allows Int→Real widening which would be too permissive for trait contracts where dimensional precision matters."
    },
    {
      "decision": "Trait members that are lets with expressions are treated as defaults (not requirements)",
      "rationale": "In the TraitDecl AST, LetDecl always has a value expression. A let binding with an expression provides a computed default. If the structure provides its own let with the same name, the structure's version takes precedence."
    },
    {
      "decision": "Params without defaults in traits are strict requirements",
      "rationale": "A param declared in a trait without a default (no = expr) means the conforming structure MUST provide it. This is the fundamental contract of trait conformance."
    }
  ],
  "reuse": [
    {
      "what": "resolve_type_name() function",
      "where": "crates/reify-compiler/src/lib.rs:333",
      "how": "Extend with 'Length' and 'Mass' cases. Reuse for resolving types in trait member compilation."
    },
    {
      "what": "compile_expr() and compile_expr_guarded()",
      "where": "crates/reify-compiler/src/lib.rs:469-884",
      "how": "Reuse for compiling default expressions in trait members when injecting defaults."
    },
    {
      "what": "CompilationScope",
      "where": "crates/reify-compiler/src/lib.rs:437-464",
      "how": "Create a temporary scope when compiling trait default expressions during injection."
    },
    {
      "what": "Test helper pattern: compile_first_template()",
      "where": "crates/reify-compiler/tests/guard_compilation.rs:10-16",
      "how": "Create similar helper for trait conformance tests that returns (CompiledModule) or (TopologyTemplate, Vec<Diagnostic>)."
    },
    {
      "what": "ContentHash combining pattern",
      "where": "crates/reify-compiler/src/lib.rs:996-1024",
      "how": "Follow same pattern for CompiledTrait content hashing and including trait_defs in module hash."
    },
    {
      "what": "Existing MemberDecl enum and member processing logic",
      "where": "reify_syntax::MemberDecl (crates/reify-syntax/src/lib.rs:38-47)",
      "how": "Trait members use the same MemberDecl variants (Param, Let, Constraint, Sub). Reuse the same match arms when categorizing trait members into requirements and defaults."
    },
    {
      "what": "Diagnostic construction patterns",
      "where": "crates/reify-compiler/src/lib.rs throughout",
      "how": "Follow existing patterns: Diagnostic::error(msg).with_label(DiagnosticLabel::new(span, hint)). Push to diagnostics vec."
    }
  ]
}
