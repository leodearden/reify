{
  "task_id": "286",
  "title": "Complex<Q>: Type and Value",
  "files": [
    "crates/reify-types/src/ty.rs",
    "crates/reify-types/src/value.rs",
    "crates/reify-constraints/src/classifier.rs",
    "crates/reify-lsp/src/analysis.rs"
  ],
  "modules": [
    "crates/reify-types",
    "crates/reify-constraints",
    "crates/reify-lsp"
  ],
  "analysis": "Task adds complex number support to the Reify type system at the foundational reify-types level. The Type enum (ty.rs) currently has 18 variants with #[derive(Debug, Clone, PartialEq, Eq, Hash)]; adding Complex(Box<Type>) follows the established compound-type pattern used by List, Set, Option, Point, Vector, Tensor. The Value enum (value.rs) currently has 14 variants with manual PartialEq, Ord, Display, and content_hash implementations; each must gain a Complex arm. Content hash tags 0-14 are allocated (10 reserved for Satisfaction), so Complex gets tag 15. Ord type_tag values 0-13 are allocated, so Complex gets tag 14. The f64 comparison pattern (to_bits for equality, NaN canonicalization for hashing) is well-established via Real and Scalar and will be applied to both re and im components. The dimension field follows Scalar's pattern \u2014 both re and im share one DimensionVector (e.g., complex impedance in ohms). Factory method Type::complex(q) follows the vec2/vec3/point2/point3/tensor pattern. All changes are confined to crates/reify-types; downstream crates with exhaustive matches on Type/Value will need separate wildcard or explicit arms (out of scope for this task but noted: reify-lsp/analysis.rs, reify-constraints/classifier.rs, reify-expr/lib.rs, reify-stdlib/lib.rs, reify-eval/lib.rs, reify-compiler/lib.rs, reify-constraints/cpsat.rs, reify-constraints/solver.rs). Post-review: two downstream files (classifier.rs and analysis.rs) contain bugs introduced by this task's Complex variant that must be fixed here \u2014 the classifier incorrectly routes Complex literals as numeric (violating the is_numeric() design contract), and format_value produces malformed output for negative imaginary parts.",
  "prerequisites": [],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing tests in ty.rs::tests for Type::Complex: (a) Display format \u2014 Complex<Real>, Complex<Scalar[m]>, nested Complex<Complex<Real>>; (b) factory method Type::complex(q) == Type::Complex(Box::new(q)); (c) Eq/Hash consistency via HashMap insert+lookup for complex(Real) vs complex(Int); (d) is_numeric() returns false; (e) as_name() returns None.",
      "status": "done",
      "commit": "adedeee"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Add Complex(Box<Type>) variant to Type enum in ty.rs. Add Display arm: Complex(inner) => write!(f, \"Complex<{}>\", inner). Add factory: pub fn complex(q: Type) -> Self { Type::Complex(Box::new(q)) }. No changes needed for is_numeric or as_name (existing catch-alls and non-matching patterns handle them correctly).",
      "status": "done",
      "commit": "05a589a"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing tests in value.rs::tests for Value::Complex Display: (a) dimensionless positive imaginary \u2014 Complex { re: 3.0, im: 4.0, dimension: DIMENSIONLESS } displays as '3+4i'; (b) negative imaginary \u2014 re: 3.0, im: -4.0 displays as '3-4i'; (c) fractional parts \u2014 re: 3.5, im: 4.2 displays as '3.5+4.2i'; (d) dimensioned with LENGTH \u2014 (3+4i) m; (e) zero imaginary \u2014 3+0i; (f) negative real \u2014 -3-4i.",
      "status": "done",
      "commit": "3188bef"
    },
    {
      "id": "step-4",
      "type": "test",
      "description": "Write failing tests in value.rs::tests for Value::Complex PartialEq: (a) equal values with same re/im/dimension; (b) different re not equal; (c) different im not equal; (d) different dimension not equal; (e) -0.0 vs 0.0 distinguished via to_bits; (f) Complex != Real with same numeric value; (g) Complex != Scalar with same value and dimension.",
      "status": "done",
      "commit": "3188bef"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing tests in value.rs::tests for Value::Complex Ord: (a) Complex sorts after Tensor (type_tag 14 > 13); (b) Complex sorts before Undef conceptually \u2014 verify relative to known-order types; (c) within-type: dimension compared first; (d) same dimension: re bits compared second; (e) same dimension+re: im bits compared third; (f) PartialOrd consistent with Ord.",
      "status": "done",
      "commit": "3188bef"
    },
    {
      "id": "step-6",
      "type": "test",
      "description": "Write failing tests in value.rs::tests for Value::Complex content_hash: (a) determinism \u2014 same value produces same hash across two calls; (b) NaN re canonicalized \u2014 two NaN-re values produce same hash; (c) hash-equality consistency \u2014 equal values produce equal hashes; (d) different re produces different hash; (e) different dimension produces different hash; (f) Complex(3.0, 0.0, DIMENSIONLESS) hash differs from Scalar(3.0, DIMENSIONLESS) hash (tag 15 vs 4).",
      "status": "done",
      "commit": "3188bef"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write failing test in value.rs::tests for Value::dimension(): Complex { re: 1.0, im: 2.0, dimension: LENGTH }.dimension() returns LENGTH, not DIMENSIONLESS.",
      "status": "done",
      "commit": "3188bef"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Add Complex { re: f64, im: f64, dimension: DimensionVector } variant to Value enum in value.rs. Add arms in all five trait/method implementations: (1) Display \u2014 format each component with Real's whole-number convention, join with +/- sign, append 'i', wrap in parens with dimension suffix if non-dimensionless; (2) PartialEq \u2014 compare re.to_bits(), im.to_bits(), and dimension; (3) Ord \u2014 type_tag=14 in type_tag(), within-type compare dimension.cmp then re.to_bits().cmp then im.to_bits().cmp; (4) content_hash \u2014 tag byte 15, 17-byte buffer [tag, re_bits(8), im_bits(8)] with NaN canonicalization, combine with dimension.content_hash(); (5) dimension() \u2014 match Complex { dimension, .. } => *dimension.",
      "status": "done",
      "commit": "cd49fdb"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write tests in classifier.rs::tests for Complex literal classification that expose the design contract violation: (a) `literal_complex_is_dimensional_via_default` \u2014 a standalone Complex literal expression should classify as ConstraintDomain::Dimensional (via the no-flags-set default path, not via has_numeric); (b) `literal_complex_with_bool_is_logical_not_cross_domain` \u2014 a binop combining a Complex literal and a Bool literal should classify as ConstraintDomain::Logical (only has_logical set), NOT CrossDomain (which would require has_numeric+has_logical). These tests will initially fail because the current code sets has_numeric=true for Complex literals, causing (b) to return CrossDomain instead of Logical.",
      "status": "done",
      "commit": "ad23040"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Fix classifier.rs collect_flags(): Remove `Value::Complex { .. }` from the has_numeric match arm (lines 77-81). Move it into the no-op catch-all group alongside Field/Lambda/Tensor with a comment: 'Complex is not numeric (Type::is_numeric() returns false for Complex); needs its own domain if special routing is required.' Update the DomainFlags docstring for has_numeric to clarify it covers 'Scalar, Real, Int' only. This makes the Literal path consistent with the ValueRef path (which uses Type::is_numeric() and correctly excludes Complex).",
      "status": "done",
      "commit": "d24aeb6"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Write tests in analysis.rs::tests for format_value with Complex values: (a) `format_value_complex_positive_im` \u2014 Complex { re: 3.0, im: 4.0, dimension: DIMENSIONLESS } \u2192 '3 + 4i'; (b) `format_value_complex_negative_im` \u2014 Complex { re: 3.0, im: -4.0, dimension: DIMENSIONLESS } \u2192 '3 - 4i' (NOT '3 + -4i'); (c) `format_value_complex_dimensioned` \u2014 Complex { re: 3.0, im: 4.0, dimension: LENGTH } \u2192 '3 + 4i m'. Test (b) will fail against the current implementation which produces '3 + -4i'.",
      "status": "done",
      "commit": "74246d6"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "Fix format_value in analysis.rs for Value::Complex: Replace `format!(\"{re} + {im}i\")` with conditional sign handling \u2014 if im < 0.0, format as `\"{re} - {}i\", im.abs()`, else `\"{re} + {im}i\"`. Apply same logic for the dimensioned branch. This ensures negative imaginary parts display as '3 - 4i' instead of '3 + -4i', consistent with the canonical Display impl in value.rs.",
      "status": "pending",
      "commit": null
    }
  ],
  "design_decisions": [
    {
      "decision": "Complex is NOT numeric (is_numeric returns false)",
      "rationale": "is_numeric() gates single-f64 scalar arithmetic paths. Complex needs distinct two-component arithmetic and cannot be losslessly converted via as_f64(). Keeping it non-numeric prevents silent misuse in scalar contexts and avoids breaking existing numeric dispatch."
    },
    {
      "decision": "Content hash tag 15, Ord type_tag 14",
      "rationale": "Sequential allocation after existing tags. Content hash tags 0-14 are used (10 reserved for Satisfaction). Ord type_tags 0-13 are used (Tensor=13). Complex sorts after Tensor in cross-type ordering."
    },
    {
      "decision": "Display format: re\u00b1im\u00b7i with Real-style whole-number formatting, parenthesized with dimension if non-dimensionless",
      "rationale": "Follows Real's convention (whole numbers without trailing .0, e.g. 3 not 3.0). Negative imaginary uses minus sign directly (3-4i not 3+-4i). Dimensioned values use parentheses \u2014 (3+4i) m \u2014 to avoid ambiguity between 3+4i\u00b7m and (3+4i)\u00b7m. Dimensionless values omit dimension entirely for clean output."
    },
    {
      "decision": "Both re and im share one DimensionVector",
      "rationale": "Follows physics convention: complex impedance is Z = R + jX where both R and X are in ohms. The dimension applies to the complex number as a whole, not separately to real and imaginary parts. This matches the task specification."
    },
    {
      "decision": "dimension() method gets explicit arm for Complex rather than relying on catch-all",
      "rationale": "The existing catch-all returns DIMENSIONLESS, which would silently lose dimension information for Complex values. An explicit arm ensures dimensioned Complex values correctly report their dimension."
    },
    {
      "decision": "f64 comparison uses to_bits() for both re and im, with NaN canonicalization in content_hash",
      "rationale": "Mirrors the established Scalar pattern exactly. Preserves -0.0 vs 0.0 distinction for cache coherency. NaN values are equal to themselves via to_bits(), satisfying Rust's Hash contract (a == b implies hash(a) == hash(b)). NaN canonicalization in content_hash prevents different NaN bit patterns from producing different hashes."
    },
    {
      "decision": "Complex literals in classifier set no domain flag (not has_numeric), matching ValueRef path",
      "rationale": "The Literal arm in collect_flags() must mirror the ValueRef arm's use of Type::is_numeric() which returns false for Complex. Setting has_numeric for Complex literals would route Complex+Bool expressions to CrossDomain while the equivalent ValueRef path would route to Logical \u2014 an internal inconsistency. Complex literals with no flag set fall through to Dimensional (the default), which is the same end result as the ValueRef path and leaves room for a dedicated Complex domain later."
    },
    {
      "decision": "format_value uses conditional sign formatting for Complex imaginary part, not delegation to Display",
      "rationale": "format_value in analysis.rs uses spaces around operators ('3 + 4i') for LSP readability, while the canonical Display impl in value.rs uses compact format ('3+4i'). Delegating to Display would change the established LSP presentation style. Instead, apply the same sign-handling logic (im < 0 \u2192 minus with abs) to keep spaced format consistent."
    }
  ],
  "reuse": [
    {
      "what": "Box<Type> compound-type pattern",
      "where": "ty.rs \u2014 List(Box<Type>), Set(Box<Type>), Option(Box<Type>), Point/Vector/Tensor all use Box<Type>",
      "how": "Complex(Box<Type>) follows the identical pattern for recursive type nesting with the inner type as the quantity parameter"
    },
    {
      "what": "Factory method pattern",
      "where": "ty.rs impl Type \u2014 vec2(q), vec3(q), point2(q), point3(q), tensor(r,n,q) all wrap q in Box::new",
      "how": "Type::complex(q) wraps q in Box::new identically: pub fn complex(q: Type) -> Self { Type::Complex(Box::new(q)) }"
    },
    {
      "what": "f64 to_bits() equality and NaN canonicalization patterns",
      "where": "value.rs \u2014 PartialEq for Real uses a.to_bits() == b.to_bits(); Scalar uses same for si_value + dimension check; content_hash canonicalizes NaN via if r.is_nan() { f64::NAN.to_bits() } else { r.to_bits() }",
      "how": "Apply identical to_bits() comparison for both re and im in PartialEq; use identical NaN canonicalization for both re and im in content_hash"
    },
    {
      "what": "ContentHash tag + bits + dimension.content_hash() composition",
      "where": "value.rs \u2014 Scalar content_hash builds [tag=4, si_value_bits(8)] then combines with dimension.content_hash()",
      "how": "Complex builds [tag=15, re_bits(8), im_bits(8)] then combines with dimension.content_hash() \u2014 same pattern extended to two f64 fields"
    },
    {
      "what": "Ord type_tag discriminant dispatch",
      "where": "value.rs \u2014 type_tag() function maps each variant to u8, cross-type ordering by tag, within-type match for same-tag comparison",
      "how": "Add type_tag 14 for Complex; within-type comparison mirrors Scalar: dimension.cmp().then_with(|| re_bits.cmp()).then_with(|| im_bits.cmp())"
    },
    {
      "what": "DimensionVector type, constants, and is_dimensionless() method",
      "where": "dimension.rs \u2014 DIMENSIONLESS, LENGTH, ANGLE constants; is_dimensionless() check; content_hash() method",
      "how": "Used for Complex's dimension field storage, Display conditional formatting, and content_hash composition"
    },
    {
      "what": "Classifier no-op catch-all pattern for non-domain types",
      "where": "classifier.rs \u2014 Field, Lambda, Tensor already grouped in the no-op arm with comment explaining they don't contribute to domain classification",
      "how": "Complex joins this group with an explanatory comment referencing the is_numeric() design contract"
    },
    {
      "what": "Existing format_value test patterns in analysis.rs",
      "where": "analysis.rs::tests \u2014 format_value_scalar_length, format_value_bool, etc. all follow assert_eq!(format_value(&value), expected_string) pattern",
      "how": "New Complex format_value tests follow the same pattern with Value::Complex struct literals and expected string assertions"
    }
  ],
  "_session_id": "286-84c537bb",
  "_created_at": "2026-03-24T10:02:50.516916+00:00"
}
