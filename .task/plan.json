{
  "task_id": "36",
  "title": "Reproduce and fix interactive edit-loop staleness after parameter edits",
  "modules": ["crates/reify-eval", "crates/reify-runtime"],
  "analysis": "## Current State\n\nSteps 1-12 from the prior plan are complete and committed. The sequential edit pipeline is fully working:\n- `edit_param()` handles auto-resolution, second-wave let-binding propagation, diagnostics\n- `edit_check()` adds constraint satisfaction checking\n- 365 tests pass\n\nThe concurrent edit path (`edit_param_concurrent`) calls `resolve_concurrent_edit()` which correctly updates `result.values` and `result.snapshot_values` in place — so values ARE correct after `apply_concurrent_edit()`. The test `edit_param_concurrent_re_resolves_auto_params` passes.\n\n## Remaining Staleness Gaps\n\n### Gap 1: ConcurrentEditResult discards resolution metadata\n`edit_param_concurrent` (concurrent_eval.rs:312-313):\n```rust\nlet (_resolved_params, _diagnostics) =\n    engine.resolve_concurrent_edit(&setup, &mut result);\n```\nThe returned `resolved_params` and `diagnostics` are assigned to underscore-prefixed variables and discarded. The `ConcurrentEditResult` struct (lib.rs:152-163) has no fields for these. Callers cannot tell what auto params were resolved or if the solver produced diagnostics.\n\n### Gap 2: No concurrent constraint checking\nThere is no `edit_check_concurrent` equivalent. After a concurrent edit, the only way to check constraint satisfaction is to call `check()` which does a full cold-start `eval()`. The sequential path has `edit_check()` which combines `edit_param()` + constraint checking, but the concurrent path has no equivalent.\n\n### Gap 3: Stale test comment\nThe test `edit_param_concurrent_re_resolves_auto_params` (concurrent_eval.rs:875) has a doc comment saying \"Currently FAILS because the concurrent edit path doesn't include a resolution phase.\" This is stale — the test passes.\n\n### Gap 4: Constraint label regression (blocking review feedback)\n`check_constraints_with_values` (lib.rs:1253) always sets `label: None` on every `ConstraintCheckEntry`, discarding user-provided constraint labels. The cold-start `check()` path (lib.rs:1611) correctly populates `label` from `compiled.label.clone()`. Both `edit_check` and `edit_check_concurrent` route through `check_constraints_with_values`, so their `CheckResult` structs lose labels. Root cause: `ConstraintNodeData` in graph.rs lacks a `label` field, and the graph builder (graph.rs:86-92) discards `constraint.label` when converting `CompiledConstraint` → `ConstraintNodeData`.\n\n## Approach\n1. Add `resolved_params` and `diagnostics` fields to `ConcurrentEditResult`\n2. Wire `resolve_concurrent_edit` return values into the result in `edit_param_concurrent`\n3. Add `check_constraints_with_values` helper to Engine (factored from `edit_check`)\n4. Create `edit_check_concurrent` function in reify-runtime\n5. Fix stale comments\n6. Fix label preservation: add `label: Option<String>` to `ConstraintNodeData`, populate in graph builder, use in `check_constraints_with_values`\n\nAll changes are additive — no existing behavior is modified.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Verify all existing tests pass (365 tests across workspace)",
      "status": "done",
      "commit": null,
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing test `concurrent_edit_result_includes_resolved_params` in crates/reify-runtime/tests/concurrent_eval.rs. Module: param `a` (default mm(3.0)), auto `x`, let `y = x * 2.0`, constraint `x > a`. SequencedMockSolver: 1st call x=mm(5.0), 2nd call x=mm(20.0). Cold eval. Concurrent edit a→mm(8.0). Assert `result.resolved_params` is non-empty and contains x→mm(20.0). This test will NOT COMPILE because `ConcurrentEditResult` has no `resolved_params` field.",
      "status": "done",
      "commit": "a2feb1d"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Add `resolved_params: HashMap<ValueCellId, Value>` and `diagnostics: Vec<Diagnostic>` fields to `ConcurrentEditResult` in crates/reify-eval/src/lib.rs. Initialize to empty defaults in `ConcurrentEvalAdapter::into_result()` and `build_result_shared()` in crates/reify-runtime/src/concurrent_eval.rs. Update `edit_param_concurrent` to capture the return value of `resolve_concurrent_edit` and set `result.resolved_params` and `result.diagnostics` before returning.",
      "status": "done",
      "commit": "a013e8c"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing test `concurrent_edit_result_includes_diagnostics_on_infeasible` in crates/reify-runtime/tests/concurrent_eval.rs. Module: param `a` (default mm(1.0)), auto `x`, constraint `x > a`. SequencedMockSolver: 1st call Solved x=mm(5.0), 2nd call Infeasible with diagnostic message. Cold eval. Concurrent edit a→mm(10.0). Assert `result.diagnostics` is non-empty and contains the infeasibility message. This verifies solver diagnostics flow through the concurrent path.",
      "status": "done",
      "commit": "5268c85"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Verify step-3 test passes with step-2 implementation (diagnostics should already flow through since resolve_concurrent_edit returns them and edit_param_concurrent now captures them). Fix if needed.",
      "status": "done",
      "commit": "5268c85"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing test `edit_check_concurrent_reports_constraint_satisfaction` in crates/reify-runtime/tests/concurrent_eval.rs. Uses SimpleConstraintChecker (from reify-constraints). Module: param `width` (default mm(10.0)), constraint `width > mm(5.0)`. Cold check(). Call new function `edit_check_concurrent(engine, width_id, mm(2.0), cancel)`. Assert: (1) CheckResult.constraint_results[0].satisfaction == Violated, (2) values[width] == mm(2.0), (3) resolved_params is empty (no auto params). This test will NOT COMPILE because `edit_check_concurrent` doesn't exist.",
      "status": "done",
      "commit": "2a02a32"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Add `check_constraints_with_values(&self, values: &ValueMap) -> (Vec<ConstraintCheckEntry>, Vec<Diagnostic>)` public method to Engine in crates/reify-eval/src/lib.rs. Factor the constraint-checking logic from `edit_check` into this method: iterate snapshot.graph.constraints, build ConstraintInput, call self.constraint_checker.check(), collect results. Refactor `edit_check` to use this new helper. Then create `edit_check_concurrent` async function in crates/reify-runtime/src/concurrent_eval.rs: calls edit_param_concurrent, clones resolved_params and diagnostics from result, calls engine.apply_concurrent_edit, calls engine.check_constraints_with_values, returns CheckResult.",
      "status": "done",
      "commit": "adcdfef"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write test `edit_check_concurrent_constraint_transitions` in crates/reify-runtime/tests/concurrent_eval.rs. Uses SimpleConstraintChecker. Module: param `width` (default mm(10.0)), constraint `width > mm(5.0)`. Cold check → Satisfied. edit_check_concurrent(width, mm(2.0)) → Violated. edit_check_concurrent(width, mm(8.0)) → Satisfied. Verifies constraint transitions work correctly through the concurrent path across multiple consecutive edits.",
      "status": "done",
      "commit": "bcca88b"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Fix any issues with concurrent constraint transitions. Ensure engine state is correctly updated between consecutive edit_check_concurrent calls (snapshot, cache, param_overrides all consistent).",
      "status": "done",
      "commit": "bcca88b"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write test `edit_check_concurrent_with_resolution_and_constraints` — end-to-end concurrent interactive loop. Module: param `a` (default mm(3.0)), auto `x`, let `y = x * 2.0`, constraint `x > a`, constraint `y < mm(100.0)`. SequencedMockSolver returns x=mm(5.0) then x=mm(20.0). Cold eval+check. edit_check_concurrent(a, mm(8.0)). Assert: (1) resolved_params contains x→mm(20.0), (2) values[y] = 0.04 SI (re-evaluated from resolved x), (3) constraint `x > a` is Satisfied (via checker), (4) constraint `y < mm(100)` is Satisfied. This is the comprehensive coherence test: concurrent edit → resolution → let-binding propagation → constraint checking, all in one interactive update.",
      "status": "done",
      "commit": "86a9969"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Fix any coherence issues found by step-9. Update the stale doc comment on `edit_param_concurrent_re_resolves_auto_params` test (remove 'Currently FAILS' note). Clean up any remaining underscore-prefixed variable names from the old discard pattern.",
      "status": "done",
      "commit": "57d26db"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Write failing test `edit_check_preserves_constraint_labels` in crates/reify-eval/tests/incremental.rs. Uses SimpleConstraintChecker. Module: param `width` (default mm(10.0)), TWO constraints: `.constraint(\"S\", 0, Some(\"min_width\"), gt(value_ref(\"S\", \"width\"), literal(mm(5.0))))` and `.constraint(\"S\", 1, None, lt(value_ref(\"S\", \"width\"), literal(mm(100.0))))`. Cold check() → verify constraint_results[0].label == Some(\"min_width\") and constraint_results[1].label == None. Then call edit_check(width, mm(2.0)). Assert: (1) constraint_results[0].label == Some(\"min_width\"), (2) constraint_results[1].label == None, (3) constraint_results[0].satisfaction == Violated. This test WILL FAIL because `check_constraints_with_values` always sets `label: None`, so the edit_check path returns `label: None` for the labeled constraint.",
      "status": "done",
      "commit": "5905aa2"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "Fix label preservation in `check_constraints_with_values`. Add `pub label: Option<String>` field to `ConstraintNodeData` in crates/reify-eval/src/graph.rs. Update the graph builder (graph.rs:86-92) to copy `constraint.label.clone()` into `ConstraintNodeData`. Update all existing `ConstraintNodeData { .. }` construction sites in tests (graph.rs tests, demand.rs, deps.rs, dirty.rs) to include `label: None`. In `check_constraints_with_values` (lib.rs:1251-1255), change `label: None` to `label: cnode.label.clone()` by looking up the constraint node from `snapshot.graph.constraints` using the constraint id. The helper already iterates `snapshot.graph.constraints` and has access to each `cnode`, so use `cnode.label.clone()` directly in the `ConstraintCheckEntry` construction.",
      "status": "done",
      "commit": "d9d6854"
    },
    {
      "id": "step-13",
      "type": "test",
      "description": "Write test `edit_check_concurrent_preserves_constraint_labels` in crates/reify-runtime/tests/concurrent_eval.rs. Uses SimpleConstraintChecker. Module: param `width` (default mm(10.0)), constraint `.constraint(\"S\", 0, Some(\"min_width\"), gt(value_ref(\"S\", \"width\"), literal(mm(5.0))))`. Cold check(). edit_check_concurrent(width, mm(2.0)). Assert: (1) constraint_results[0].label == Some(\"min_width\"), (2) satisfaction == Violated. This verifies label preservation through the concurrent constraint-checking path. Should pass immediately since edit_check_concurrent also uses check_constraints_with_values which was fixed in step-12.",
      "status": "done",
      "commit": "a8652fa"
    },
    {
      "id": "step-14",
      "type": "impl",
      "description": "Verify step-13 test passes (it should, since edit_check_concurrent routes through the same check_constraints_with_values helper fixed in step-12). Run full test suite to confirm no regressions. Fix any compilation issues in test files that construct ConstraintNodeData without the new `label` field.",
      "status": "pending",
      "commit": null
    }
  ],
  "design_decisions": [
    {
      "decision": "Add resolved_params/diagnostics as fields on ConcurrentEditResult rather than returning them separately from edit_param_concurrent",
      "rationale": "Keeping all edit output in one struct mirrors the EvalResult pattern, prevents callers from losing track of associated data, and means existing callers just get additional fields (backward-compatible). The struct already carries values and snapshot_values; adding resolution metadata completes it."
    },
    {
      "decision": "Factor constraint checking into Engine::check_constraints_with_values helper, reuse in both edit_check and edit_check_concurrent",
      "rationale": "Avoids duplicating constraint-checking logic. edit_check currently inlines the constraint loop; factoring it out makes it available to the concurrent path without copying. Both paths need identical constraint evaluation logic — only the value-computation step differs (sequential vs concurrent)."
    },
    {
      "decision": "edit_check_concurrent is a free function in reify-runtime (not an Engine method)",
      "rationale": "Matches the existing edit_param_concurrent pattern. Keeps async code in the runtime crate rather than making Engine depend on tokio. The function orchestrates Engine methods (prepare, apply, check) without owning any state."
    },
    {
      "decision": "edit_check_concurrent applies the concurrent edit before checking constraints (apply-then-check, not check-with-temporary-values)",
      "rationale": "Applying first ensures the engine state is fully updated for subsequent operations. The caller gets a consistent engine state plus a complete CheckResult. This matches edit_check's pattern where edit_param updates engine state before constraint checking reads from it."
    },
    {
      "decision": "Defer incremental geometry realization to a separate task",
      "rationale": "The task mentions 'realized outputs' but geometry realization requires OCCT kernel integration with thread-safety constraints (M4 'Living Design' scope). The 'realized outputs' gap for this task is addressed by ensuring let-binding values that feed realization (e.g., volume) are correctly updated in the interactive loop. Incremental geometry belongs to a dedicated task."
    },
    {
      "decision": "Use SimpleConstraintChecker (real checker) for edit_check_concurrent tests, not MockConstraintChecker",
      "rationale": "The concurrent constraint tests need to verify actual satisfaction transitions (Satisfied→Violated→Satisfied). MockConstraintChecker returns predetermined results regardless of values, which wouldn't test the pipeline. SimpleConstraintChecker evaluates constraint expressions against actual values, proving the full pipeline is coherent."
    },
    {
      "decision": "Store constraint labels on ConstraintNodeData in the graph rather than passing a label map to check_constraints_with_values",
      "rationale": "Labels are a static property of a constraint, logically belonging on the graph node alongside id and expr. Storing them in ConstraintNodeData makes them available everywhere the graph is used, not just in constraint-checking. It avoids threading extra parameters (a label map built from the compiled template) through check_constraints_with_values. The graph builder already has access to the label from CompiledConstraint; it just wasn't copying it. This is a one-field addition to a struct, matching the existing pattern where ValueCellNode stores all static cell metadata."
    }
  ],
  "reuse": [
    {
      "what": "TopologyTemplateBuilder, CompiledModuleBuilder, expression helpers (binop, gt, lt, literal, value_ref)",
      "where": "crates/reify-test-support/src/builders.rs",
      "how": "Build test modules with .param(), .auto_param(), .let_binding(), .constraint(). Same pattern used in all existing concurrent_eval.rs and resolution.rs tests. The .constraint() method already accepts label: Option<&str> as its 3rd argument."
    },
    {
      "what": "MockConstraintChecker, SequencedMockConstraintSolver",
      "where": "crates/reify-test-support/src/mocks.rs",
      "how": "MockConstraintChecker for Engine construction in resolution tests. SequencedMockSolver for multi-phase resolution testing (cold eval → edit). Already used in edit_param_concurrent_re_resolves_auto_params test."
    },
    {
      "what": "SimpleConstraintChecker (real constraint evaluator)",
      "where": "crates/reify-constraints/src/lib.rs",
      "how": "For edit_check_concurrent tests that need actual constraint evaluation. Already used in edit_check_constraint_transitions_satisfied_to_violated_and_back test in incremental.rs."
    },
    {
      "what": "mm() value constructor",
      "where": "crates/reify-test-support/src/values.rs",
      "how": "Create length values in millimeters. mm(5.0) → Value::Scalar { si_value: 0.005, dimension: LENGTH }."
    },
    {
      "what": "edit_param_concurrent() existing function",
      "where": "crates/reify-runtime/src/concurrent_eval.rs:282-324",
      "how": "edit_check_concurrent wraps edit_param_concurrent with constraint checking, following the same composition pattern as edit_check wraps edit_param."
    },
    {
      "what": "edit_check() constraint-checking logic",
      "where": "crates/reify-eval/src/lib.rs:1218-1261",
      "how": "Factor the constraint-checking loop (lines 1228-1253) into check_constraints_with_values helper. The helper takes &self and &ValueMap, accesses self.current_snapshot.graph.constraints and self.constraint_checker."
    },
    {
      "what": "ConcurrentEvalAdapter::into_result() and build_result_shared()",
      "where": "crates/reify-runtime/src/concurrent_eval.rs:111-167",
      "how": "Update these to initialize new resolved_params and diagnostics fields to empty defaults. The resolution metadata is set after the adapter produces the result, in edit_param_concurrent."
    },
    {
      "what": "CheckResult, ConstraintCheckEntry, EvalResult types",
      "where": "crates/reify-eval/src/lib.rs:68-90",
      "how": "edit_check_concurrent returns CheckResult (same as edit_check). No new result types needed."
    },
    {
      "what": "ConstraintNodeData struct and graph builder",
      "where": "crates/reify-eval/src/graph.rs:20-27 (struct), graph.rs:86-94 (builder)",
      "how": "Add `label: Option<String>` field to ConstraintNodeData. In the graph builder loop (graph.rs:86-94), copy `constraint.label.clone()` into the new field. This makes labels available to check_constraints_with_values without needing the compiled template."
    }
  ]
}
