{
  "task_id": "113",
  "title": "Theme and CSS: define missing variables, fix surface/color tokens, add spacing system",
  "files": [
    "gui/src/theme.ts",
    "gui/src/index.tsx",
    "gui/src/App.tsx",
    "gui/src/__tests__/theme.test.ts"
  ],
  "modules": [
    "gui/src"
  ],
  "analysis": "Analysis of the codebase reveals 7 CSS custom properties referenced in component CSS modules but never defined by applyTheme() in theme.ts: --reify-surface0 (FileBrowser, ExportDialog, Toast), --reify-surface1 (FileBrowser, ExportDialog, ReloadPrompt), --reify-surface2 (ExportDialog, ReloadPrompt), --reify-subtext (FileBrowser, ExportDialog, Toast), --reify-overlay0 (ReloadPrompt), --reify-green (Toast), --reify-red (Toast). These CSS files use fallback values (e.g., var(--reify-surface0, #313244)) so the UI renders correctly in dark mode, but theme switching won't affect these properties since they're never set as CSS custom properties by applyTheme().\n\nThe task description mentions --reify-font-mono as missing, but it IS defined via the 'fontMono' key in THEME_TOKENS and correctly converted by camelToKebab() to --reify-font-mono.\n\nAdditionally, applyTheme() is called inside onMount() in App.tsx (line 115), which runs after the initial render. This causes a potential flash of unstyled content (FOUC) since CSS variables aren't set until the component mounts. The fix is to call applyTheme() synchronously in index.tsx before render().\n\nFor T4 (spacing system), no spacing tokens exist \u2014 CSS files use hardcoded pixel values (4px, 6px, 8px, 12px, 16px, 20px). We'll define spacing scale tokens in THEME_TOKENS but defer CSS file migration to a future task.\n\nLight mode (T2) is explicitly deferred per the task description.\n\nExisting test file (gui/src/__tests__/theme.test.ts) has 4 tests: required keys check, Catppuccin Mocha values check, hex validation for color tokens, and applyTheme callable check. These will be extended.",
  "prerequisites": [],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing tests in theme.test.ts asserting THEME_TOKENS contains the 7 missing keys: surface0, surface1, surface2, subtext, overlay0, green, red. Add to the existing 'has all required color keys' test array AND add a new test 'has correct Catppuccin Mocha values for surface/accent tokens' asserting surface0='#313244', surface1='#45475a', surface2='#585b70', subtext='#a6adc8', overlay0='#6c7086', green='#a6e3a1', red='#f38ba8'.",
      "status": "done",
      "commit": "3f4d2b0"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Add the 7 missing token entries to THEME_TOKENS in theme.ts: surface0: '#313244', surface1: '#45475a', surface2: '#585b70', subtext: '#a6adc8', overlay0: '#6c7086', green: '#a6e3a1', red: '#f38ba8'. The camelToKebab function handles these correctly (surface0 stays surface0, no uppercase letters to convert).",
      "status": "done",
      "commit": "46f54d9"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write a test 'applyTheme sets all tokens as CSS custom properties' that calls applyTheme() and verifies every THEME_TOKENS entry is set on document.documentElement.style. For each key, compute the expected CSS variable name using the same camelToKebab logic, then assert document.documentElement.style.getPropertyValue(`--reify-${kebab}`) equals the token value. This catches any token silently dropped or misnamed.",
      "status": "done",
      "commit": "2d945c0"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "No code change needed \u2014 step-2's token additions make step-3 pass. Verify all tests pass.",
      "status": "done",
      "commit": "2d945c0"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write a completeness guard test 'every CSS variable reference has a THEME_TOKENS entry'. Hardcode a list of all unique --reify-* variable names found in the CSS files (approximately 20 names: background, surface, surface-hover, text, text-secondary, text-muted, accent, accent-hover, border, error, warning, success, editor-bg, viewport-bg, panel-bg, font-mono, surface0, surface1, surface2, subtext, overlay0, green, red). For each, convert kebab to camelCase and assert it exists in THEME_TOKENS.",
      "status": "done",
      "commit": "b2f5692"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Verify step-5 passes. If any CSS variable is still missing from THEME_TOKENS, add it. This step confirms full coverage.",
      "status": "done",
      "commit": "b2f5692"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write failing test for spacing token keys: spaceXs='2px', spaceSm='4px', spaceMd='8px', spaceLg='12px', spaceXl='16px', space2xl='20px', space3xl='24px', and radius tokens: radiusSm='2px', radiusMd='4px', radiusLg='8px'. Assert these keys exist in THEME_TOKENS with the correct values. Also update the existing 'all color tokens are valid hex' test to skip non-color tokens by checking key prefixes (fontMono, space*, radius*).",
      "status": "done",
      "commit": "cd09f91"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Add spacing and radius tokens to THEME_TOKENS in theme.ts: spaceXs:'2px', spaceSm:'4px', spaceMd:'8px', spaceLg:'12px', spaceXl:'16px', space2xl:'20px', space3xl:'24px', radiusSm:'2px', radiusMd:'4px', radiusLg:'8px'. Update the hex validation test's skip list to include these non-color keys.",
      "status": "done",
      "commit": "67b9073"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write a test verifying applyTheme is called before render, not inside a component. Since we can't easily test index.tsx execution order in a unit test, write a structural test: import App.tsx source as a string (or just check that theme.ts exports are used correctly). More practically, test that after calling applyTheme() directly, CSS variables are immediately available \u2014 confirming it works synchronously without needing a component mount cycle.",
      "status": "done",
      "commit": "4429eef"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Move applyTheme() from App.tsx to index.tsx: (1) In index.tsx, add `import { applyTheme } from './theme';` and call `applyTheme();` before the `render()` call. (2) In App.tsx, remove the `import { applyTheme } from './theme';` import and remove the `applyTheme();` call from onMount (line 115). Keep the rest of onMount intact (getInitialState, subscribeToEvents, etc.).",
      "status": "done",
      "commit": "47785c9"
    }
  ],
  "design_decisions": [
    {
      "decision": "Add missing tokens as new THEME_TOKENS entries rather than renaming existing tokens",
      "rationale": "The CSS uses both naming conventions: --reify-surface is a general panel background (#2a2a3a), while --reify-surface0/1/2 are Catppuccin's layered surface scale (#313244/#45475a/#585b70). These are semantically different and both must coexist."
    },
    {
      "decision": "Keep 'green' and 'red' as separate tokens rather than aliasing to 'success' and 'error'",
      "rationale": "The CSS uses --reify-success (#a6e3a1) and --reify-error (#f38ba8) for semantic status colors, AND --reify-green and --reify-red for visual accent colors (e.g., Toast border). Although they currently share values, they serve different semantic purposes \u2014 green/red are raw palette colors while success/error are semantic roles. Keeping both allows future themes to decouple them."
    },
    {
      "decision": "Define spacing tokens but don't migrate CSS files to use them",
      "rationale": "Migrating all hardcoded pixel values across 12+ CSS modules is a large mechanical change that adds risk and noise to this PR. Defining the tokens enables incremental adoption. CSS files can be migrated in a follow-up task."
    },
    {
      "decision": "Call applyTheme() synchronously in index.tsx before render() rather than in a <script> tag or CSS :root block",
      "rationale": "Synchronous call before render() is the simplest fix that prevents FOUC. CSS :root definitions would duplicate the values. A <script> tag in index.html would require build tooling changes. The index.tsx approach keeps all theme logic in TypeScript and is consistent with SolidJS patterns."
    },
    {
      "decision": "Use a hardcoded list of CSS variable references in the completeness test",
      "rationale": "Parsing CSS files at test time creates fragile dependencies on file system layout and test runner working directory. A hardcoded list is explicit, maintainable, and catches regressions \u2014 when someone adds a new --reify-* CSS variable reference, they must also update the test list."
    },
    {
      "decision": "Skip non-color tokens (fontMono, spacing, radius) in the hex-validation test using key prefix matching",
      "rationale": "The existing test validates all color tokens are valid hex. Spacing tokens like '4px' and font stacks are not hex colors. Rather than maintaining a brittle exclusion list, use prefix-based detection: keys starting with 'font', 'space', or 'radius' are non-color."
    }
  ],
  "reuse": [
    {
      "what": "Existing camelToKebab() function in theme.ts",
      "where": "gui/src/theme.ts line 20-22",
      "how": "Already converts camelCase THEME_TOKENS keys to kebab-case CSS variable names. New tokens like 'surface0' stay as 'surface0' (no uppercase), 'spaceXs' becomes 'space-xs'. Works correctly for all new token names without modification."
    },
    {
      "what": "Existing applyTheme() function in theme.ts",
      "where": "gui/src/theme.ts line 24-29",
      "how": "Iterates all THEME_TOKENS entries and sets CSS custom properties. No changes needed to the function itself \u2014 adding new entries to THEME_TOKENS automatically includes them in the applied theme."
    },
    {
      "what": "Existing theme.test.ts test structure and patterns",
      "where": "gui/src/__tests__/theme.test.ts",
      "how": "Extend the existing 'has all required color keys' test to include the 7 new keys. Follow the same test patterns (describe blocks, expect().toBe() for values, regex for hex validation) for consistency."
    },
    {
      "what": "CSS fallback values already present in all module CSS files",
      "where": "All *.module.css files under gui/src/",
      "how": "Every CSS variable reference already has a correct fallback value (e.g., var(--reify-surface0, #313244)). No CSS file changes are needed for this task \u2014 the tokens just need to be defined in THEME_TOKENS so theme switching works correctly."
    }
  ],
  "_session_id": "113-24eb0c4f",
  "_created_at": "2026-03-21T17:11:52.167192+00:00"
}
