{
  "task_id": "114",
  "title": "Viewport performance: BVH raycasting, render-on-demand, wireframe optimization",
  "files": [
    "gui/package.json",
    "gui/src/viewport/selection.ts",
    "gui/src/viewport/Viewport.tsx",
    "gui/src/viewport/meshManager.ts",
    "gui/src/__tests__/viewport/selection.test.ts",
    "gui/src/__tests__/viewport/Viewport.test.tsx",
    "gui/src/__tests__/viewport/meshManager.test.ts"
  ],
  "modules": [
    "gui/src/viewport",
    "gui/src/__tests__/viewport"
  ],
  "analysis": "Task addresses 4 viewport performance issues. (P-01) Raycasting in selection.ts:65-73 calls raycaster.intersectObjects on ALL meshes with no spatial indexing \u2014 fix by adding three-mesh-bvh for BVH acceleration on geometries and enabling firstHitOnly on the raycaster. (P-02) WireframeGeometry in selection.ts:168 is computationally expensive for large meshes, creating a line segment for every triangle edge \u2014 replace with EdgesGeometry which only computes edges where face normals differ significantly, a direct drop-in replacement. (P-03) Animation loop in Viewport.tsx:77-83 runs requestAnimationFrame continuously and calls renderer.render every frame even when completely idle \u2014 implement render-on-demand using OrbitControls 'change' event and a dirty flag, keeping the rAF loop alive (needed for damping) but skipping the expensive render call when nothing has changed. (P-04) meshManager.sync in meshManager.ts:116-117 creates new Set(Object.keys(meshes)) on every sync call \u2014 minor optimization to use direct property lookup instead. All existing tests use vi.mock('three', ...) with mock classes; new behavior can be tested by extending existing mock infrastructure. The controls module already exposes the OrbitControls instance so Viewport can listen to 'change' events directly without modifying controls.ts.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Add three-mesh-bvh to gui/package.json dependencies and run npm install. This package provides computeBoundsTree/disposeBoundsTree for BufferGeometry BVH acceleration and acceleratedRaycast for Mesh.prototype.raycast. Required for P-01 (BVH raycasting).",
      "status": "done",
      "commit": "e67c9d4",
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "P-03 render-on-demand: Write failing tests in Viewport.test.tsx. Update controls mock to include addEventListener/removeEventListener on the controls object (currently just {}). Add test: 'animate loop does NOT call renderer.render when idle after initial frame' \u2014 use capturing rAF mock, fire first rAF callback (initial render), clear renderer.render mock, fire second rAF callback with no interactions, assert renderer.render was NOT called. This test fails against current code which always renders every frame.",
      "status": "done",
      "commit": "ca26090"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "P-03 render-on-demand: Implement in Viewport.tsx. Add let needsRender = true flag. Add requestRender() helper that sets needsRender = true. Register controls.controls.addEventListener('change', requestRender) to mark dirty on camera movement. In animate loop: keep calling controls.update() every frame (required for enableDamping), but guard renderer.render(scene, camera) behind if(needsRender), then reset needsRender = false. Add controls.controls.removeEventListener('change', requestRender) in onCleanup. Initial needsRender = true ensures first frame renders.",
      "status": "done",
      "commit": "be2287b"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "P-03 render triggers: Write tests verifying all re-render triggers. (1) 'controls change event triggers re-render on next frame' \u2014 fire controls mock's captured 'change' callback, then fire rAF, assert renderer.render called. (2) 'resize triggers re-render' \u2014 verify renderer.render is called on next frame after ResizeObserver fires. (3) Test that renderer.render is still called once for initial frame (existing test 'animate loop does not call renderer.render after cleanup/dispose' should continue to pass).",
      "status": "done",
      "commit": "f17a85b"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "P-03 render triggers: Wire requestRender() calls in Viewport.tsx at all trigger points. Add requestRender() after meshManager.sync(props.meshes) in the mesh sync createEffect. Add requestRender() after selection.setHovered() and selection.setSelected() in their createEffects. Add requestRender() after resize() call in the ResizeObserver callback (alongside existing invalidateRect). Verify all tests from step-3 pass plus existing tests remain green.",
      "status": "done",
      "commit": "fba82c2"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "P-02 EdgesGeometry: Write failing test in selection.test.ts. Add MockEdgesGeometry to the three mock (identical structure to existing MockWireframeGeometry). Add test: 'setSelected creates EdgesGeometry from mesh geometry' \u2014 verify that when setSelected('A') is called, EdgesGeometry constructor is invoked with mesh.geometry and the resulting LineSegments is added to scene. Update existing wireframe tests to use EdgesGeometry mock if needed. This test fails because current code imports/uses WireframeGeometry.",
      "status": "done",
      "commit": "0468ab5"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "P-02 EdgesGeometry: In selection.ts, change import from 'WireframeGeometry' to 'EdgesGeometry' in the three import statement. In setSelected(), replace 'new WireframeGeometry(mesh.geometry)' with 'new EdgesGeometry(mesh.geometry)'. EdgesGeometry is a direct drop-in replacement that produces LineSegments-compatible geometry but only includes edges where adjacent face normals differ by > 1 degree threshold, making it dramatically lighter for dense meshes while preserving the visual outline.",
      "status": "done",
      "commit": "21f3365"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "P-01 BVH geometry: Write failing tests in meshManager.test.ts. Add computeBoundsTree = vi.fn() and disposeBoundsTree = vi.fn() to MockBufferGeometry. Mock the 'three-mesh-bvh' module to export computeBoundsTree, disposeBoundsTree, acceleratedRaycast. Add tests: (1) 'sync calls geometry.computeBoundsTree() on newly created mesh', (2) 'sync calls geometry.computeBoundsTree() after updating existing mesh geometry', (3) 'removeMesh calls geometry.disposeBoundsTree() before disposing geometry'. These fail because current meshManager doesn't invoke BVH methods.",
      "status": "done",
      "commit": "effc0f6"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "P-01 BVH geometry: In meshManager.ts, import { computeBoundsTree, disposeBoundsTree } from 'three-mesh-bvh' and BufferGeometry from 'three'. Apply prototype patches at module scope: BufferGeometry.prototype.computeBoundsTree = computeBoundsTree; BufferGeometry.prototype.disposeBoundsTree = disposeBoundsTree. In createMeshFromData(), call geometry.computeBoundsTree() after all attributes are set. In updateMeshGeometry(), call geometry.computeBoundsTree() after updating all attributes (rebuilds BVH for changed geometry). In removeMesh(), call (mesh.geometry as any).disposeBoundsTree() before geometry.dispose().",
      "status": "done",
      "commit": "0727cff"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "P-01 BVH raycasting: Write failing test in selection.test.ts. Add mock for 'three-mesh-bvh' module. Add test: 'raycaster has firstHitOnly set to true' \u2014 after createSelection, inspect the mock Raycaster instance to verify firstHitOnly property is true. This test fails because current code doesn't set firstHitOnly on the raycaster.",
      "status": "done",
      "commit": "6c9eb7f"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "P-01 BVH raycasting: In selection.ts, import { acceleratedRaycast } from 'three-mesh-bvh' and import Mesh from 'three'. Apply prototype patch at module scope: Mesh.prototype.raycast = acceleratedRaycast. After creating the Raycaster instance (line 42), set (raycaster as any).firstHitOnly = true. This enables BVH-accelerated raycasting that exits early on first hit, which is correct since we only use intersections[0].object.name.",
      "status": "done",
      "commit": "32b83be"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "P-04 sync optimization: Run existing meshManager tests to verify they all pass (behavior is unchanged, this is a pure refactor). Optionally add test: 'sync with empty incoming object removes all existing meshes' to ensure edge case coverage if not already covered.",
      "status": "done",
      "commit": "ee8e317"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "P-04 sync optimization: Refactor sync() in meshManager.ts. Replace 'const incomingKeys = new Set(Object.keys(meshes))' and 'incomingKeys.has(key)' with direct property check: 'if (!(key in meshes))'. This avoids one Set + Array allocation per sync call while maintaining identical O(1) lookup semantics. Iterate meshMap.keys() for removal check, then Object.entries(meshes) for add/update (same as current).",
      "status": "done",
      "commit": "70b62bf"
    },
    {
      "id": "step-13",
      "type": "impl",
      "description": "Fix resource leak in 'resize triggers re-render on next frame' test (Viewport.test.tsx lines 273-302). The test overrides globalThis.ResizeObserver but restores it only at line 301 \u2014 not in a try/finally. If any assertion between render() and the restore throws (e.g. roCallback is undefined when the non-null assertion roCallback!() fires on a failed mount), the original ResizeObserver is never restored, causing cascading failures in all subsequent tests that depend on ResizeObserver. Fix: wrap the test body (lines 284-301) in a try/finally block so that `globalThis.ResizeObserver = OrigRO` always executes regardless of assertion failures. The finally block must contain only the restore; all assertions remain in the try block.",
      "status": "done",
      "commit": "10394b3"
    }
  ],
  "design_decisions": [
    {
      "decision": "Use EdgesGeometry instead of WireframeGeometry for selection overlay",
      "rationale": "EdgesGeometry only computes edges where adjacent face normals differ by a threshold (default 1 degree), producing far fewer line segments than WireframeGeometry which duplicates every triangle edge. This is a direct drop-in replacement that dramatically reduces compute time for dense meshes. A full stencil-based outline pass (OutlinePass + EffectComposer) would require setting up a multi-pass render pipeline \u2014 disproportionate complexity for v0.1."
    },
    {
      "decision": "Use dirty-flag render-on-demand pattern while keeping the rAF loop alive",
      "rationale": "OrbitControls with enableDamping=true requires controls.update() to be called every frame for smooth deceleration. Stopping/restarting the rAF loop would cause damping to stutter. The dirty-flag pattern keeps the loop running (cheap controls.update() call) but skips the expensive renderer.render() when nothing has changed. OrbitControls' built-in 'change' event fires during damping, correctly triggering re-renders while the camera decelerates."
    },
    {
      "decision": "Add three-mesh-bvh for BVH acceleration rather than custom spatial indexing",
      "rationale": "three-mesh-bvh is the de-facto standard for raycasting acceleration in Three.js (recommended in official docs and Discord). It provides O(log n) triangle intersection via bounding volume hierarchy, plus firstHitOnly for early-exit. Custom spatial indexing would duplicate well-tested functionality. The package adds ~35KB gzipped and is compatible with three ^0.183."
    },
    {
      "decision": "Apply three-mesh-bvh prototype patches at module scope in consuming files",
      "rationale": "three-mesh-bvh works by patching BufferGeometry.prototype.computeBoundsTree and Mesh.prototype.raycast. Applying patches at module scope in meshManager.ts and selection.ts keeps them co-located with their usage. An alternative dedicated bvh.ts init module adds indirection for just two simple prototype assignments."
    },
    {
      "decision": "Use 'in' operator for sync key membership instead of Set allocation",
      "rationale": "Current code creates new Set(Object.keys(meshes)) on every sync call. For Record<string, MeshData> objects, 'key in meshes' provides O(1) property lookup without any allocation. Behavioral equivalent, but avoids one Set + Array allocation per call."
    },
    {
      "decision": "Use try/finally for global stub restoration in tests instead of afterEach",
      "rationale": "The resize test overrides globalThis.ResizeObserver. Restoring in a try/finally within the test body is preferred over afterEach because it keeps the save/restore co-located with the override (easier to reason about), and avoids polluting the shared afterEach with cleanup for a single test's specific stub. The finally block guarantees restoration even if assertions throw."
    }
  ],
  "reuse": [
    {
      "what": "Existing Three.js vi.mock() infrastructure in all viewport test files",
      "where": "gui/src/__tests__/viewport/selection.test.ts, meshManager.test.ts, Viewport.test.tsx",
      "how": "Extend existing MockBufferGeometry to add computeBoundsTree/disposeBoundsTree mock methods. Create MockEdgesGeometry following the same pattern as existing MockWireframeGeometry. Add addEventListener/removeEventListener to the controls mock object in Viewport.test.tsx."
    },
    {
      "what": "OrbitControls built-in 'change' event dispatch",
      "where": "gui/src/viewport/Viewport.tsx",
      "how": "OrbitControls already dispatches 'change' events when camera moves (including during damping update cycles). Viewport listens via controls.controls.addEventListener('change', requestRender) \u2014 no changes to controls.ts needed."
    },
    {
      "what": "ControlsContext.controls property exposing OrbitControls instance",
      "where": "gui/src/viewport/controls.ts \u2192 used in gui/src/viewport/Viewport.tsx",
      "how": "The controls module already returns the OrbitControls instance with addEventListener/removeEventListener via ControlsContext.controls. No interface changes needed."
    },
    {
      "what": "Existing rAF-based test infrastructure in Viewport.test.tsx",
      "where": "gui/src/__tests__/viewport/Viewport.test.tsx lines 15-22",
      "how": "The test file already has rafCallbacks array, rafIdCounter, and mock requestAnimationFrame/cancelAnimationFrame. Render-on-demand tests reuse this pattern to capture and selectively fire frame callbacks to verify rendering behavior."
    },
    {
      "what": "Existing wireframe test patterns in selection.test.ts",
      "where": "gui/src/__tests__/viewport/selection.test.ts 'wireframe overlay on selection' describe block",
      "how": "Tests for setSelected already verify geometry creation, scene.add, dispose, and material cleanup. EdgesGeometry tests follow the same pattern, just with the updated mock class name."
    }
  ],
  "_session_id": "114-4ad7e2e4",
  "_created_at": "2026-03-21T17:54:38.652513+00:00"
}
