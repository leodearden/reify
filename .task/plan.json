{
  "task_id": "11",
  "title": "Dependency tracking + reverse index",
  "modules": [
    "crates/reify-types/src/identity.rs",
    "crates/reify-types/src/lib.rs",
    "crates/reify-expr/src/lib.rs",
    "crates/reify-eval/src/deps.rs",
    "crates/reify-eval/src/lib.rs"
  ],
  "analysis": "This task implements Adapton-style dynamic dependency tracing for incremental re-evaluation. The key challenge is the crate layering: reify-types (shared vocab) → reify-expr (expression evaluator) → reify-eval (engine). DependencyTrace/TraceRecorder/ReverseDependencyIndex live in reify-eval, but eval_expr lives in reify-expr. To avoid circular dependencies, we use a callback approach: reify-expr gets a new eval_expr_traced() that takes &mut dyn FnMut(&ValueCellId) invoked on every ValueRef read. reify-eval's TraceRecorder implements this by capturing reads into a DependencyTrace. NodeId (enum unifying ValueCellId, ConstraintNodeId, RealizationNodeId) goes in reify-types since it's a shared vocabulary type. The ReverseDependencyIndex inverts traces into a HashMap<ValueCellId, HashSet<NodeId>> for O(1) dirty propagation lookups. Engine integration adds traced evaluation and stores per-node traces plus the reverse index in EvalResult.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Create crates/reify-eval/src/deps.rs (empty file) and add `pub mod deps;` to crates/reify-eval/src/lib.rs",
      "status": "done",
      "commit": "28f8092",
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write tests for NodeId enum in reify-types/src/identity.rs: construction from each variant (ValueCell, Constraint, Realization), PartialEq, Eq, Hash (use as HashMap key), Display format, Clone. Verify different variant types with same inner data are not equal.",
      "status": "done",
      "commit": "b1caf66"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Add NodeId enum to reify-types/src/identity.rs with variants ValueCell(ValueCellId), Constraint(ConstraintNodeId), Realization(RealizationNodeId). Derive Clone, Debug, PartialEq, Eq, Hash. Implement Display. Re-export NodeId from reify-types/src/lib.rs.",
      "status": "done",
      "commit": "f3eeb5a"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write tests for DependencyTrace in reify-eval/src/deps.rs: Default is empty reads vec, can push reads, Clone produces independent copy, Debug works. Test that duplicate reads are preserved (a node may read the same cell multiple times in different sub-expressions).",
      "status": "done",
      "commit": "54ed8ac"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Add DependencyTrace struct to deps.rs with `pub reads: Vec<ValueCellId>`, derive Clone, Debug, Default.",
      "status": "done",
      "commit": "7a23011"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write tests for TraceRecorder in deps.rs: new() creates empty recorder, record_read captures ValueCellId, finish() returns DependencyTrace with correct reads in order. Test recording multiple reads including duplicates.",
      "status": "done",
      "commit": "73d0720"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Add TraceRecorder struct to deps.rs with trace: DependencyTrace field. Implement new(), record_read(&mut self, cell: ValueCellId), finish(self) -> DependencyTrace. Also add a as_callback(&mut self) -> impl FnMut(&ValueCellId) method or equivalent to bridge to the eval_expr_traced callback API.",
      "status": "done",
      "commit": "515b28b"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write test in reify-expr for eval_expr_traced: evaluate a ValueRef expression, verify the callback is called with the correct ValueCellId. Test with a literal (no callback call). Test with a BinOp(ValueRef, ValueRef) that the callback is called twice.",
      "status": "done",
      "commit": "5674f16"
    },
    {
      "id": "step-8",
      "type": "impl",
      "description": "Add eval_expr_traced(expr, values, on_read: &mut dyn FnMut(&ValueCellId)) -> Value to reify-expr/src/lib.rs. Mirrors eval_expr but calls on_read(id) at every ValueRef(id) before reading the value. All recursive calls use the traced variant. Refactor internal helpers (eval_binop, eval_unop, etc.) to accept the optional callback, or implement eval_expr_traced as a separate recursive function to avoid changing eval_expr's API.",
      "status": "done",
      "commit": "93553a0"
    },
    {
      "id": "step-9",
      "type": "test",
      "description": "Write test in reify-expr for eval_expr_traced with compound expression: volume = width * height * thickness. Verify callback is called exactly 3 times with width, height, thickness ValueCellIds. Verify the computed value is correct (same as eval_expr). Also test conditional expression only traces the taken branch.",
      "status": "done",
      "commit": "57528ee"
    },
    {
      "id": "step-10",
      "type": "impl",
      "description": "Ensure eval_expr_traced correctly handles all expression kinds recursively: Conditional (traces condition + taken branch only, matching Kleene AND/OR short-circuit behavior), FunctionCall args, nested BinOp/UnOp. Fix any issues found by step-9 tests.",
      "status": "done",
      "commit": "57528ee"
    },
    {
      "id": "step-11",
      "type": "test",
      "description": "Write tests for TraceRecorder + eval_expr_traced integration in reify-eval/src/deps.rs: Use TraceRecorder's callback with eval_expr_traced for the bracket volume expression (width * height * thickness). Verify the resulting DependencyTrace has reads = [width, height, thickness].",
      "status": "done",
      "commit": "b9c4a6a"
    },
    {
      "id": "step-12",
      "type": "impl",
      "description": "Ensure TraceRecorder provides the right callback interface. Add a convenience method like `TraceRecorder::record_callback(&mut self) -> impl FnMut(&ValueCellId) + '_` that returns a closure suitable for eval_expr_traced, or just use |id| recorder.record_read(id.clone()) at call sites.",
      "status": "done",
      "commit": "b9c4a6a"
    },
    {
      "id": "step-13",
      "type": "test",
      "description": "Write tests for ReverseDependencyIndex::build in deps.rs: Build from a HashMap<NodeId, DependencyTrace> containing volume node reading [width, height, thickness] and a constraint node reading [thickness]. Verify dependents_of(width) = {volume_node}, dependents_of(thickness) = {volume_node, constraint_node}, dependents_of(height) = {volume_node}. Test empty traces map produces empty index.",
      "status": "done",
      "commit": "a662e6a"
    },
    {
      "id": "step-14",
      "type": "impl",
      "description": "Add ReverseDependencyIndex struct to deps.rs with index: HashMap<ValueCellId, HashSet<NodeId>>. Implement build(traces: &HashMap<NodeId, DependencyTrace>) -> Self and dependents_of(&self, cell: &ValueCellId) -> &HashSet<NodeId> (return empty set ref for missing keys using a static or Option approach).",
      "status": "done",
      "commit": "e407e56"
    },
    {
      "id": "step-15",
      "type": "test",
      "description": "Write tests for ReverseDependencyIndex::add_trace and remove_node: add_trace adds a new node's dependencies to the index. remove_node cleans up all entries for that node. Test that removing a node from one cell's dependents doesn't affect other cells' dependents.",
      "status": "done",
      "commit": "c3be018"
    },
    {
      "id": "step-16",
      "type": "impl",
      "description": "Add add_trace(&mut self, node: NodeId, trace: &DependencyTrace) and remove_node(&mut self, node: &NodeId) methods to ReverseDependencyIndex. remove_node iterates all cells in the index and removes the node from each HashSet; also store a reverse mapping (node -> cells it reads) to make removal O(deps) instead of O(all cells).",
      "status": "done",
      "commit": "bc3c6f1"
    },
    {
      "id": "step-17",
      "type": "test",
      "description": "Write integration test in reify-eval: Build a bracket-like compiled module using test fixtures. Evaluate with Engine::eval (or a new eval_traced method). Verify that traces are captured for all value cells (params have no trace or empty trace, let volume has trace reading width/height/thickness). Verify reverse index has correct entries.",
      "status": "done",
      "commit": "7acad2a"
    },
    {
      "id": "step-18",
      "type": "impl",
      "description": "Add traces: HashMap<NodeId, DependencyTrace> and reverse_deps: ReverseDependencyIndex fields to EvalResult. Modify Engine::eval to use eval_expr_traced with a TraceRecorder for each value cell evaluation. After evaluating all cells, build the ReverseDependencyIndex from collected traces. Store both in EvalResult.",
      "status": "done",
      "commit": "7606bf8"
    },
    {
      "id": "step-19",
      "type": "test",
      "description": "Write integration test for constraint tracing: Build a module with constraints (thickness > 2mm reads thickness). Evaluate with Engine::check. Verify constraint node traces are captured. Verify reverse_deps.dependents_of(thickness_id) includes both the volume let-node AND the constraint node.",
      "status": "done",
      "commit": "b2b827c"
    },
    {
      "id": "step-20",
      "type": "impl",
      "description": "Extend Engine::check (and transitively Engine::eval) to also trace constraint expression evaluations. Add constraint node traces to the traces map. Rebuild reverse index after constraints are evaluated. Ensure CheckResult and BuildResult also expose traces/reverse_deps (or they inherit from EvalResult).",
      "status": "done",
      "commit": "eed8f89"
    },
    {
      "id": "step-21",
      "type": "test",
      "description": "Run full workspace test suite (cargo test --workspace) to verify no regressions. Verify existing eval tests still pass (eval_expr API unchanged). Verify existing constraint tests still pass. Verify existing engine tests still pass.",
      "status": "done",
      "commit": "eed8f89"
    },
    {
      "id": "step-22",
      "type": "impl",
      "description": "Fix any regressions found in step-21. Ensure backward compatibility: eval_expr unchanged, EvalResult field additions are non-breaking (new fields with defaults).",
      "status": "done",
      "commit": "eed8f89"
    }
  ],
  "design_decisions": [
    {
      "decision": "Use callback approach (&mut dyn FnMut(&ValueCellId)) for eval_expr_traced instead of passing TraceRecorder directly",
      "rationale": "Avoids circular dependency: TraceRecorder lives in reify-eval but eval_expr lives in reify-expr. reify-expr cannot depend on reify-eval. A callback/closure lets reify-expr stay independent while still supporting tracing. The closure is zero-cost when not tracing (just don't call eval_expr_traced)."
    },
    {
      "decision": "Keep eval_expr unchanged, add eval_expr_traced as a separate public function",
      "rationale": "Non-breaking change. Existing callers (reify-constraints SimpleConstraintChecker, Engine geometry ops) continue using eval_expr. Only Engine::eval switches to eval_expr_traced when it needs traces. This follows the task requirement: 'This must not break the existing eval_expr API'."
    },
    {
      "decision": "Put NodeId in reify-types/src/identity.rs",
      "rationale": "NodeId is composed of ValueCellId, ConstraintNodeId, RealizationNodeId which all live in reify-types. It's a shared vocabulary type needed by both reify-eval (for ReverseDependencyIndex) and potentially other crates. Placing it in reify-types follows the existing pattern."
    },
    {
      "decision": "Put DependencyTrace, TraceRecorder, ReverseDependencyIndex in reify-eval/src/deps.rs",
      "rationale": "These are evaluation-engine concerns. DependencyTrace is simple (Vec<ValueCellId>) but its semantics are tied to the evaluation process. ReverseDependencyIndex is clearly an engine optimization. Keeping them together in one module makes the dependency tracking system cohesive."
    },
    {
      "decision": "ReverseDependencyIndex stores a secondary map (node -> set of cells it reads) for efficient remove_node",
      "rationale": "Without this, remove_node must scan all cells in the index O(total cells). With a reverse mapping, removal is O(number of cells the removed node reads), which is typically small. This is important for incremental updates where nodes are frequently re-evaluated."
    },
    {
      "decision": "Conditional/short-circuit expressions only trace actually-evaluated branches",
      "rationale": "Following Adapton semantics: a dependency trace should reflect the actual reads that produced the value. If the condition is false and the else-branch is taken, the then-branch's reads aren't dependencies. This ensures minimal, precise dirty propagation."
    },
    {
      "decision": "Store traces in EvalResult (value type) rather than Engine (stateful)",
      "rationale": "EvalResult is the output of evaluation. Traces are a product of evaluation, not engine state. This keeps Engine stateless between evaluations (aside from injected checkers/kernels) and makes traces available to callers without reaching into the engine."
    }
  ],
  "reuse": [
    {
      "what": "ValueCellId, ConstraintNodeId, RealizationNodeId types",
      "where": "reify-types/src/identity.rs",
      "how": "NodeId enum wraps these existing types as variants. No modification to existing types needed."
    },
    {
      "what": "eval_expr function structure",
      "where": "reify-expr/src/lib.rs",
      "how": "eval_expr_traced mirrors eval_expr's recursive structure, adding a callback parameter. Internal match arms are the same, with on_read(id) added at the ValueRef arm."
    },
    {
      "what": "TopologyTemplateBuilder and bracket_compiled_module fixtures",
      "where": "reify-test-support/src/builders.rs, reify-test-support/src/fixtures.rs",
      "how": "Use existing test builders to construct compiled modules for integration tests. bracket_compiled_module provides a realistic test case with params, lets, and constraints."
    },
    {
      "what": "Value helpers (mm, vcid, cnid) from test-support",
      "where": "reify-test-support/src/values.rs",
      "how": "Use mm() for creating scalar values, vcid()/cnid() for creating IDs in tests."
    },
    {
      "what": "MockConstraintChecker",
      "where": "reify-test-support/src/mocks.rs",
      "how": "Use in Engine integration tests to avoid needing real constraint checking while testing trace capture."
    },
    {
      "what": "CompiledExpr builder methods (literal, value_ref, binop)",
      "where": "reify-types/src/expr.rs",
      "how": "Use to construct test expressions for trace verification without going through the compiler."
    },
    {
      "what": "PersistentMap (im::HashMap)",
      "where": "reify-types/src/persistent.rs",
      "how": "Could use for traces HashMap in EvalResult if structural sharing is desired, but std HashMap is fine for initial implementation."
    }
  ]
}
