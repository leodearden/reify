{
  "task_id": "183",
  "title": "Trait conformance: port and sub checking",
  "files": [
    "crates/reify-compiler/src/lib.rs",
    "crates/reify-compiler/tests/trait_conformance_tests.rs"
  ],
  "modules": [
    "crates/reify-compiler"
  ],
  "analysis": "Task 182 established the pure `check_trait_conformance` function and `ConformanceError` enum with 4 variants covering Param and Let checking. The function takes a flat `HashMap<String, Type>` of structure members plus a `CompiledTrait`, and returns `Vec<ConformanceError>`. Port and Sub requirements are explicitly skipped with a `// Sub checking not in scope for this function` comment.\n\nThis task extends conformance checking to cover Port and Sub members. Key findings:\n\n**Port conformance:** `TraitMember::Port` has name, ty (Type), and direction (PortDirection). In `compile_trait()`, the `MemberDecl::Port` variant falls through the catch-all `_ =>` branch. We need to (1) add `RequirementKind::Port { type_name, direction }`, (2) handle `MemberDecl::Port` in `compile_trait()` to emit port requirements, (3) add ConformanceError variants for missing/mismatched ports, and (4) extend `check_trait_conformance()` to accept port data and check it.\n\n**Sub conformance:** `RequirementKind::Sub(String)` already exists and `compile_trait()` already emits sub requirements. However, `check_trait_conformance()` ignores them. The check_and_apply_trait_conformance integration function does a simple name+structure_name equality check, but the task asks for trait-bound satisfaction checking. We need to (1) add ConformanceError variants for missing/unsatisfied subs, (2) extend `check_trait_conformance()` to accept sub data and check it.\n\n**Function signature change:** The pure function needs richer input. We add `PortInfo` and `SubInfo` structs as lightweight descriptors, and extend the function to take `&[PortInfo]` and `&[SubInfo]` slices. All 8 existing call sites in tests pass `&[]` for the new params (backward-compatible behavior).\n\n**Direction semantics:** For conformance, we use exact direction matching (not connection-compatibility). A trait requiring `in` means the structure must declare `in`, not just a compatible direction.",
  "prerequisites": [
    {
      "id": "pre-1",
      "description": "Add PortInfo and SubInfo structs to reify-compiler/src/lib.rs for passing port/sub data to the conformance checker",
      "status": "done",
      "commit": "b1c8180e",
      "tests": []
    },
    {
      "id": "pre-2",
      "description": "Add RequirementKind::Port { type_name: String, direction: PortDirection } variant to the RequirementKind enum",
      "status": "done",
      "commit": "b1c8180e",
      "tests": []
    },
    {
      "id": "pre-3",
      "description": "Update check_trait_conformance() signature to accept &[PortInfo] and &[SubInfo] slices; update all 8 existing test call sites to pass &[] for new params",
      "status": "done",
      "commit": "b1c8180e",
      "tests": []
    }
  ],
  "steps": [
    {
      "id": "step-1",
      "type": "test",
      "description": "Write failing test: MissingPort \u2014 trait requires port 'input' of type 'Signal' with direction In, structure has no ports \u2192 ConformanceError::MissingPort",
      "status": "done",
      "commit": "94d7c9d0"
    },
    {
      "id": "step-2",
      "type": "impl",
      "description": "Add ConformanceError::MissingPort { name, expected_type, expected_direction } variant. Implement port name lookup in check_trait_conformance: if no matching port name found, emit MissingPort",
      "status": "done",
      "commit": "b1c8180e"
    },
    {
      "id": "step-3",
      "type": "test",
      "description": "Write failing test: PortTypeMismatch \u2014 trait requires port 'mount' of type 'MountInterface', structure has port 'mount' of type 'OtherInterface' \u2192 ConformanceError::PortTypeMismatch",
      "status": "done",
      "commit": "94d7c9d0"
    },
    {
      "id": "step-4",
      "type": "impl",
      "description": "Add ConformanceError::PortTypeMismatch { name, expected_type, actual_type } variant. Extend port check: if port found but type_name differs, emit PortTypeMismatch",
      "status": "done",
      "commit": "b1c8180e"
    },
    {
      "id": "step-5",
      "type": "test",
      "description": "Write failing test: PortDirectionMismatch \u2014 trait requires port 'output' with direction Out, structure has port 'output' with direction In \u2192 ConformanceError::PortDirectionMismatch",
      "status": "done",
      "commit": "94d7c9d0"
    },
    {
      "id": "step-6",
      "type": "impl",
      "description": "Add ConformanceError::PortDirectionMismatch { name, expected_direction, actual_direction } variant. Extend port check: if port found with correct type but wrong direction, emit PortDirectionMismatch",
      "status": "done",
      "commit": "b1c8180e"
    },
    {
      "id": "step-7",
      "type": "test",
      "description": "Write passing test: port fully satisfied \u2014 trait requires port 'input : Signal in', structure has matching port \u2192 no errors (happy path verification)",
      "status": "done",
      "commit": "94d7c9d0"
    },
    {
      "id": "step-8",
      "type": "test",
      "description": "Write failing test: MissingSub \u2014 trait requires sub 'hole' satisfying trait 'Hole', structure has no subs \u2192 ConformanceError::MissingSub",
      "status": "done",
      "commit": "94d7c9d0"
    },
    {
      "id": "step-9",
      "type": "impl",
      "description": "Add ConformanceError::MissingSub { name, expected_trait } variant. Implement sub name lookup in check_trait_conformance: if no matching sub name found, emit MissingSub",
      "status": "done",
      "commit": "b1c8180e"
    },
    {
      "id": "step-10",
      "type": "test",
      "description": "Write failing test: SubTraitNotSatisfied \u2014 trait requires sub 'mount' satisfying trait 'MountInterface', structure has sub 'mount' whose structure type does not declare MountInterface bound \u2192 ConformanceError::SubTraitNotSatisfied",
      "status": "done",
      "commit": "94d7c9d0"
    },
    {
      "id": "step-11",
      "type": "impl",
      "description": "Add ConformanceError::SubTraitNotSatisfied { name, expected_trait, actual_structure } variant. Extend sub check: if sub found but its trait_bounds don't include the required trait, emit SubTraitNotSatisfied",
      "status": "done",
      "commit": "b1c8180e"
    },
    {
      "id": "step-12",
      "type": "test",
      "description": "Write passing test: sub fully satisfied \u2014 trait requires sub 'hole' satisfying 'Hole', structure has sub 'hole' with trait_bounds containing 'Hole' \u2192 no errors",
      "status": "done",
      "commit": "94d7c9d0"
    },
    {
      "id": "step-13",
      "type": "test",
      "description": "Write failing test: compile_trait handles MemberDecl::Port \u2014 parse a trait with 'port input : Signal in', verify compiled trait has RequirementKind::Port with correct type_name and direction",
      "status": "done",
      "commit": "d1c93299"
    },
    {
      "id": "step-14",
      "type": "impl",
      "description": "Add MemberDecl::Port(port_decl) case to compile_trait(): extract type_name from port_decl.type_name, direction from port_decl.direction (default Bidi), emit TraitRequirement with RequirementKind::Port",
      "status": "done",
      "commit": "38aa614d"
    },
    {
      "id": "step-15",
      "type": "test",
      "description": "Write integration test: structure with matching port satisfies trait port requirement \u2014 compile full source with trait+structure, assert no error diagnostics",
      "status": "done",
      "commit": "cfab6e3d"
    },
    {
      "id": "step-16",
      "type": "test",
      "description": "Write integration test: structure missing required port \u2014 compile source where structure lacks the port required by trait, assert error diagnostic mentioning missing port",
      "status": "done",
      "commit": "cfab6e3d"
    },
    {
      "id": "step-17",
      "type": "impl",
      "description": "Update check_and_apply_trait_conformance() to handle RequirementKind::Port: collect structure ports, check name/type/direction match, emit diagnostics for mismatches",
      "status": "done",
      "commit": "b1c8180e"
    },
    {
      "id": "step-18",
      "type": "test",
      "description": "Write integration test: structure with sub satisfying trait sub requirement \u2014 compile full source, assert no error diagnostics",
      "status": "done",
      "commit": "cfab6e3d"
    },
    {
      "id": "step-19",
      "type": "test",
      "description": "Write integration test: structure missing required sub \u2014 compile source where structure lacks the sub required by trait, assert error diagnostic mentioning missing sub",
      "status": "done",
      "commit": "cfab6e3d"
    },
    {
      "id": "step-20",
      "type": "test",
      "description": "Write test: multiple port requirements in a single trait \u2014 one satisfied, one missing \u2192 exactly one error",
      "status": "done",
      "commit": "94d7c9d0"
    }
  ],
  "design_decisions": [
    {
      "decision": "Extend check_trait_conformance() signature rather than creating a new function",
      "rationale": "The pure function is the right place for all conformance checks. Adding parameters is simpler than maintaining parallel functions. Existing callers pass &[] for new params, preserving backward behavior."
    },
    {
      "decision": "Use lightweight PortInfo and SubInfo structs rather than passing full CompiledPort/SubComponentDecl",
      "rationale": "The pure function should not depend on compilation-stage types. PortInfo/SubInfo contain only the fields needed for conformance checking (name, type_name, direction for ports; name, structure_name, trait_bounds for subs), keeping the API clean and testable."
    },
    {
      "decision": "Port direction conformance uses exact match, not connection-compatibility",
      "rationale": "Connection compatibility (is_forward_compatible) is for port-to-port wiring, not trait conformance. A trait requiring 'port x : T in' means the structure must declare the port with direction In. Bidi is not a wildcard for conformance \u2014 the trait specifies the contract."
    },
    {
      "decision": "Sub conformance checks trait_bounds membership rather than structure_name equality",
      "rationale": "The task specifies 'verify structure has a sub satisfying the trait bound'. TraitMember::Sub has a trait_ref, meaning the sub's structure type must conform to that trait. Checking trait_bounds is more semantically correct than requiring an exact structure name match."
    },
    {
      "decision": "Separate ConformanceError variants for Port type vs direction mismatch",
      "rationale": "Fine-grained error variants enable precise diagnostics. A user seeing 'wrong direction' vs 'wrong type' can fix the issue faster than a generic 'port mismatch'."
    },
    {
      "decision": "Port requirements are always TraitRequirements (never TraitDefaults)",
      "rationale": "Unlike params (which can have default values), ports describe structural contracts that the implementor must provide. There is no meaningful 'default port' that a trait could inject."
    }
  ],
  "reuse": [
    {
      "what": "Existing check_trait_conformance() function structure",
      "where": "crates/reify-compiler/src/lib.rs:100-143",
      "how": "Extend the match on RequirementKind with new Port and Sub arms, following the same pattern of push-to-errors-vec"
    },
    {
      "what": "Existing RequirementKind::Sub variant and compile_trait Sub handling",
      "where": "crates/reify-compiler/src/lib.rs:54 and 1730-1735",
      "how": "Sub compilation already works; only the conformance checking in check_trait_conformance needs to stop ignoring Sub"
    },
    {
      "what": "Existing test helpers: empty_trait(), test_span(), compile_module(), compile_first_template()",
      "where": "crates/reify-compiler/tests/trait_conformance_tests.rs:10-279",
      "how": "Reuse all helpers for new unit and integration tests"
    },
    {
      "what": "PortDirection enum from reify-types",
      "where": "crates/reify-types/src/traits.rs:15-23",
      "how": "Use in RequirementKind::Port, ConformanceError variants, and PortInfo struct"
    },
    {
      "what": "ContentHash::of_str for test trait construction",
      "where": "crates/reify-types",
      "how": "Same pattern used in existing tests for constructing CompiledTrait in unit tests"
    },
    {
      "what": "check_and_apply_trait_conformance integration pattern",
      "where": "crates/reify-compiler/src/lib.rs:4521-4687",
      "how": "Add RequirementKind::Port handling following the same diagnostic-push pattern used for Sub (lines 4665-4685)"
    }
  ],
  "_session_id": "183-1d1af319",
  "_created_at": "2026-03-25T01:57:53.874730+00:00"
}
