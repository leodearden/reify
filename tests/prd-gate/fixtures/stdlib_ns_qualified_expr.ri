// Fixture (stdlib-namespace decompose-phase premise, negative assertion — μ is
// the in-batch grammar producer): qualified references via an import binding
// (`pp.Pulley()` in expression position) do NOT parse today. tree-sitter parse
// must FAIL (exit 1 / ERROR nodes) — pre-state for PRD §8 μ (grammar_confirmed:
// false; μ commits the passing form of this fixture with the production).

import parts as pp

structure def QualifiedExprProbe {
    sub p = pp.Pulley()
}
