// Compute-node dispatch registry and associated types.
//
// This module is the scaffolding for task γ (3422). Types and implementations
// will be added in subsequent steps; the empty module is created first so that
// the crate compiles with `mod engine_compute;` before any new definitions land.
