// Scenario 10 (PRD §9), inline-escape half: a marker whose line carries the
// `ptodo:allow` inline escape hatch (§6.8) — must produce NO finding.
// TODO: leave me  // ptodo:allow
fn scenario10_inline_escape() {}
