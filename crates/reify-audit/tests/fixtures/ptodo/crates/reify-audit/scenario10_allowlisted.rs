// Scenario 10 (PRD §9), allowlist half: nested under a path that, relative to
// the fixture root, starts with the allowlisted `crates/reify-audit/` prefix
// (§6.8) — the marker on the next line must produce NO finding.
// TODO: allowlisted self
fn scenario10_allowlisted() {}
