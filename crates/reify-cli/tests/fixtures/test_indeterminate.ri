// CLI test fixture: one @test structure with an auto param making the constraint indeterminate.
// x = auto means the solver has not resolved x yet, so x > 0 is indeterminate.
// Used by test_command_on_indeterminate_file_exits_success.

@test structure TestAuto {
    param x: Length = auto
    constraint x > 0
}
