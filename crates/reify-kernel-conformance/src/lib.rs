//! Integration-test-only crate; see tests/ for the kernel-pair conformance
//! matrix (kernel-seam ε, INV-GEO-2): producer×consumer real-fixture tests
//! exercising produce → validate(α) → consume → re-validate.
