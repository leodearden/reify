//! Process-global initialisation + serialisation primitives for libgmsh.
//!
//! Populated in task 3092 plan step 2. This file exists so the `mod init;`
//! declaration in `lib.rs` resolves between pre-3 and step-2.
//!
//! Only compiled when `cfg(has_gmsh)` is set by `build.rs`.
