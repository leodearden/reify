[package]
name = "reify-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared type vocabulary and trait definitions for Reify"

[dependencies]
reify-core.workspace = true
reify-ast.workspace = true
reify-ir.workspace = true
serde = { workspace = true, features = ["derive"], optional = true }

[features]
serde = ["dep:serde", "reify-core/serde", "reify-ast/serde", "reify-ir/serde"]

[dev-dependencies]
serde_json = "1"
