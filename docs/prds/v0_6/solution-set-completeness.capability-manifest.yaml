prd: docs/prds/v0_6/solution-set-completeness.md
schema_version: 1
tasks:
- label: α
  task_id: 6706
  title: 'solution-set-completeness α: Completeness/SolutionSet carrier in reify-ir (additive; SolveResult
    stays frozen)'
  note: 'E3 coarse arm: reused in place as a singleton (`x_e3_reused: true`, status `pending`); still the live
    producer for this capability. The four coarse tasks #6900-#6903 carry HAND-AUTHORED
    `metadata.delivered_checks` that are NOT derived from this sidecar and have no sidecar of their own
    (docs/prds/v0_6/e3-granularity/ holds prose specs and analysis scripts only), so this file remains the
    record for the twelve standard leaves only.'
  capabilities:
  - name: ranked-rs-co-tenancy-with-p2-mu
    binding: 'seam with P2 leaf mu = 6680, which edits the SAME file (crates/reify-ir/src/ranked.rs) to
      add BestFoundReason::FirstOrderStationary REPLACING Unreported. Two consequences: (a) alpha must
      key its default trait lift off ''whatever the trait default reports'', NOT the named Unreported
      variant, which mu removes — whichever leaf lands second reconciles; (b) FirstOrderStationary is
      a LOCAL first-order stationarity certificate (norm of the projected gradient) and must NOT be read
      as satisfying invariant C2, which requires Exhaustive. A stationary point in one basin says nothing
      about other basins — promoting it to ProvenOptimal would be the false-completeness claim this PRD
      exists to prevent, arriving from the optimality side.'
    verdict: PASS
    delivered_check:
      kind: manual
      reason: a cross-PRD reconciliation between two leaves editing one file; no stable grep expresses
        it.
  - name: ranked-carrier-wired
    binding: capability→producer — RankedSolveResult / RankedCandidate / OptimalityStatus / BestFoundReason
      in crates/reify-ir/src/ranked.rs, wired on the production path via DimensionalSolver::solve_ranked_impl
      and both engine_eval.rs consumption seams. Grep-verified 2026-08-26.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: enum[[:space:]]+Completeness
      paths:
      - crates/reify-ir/src
      expect: present
  - name: additive-only-is-required
    binding: anti-inversion — F-result invariant I1 freezes SolveResult and ConstraintSolver::solve().
      Production matches destructure Solved{values, unique} WITHOUT `..`, so a sibling carrier is the
      only back-compatible shape.
    verdict: PASS
  - name: completeness-precedent
    binding: 'capability→producer — SolveAllResult (solutions + complete) in crates/reify-constraints/src/cpsat.rs,
      landed by PRD 2 beta #5468 (done). Its doc already states the conjunction doctrine this PRD promotes
      to a law.'
    verdict: PASS
- label: β
  task_id: 6707
  title: 'solution-set-completeness β (adopts #5388): honesty floor — stop asserting unproven uniqueness;
    typed cause; code the five free-auto sites'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6901; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: uniqueness-path-exists
    binding: capability→producer — verify_uniqueness and finalise_uniqueness in crates/reify-constraints/src/solver.rs,
      reached from solve_with_meta and solve_ranked_impl. Grep-verified.
    verdict: PASS
  - name: rewritten-message-actually-fires
    binding: 'G6 branch 4 (rejection OBSERVED, not assumed) — tests/prd-gate/fixtures/ssc_two_roots_strict.ri
      observed at the anchor commit as exit 1 with ''strict auto parameter resolution is not uniquely
      determined — consider using auto(free) for exploration'' and the cause mislabelled ''solve failed:
      infeasible''. β modifies an emission that demonstrably fires. (delivered_check pattern NARROWED
      2026-09-03, task 7221: it formerly grepped the whole sentence ''consider using auto(free) for
      exploration'', which a line-oriented git grep can never match — solver.rs:3496-3498 splits the
      literal across three Rust string-continuation lines and writes the em-dash as the escape
      \u{2014}. Scoped to this check''s own paths the full sentence matched nothing at the anchor
      2128c3692c or at main d6909c3497 (rc=1), so an expect:absent check read DELIVERED vacuously from
      authoring. The narrowed form matches exactly solver.rs:3497 — rc=0 at both refs, i.e. FAILED /
      correctly not-yet-delivered, flipping to DELIVERED only when β retires the message.)'
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: consider using auto\(free\)
      paths:
      - crates/reify-constraints/src
      expect: absent
  - name: codeless-sites-are-informationless
    binding: capability→producer — five byte-identical code-less Diagnostic::warning sites emitting 'resolved
      via auto(free) -- result is not uniquely determined' (engine_eval.rs x3 incl. push_merged_cluster_nonunique_warnings,
      engine_edit.rs x2). OBSERVED emitting identical text on ssc_two_roots_free.ri (two roots) and ssc_single_root_free.ri
      (one root) — the pair that proves the signal carries no information.
    verdict: PASS
  - name: undef-cause-enum-exists
    binding: capability→producer — pub enum UndefCause in crates/reify-ir/src/value.rs, the INV-SF-1 typed-cause
      precedent β extends.
    verdict: PASS
  - name: no-inverted-edge-on-5711
    binding: 'anti-inversion — #5711 (in-progress) edits the same verify_uniqueness arm. It is a SIBLING,
      not downstream of β; coordination is required, an edge would point backwards.'
    verdict: PASS
  - name: determinacy-unchanged
    binding: signal premise (PRD section 8 scope rail a) — β changes what is CLAIMED, never what is COMPUTED;
      a strict auto whose set is Partial still resolves and stays Determined, or the corpus stops realizing
      geometry.
    verdict: PASS
    delivered_check:
      kind: manual
      reason: a negative behavioural property across the corpus; pinned by β's own regression tests, not
        by any stable grep.
- label: γ
  task_id: 6708
  title: 'solution-set-completeness γ: box branch-and-bound enumerator over #6655''s HC4 propagator (one
    engine, three verdicts)'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6900; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: hc4-propagator-upstream
    binding: capability→producer — producer:task-6655 (pending, no deps, dispatchable) delivers crates/reify-constraints/src/interval.rs
      and hc4.rs and is chartered to run on a sub-box for this PRD. UPSTREAM via a real add_dependency
      edge.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: fn[[:space:]]+.*subdivide|enum[[:space:]]+BoxVerdict|fn[[:space:]]+enumerate_solutions
      paths:
      - crates/reify-constraints/src
      expect: present
  - name: interval-substrate-absent-today
    binding: 'anti-orphan — grep-verified ABSENT on main: no interval arithmetic, contractor, box consistency,
      HC4 or branch-and-bound in crates/. DerivedInterval is a one-pass syntactic bound miner over four
      linear-in-one-auto shapes. This is why #6655 is a hard prerequisite, not a background assumption.'
    verdict: PASS
  - name: jacobian-source
    binding: 'capability→producer — preferred P2 leaf epsilon = task 6672 (forward-mode dual numbers over
      CompiledExpr; no autodiff exists in the workspace today and no external crate is adopted). Unconditional
      fallback: local finite differences. Soundness is unaffected either way (C8), so the edge is DELIBERATELY
      LEFT UNWIRED rather than blocking gamma on a sibling PRD for an accuracy gain it does not need.'
    verdict: PASS
    delivered_check:
      kind: manual
      reason: which of two sound sources is used is a section-11 Q1 implementation choice, not a checkable
        postcondition.
  - name: angle-domains-canonicalised
    binding: correctness precondition (G7 INV-AD-2) — default_bounds_for gives ANGLE a (-tau, tau) box,
      a DOUBLE COVER of the circle; section 3.3 requires canonicalisation to one period before Exhaustive
      may be claimed, else the completeness machinery reports every rotational solution twice.
    verdict: PASS
    delivered_check:
      kind: manual
      reason: surfaced by the G7 walk; pinned by γ's own angle-domain test, and the helper name is not
        fixed until implementation.
- label: δ
  task_id: 6709
  title: 'solution-set-completeness δ: verdict policy — both roots reach the user; retire the unconditional
    free-auto warning'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6901; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: toleranced-verdicts-upstream
    binding: capability→producer — producer:task-6653 (pending, high). Without it a correctly-solved root
      is re-checked at exact f64 and reported violated (probe-observed on five fixtures), so δ's fixtures
      could not be green end-to-end. UPSTREAM edge.
    verdict: PASS
  - name: fixtures-parse
    binding: grammar-fixture — tests/prd-gate/fixtures/ssc_two_roots_free.ri, ssc_two_roots_strict.ri
      and ssc_single_root_free.ri each parse under tree-sitter parse --quiet with exit 0 and 0 ERROR nodes,
      2026-08-26. No novel syntax (PRD section 2.3).
    verdict: PASS
  - name: baseline-is-measured
    binding: G6 branch 3 — every 'today' claim δ's signal contrasts against is OBSERVED with exit code
      and verbatim text (PRD section 0.2), not inferred from code reading.
    verdict: PASS
  - name: silence-assertion-falsifiable
    binding: G6 branch 4 (negative assertion) — BT4 asserts the non-uniqueness warning STOPS firing on
      ssc_single_root_free.ri. It is OBSERVED firing there today, so the assertion has an observable before-state
      and cannot pass vacuously.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: W_SOLUTION_NOT_UNIQUE|SolutionNotUnique
      paths:
      - crates/reify-core/src/diagnostics.rs
      expect: present
  - name: ordering-edge-not-on-critical-path
    binding: anti-inversion — delta's fixtures carry ONE auto by design, so they have no axis order to
      permute and C6's multi-auto ordering claim (P1 gamma = 6691) is not on delta's critical path; it
      is asserted at zeta = 6711, which is dep-wired to 6691.
    verdict: PASS
- label: ε
  task_id: 6710
  title: 'solution-set-completeness ε: refutation by subdivision — typed proven-infeasible naming the
    narrowing constraint, zero solver iterations'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6900; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: empty-box-arm-upstream
    binding: capability→producer — producer:task-6655 delivers the whole-box empty implies typed proven-infeasible
      arm; ε extends it to the subdivision case. UPSTREAM edge.
    verdict: PASS
  - name: fixture-is-not-6655s
    binding: 'producer-extent guard (deliberate) — tests/prd-gate/fixtures/ssc_refuted_pair.ri (L == 10mm
      AND L == 20mm) parses clean and is OBSERVED today as exit 1 with ''constraints could not be satisfied
      (max absolute residual: 5.00e-3)''. HC4 refutes it AT THE ROOT BOX, so #6655 alone turns it green;
      it is committed as C3 baseline evidence, NOT as ε''s signal. ε''s own fixture — a root box HC4 does
      not empty, refuted only by subdivision — cannot be authored or verified before #6655''s propagator
      exists.'
    verdict: PASS
    delivered_check:
      kind: manual
      reason: constructing the fixture AND demonstrating that root-box HC4 leaves it undecided is part
        of ε's acceptance; it is an empirical property of a propagator that does not exist yet.
- label: ζ
  task_id: 6711
  title: 'solution-set-completeness ζ: stop discarding the alternatives — dedup by basin box, attach completeness,
    render in reify explain'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6902; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: alternatives-produced-and-discarded
    binding: capability→producer — solve_ranked_impl produces K = 2(dim+1) ranked candidates; both engine_eval.rs
      seams call swap_remove(0) and discard the rest. RankedSolveResult appears in NO crate outside reify-ir,
      reify-constraints and reify-eval. The producer exists and the consumer is absent — ζ IS the consumer.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: swap_remove\(0\)
      paths:
      - crates/reify-eval/src/engine_eval.rs
      expect: absent
  - name: rendering-surface-exists
    binding: 'capability→producer — reify explain (cmd_explain) plus ObjectiveProvenance, landed by constraint-solver-completion
      theta #4015 and iota #4017 (both done), wired on the production CLI path. Carries no optimality,
      uniqueness or completeness token today.'
    verdict: PASS
  - name: dedup-key-genuinely-missing
    binding: signal premise — solve_ranked_impl documents that non-winning candidates are NOT deduplicated,
      so for a single-basin objective most of K are near-identical convergences of one optimum. C5's box
      identity is the missing key.
    verdict: PASS
  - name: deterministic-axis-order
    binding: DAG-direction, WIRED — P1 leaf gamma = task 6691 (deterministic ordering, I4 / T1). P1 landed
      as 108d1d9226 and decomposed during this session, so this is a real add_dependency edge (6711 ->
      6691), not a deferred obligation. zeta is the first multi-auto leaf and the first that can assert
      C6 in full.
    verdict: PASS
- label: η
  task_id: 6712
  title: 'solution-set-completeness η: basin stability across warm re-solves — continuation from the incumbent
    + W_BASIN_CHANGED'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6902; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: warm-paths-exist
    binding: capability→producer — edit_param (crates/reify-eval/src/engine_edit.rs) seeds current_values
      from the prior snapshot INCLUDING previously solved autos; reached from the GUI set_parameter and
      crates/reify-cli/src/mcp_context.rs. Engine::eval_cached is the second warm path.
    verdict: PASS
  - name: cold-only-hole-is-real
    binding: capability→producer — W_SOLVER_OPTIMALITY_UNPROVEN and detect_underdetermined have production
      call sites only in Engine::eval, not eval_cached; dispatch_merged_cluster_solve_cached documents
      the consequence in-tree.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: W_BASIN_CHANGED|BasinChanged
      paths:
      - crates/reify-core/src/diagnostics.rs
      expect: present
  - name: entry-point-parity
    binding: DAG-direction, WIRED — P1 leaf kappa = task 6699 (warm and cold choose the same solve entry
      point). Real add_dependency edge 6712 -> 6699. eta owns basin IDENTITY; P1 owns entry-point PARITY.
      Without parity a basin change is indistinguishable from an entry-point artefact.
    verdict: PASS
- label: θ
  task_id: 6713
  title: 'solution-set-completeness θ: C1 conformance — no unique flag written from anything but a completeness
    verdict'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6903; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: overclaiming-sites-exist
    binding: capability→producer — SolveSpaceSolver::solve hardcodes a true unique flag on every libslvs
      Ok arm (and it IS in SolverRegistry::production()'s Geometric slot); relate_solve::solve_frame derives
      the flag from LOCAL Jacobian rank. Grep-verified.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: unique:[[:space:]]*true
      paths:
      - crates/reify-constraints/src/solvespace.rs
      expect: absent
  - name: does-not-fix-code-p2-deletes
    binding: anti-inversion, resolved by re-scoping and now WIRED — P2 leaf xi = 6682 deletes SolveSpaceSolver,
      GeometricPattern and recognize_pattern outright; P2 leaf iota = 6677 routes relate-solve through
      the unified problem. theta was re-scoped from 'fix two drivers' to 'C1 conformance over whatever
      writes the unique flag after P2 iota', and is dep-wired 6713 -> 6677.
    verdict: PASS
  - name: in-progress-collision-sequenced
    binding: 'anti-inversion — #6659 is in-progress with a LIVE CLAIMANT editing registry.rs and solvespace.rs;
      θ is sequenced after it rather than racing it.'
    verdict: PASS
- label: ι
  task_id: 6715
  title: 'solution-set-completeness ι: the completeness composition law at the registry (the cross-domain
    contract)'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6903; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: conjunction-exists-to-replace
    binding: capability→producer — SolverRegistry::solve_inner conjoins the per-component unique flag
      (all_unique / other_unique) and forces it false on intermediate lexicographic ranks. The section-3.5
      meet replaces that conjunction in place.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: fn[[:space:]]+.*meet_completeness|Completeness::meet
      paths:
      - crates/reify-constraints/src/registry.rs
      expect: present
  - name: not-blocked-on-prd2-zeta
    binding: 'anti-inversion — ι implements the meet over COMPONENTS, which exist today (multiple continuous
      components per model). PRD 2 zeta #5472 is a CONSUMER of the law, not a producer of ι''s capability,
      so ι is not blocked on a pending PRD-2 leaf.'
    verdict: PASS
  - name: registry-collision-sequenced
    binding: 'anti-inversion — sequenced after #6659 (in-progress, live claimant, same file).'
    verdict: PASS
  - name: declined-component-has-a-verdict
    binding: 'seam with P2 leaf nu = 6681 (capability-based routing, same file registry.rs). Once routing
      can DECLINE a component, the section-3.5 meet needs a Completeness value for it: Partial carrying
      P2 leaf delta = 6671''s typed per-constraint refusal reason — never NotAttempted, which theta retires
      per the INV-SF-4 resolution. Stated so an iota implementer meeting a declined component does not
      improvise a verdict.'
    verdict: PASS
    delivered_check:
      kind: manual
      reason: depends on the refusal vocabulary P2 delta 6671 ships; assertable only once both land.
- label: λ
  task_id: 6716
  title: 'solution-set-completeness λ: envelope calibration — measure the dimension/node envelope, set
    the caps from the data'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6900; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: node-budget-not-wallclock
    binding: numeric floor / C7 — the enumeration budget is a NODE COUNT, never seconds. tests/infra/test_no_new_wallclock_upper_bounds.sh
      therefore needs no new registration, and determinism survives a loaded machine.
    verdict: PASS
  - name: cap-is-measured-not-guessed
    binding: G6 branch 1 — the cap value is DELIBERATELY not asserted at authoring; λ IS the measurement,
      which is branch 1's sanctioned resolution for an otherwise-guessed threshold. The only neighbouring
      datum is the measured Nelder-Mead simplex knee (about 10-15 vars; WHOLE_MODEL_CLUSTER_DIM_CAP =
      12), used as an ordering hint, not as the bound. Enumeration and refutation get SEPARATE caps —
      refutation needs no uniqueness test and prunes harder.
    verdict: PASS
    delivered_check:
      kind: manual
      reason: the deliverable is a committed dated measurement note plus a constant derived from it; the
        constant's value is unknown until λ runs.
- label: κ
  task_id: 6718
  title: 'solution-set-completeness κ: docs-truth — constraints.md multiplicity section, discrete_choice.ri
    correction, INDEX + cheatsheet, discoverability'
  note: 'E3 coarse-arm A/B: this standard leaf was retired to `deferred` on 2026-08-28 and its scope replaced
    by #6902; its `x_e3_flip_condition` makes it a section-11 ROLLBACK TARGET, so this entry''s text and
    delivered_check stay live rather than being deleted. Mapping:
    docs/prds/v0_6/e3-decomposition-granularity-ab.md section 5.'
  capabilities:
  - name: chunk-gap-is-real
    binding: capability→producer — crates/reify-mcp/src/tools/chunks/constraints.md contains ZERO uniqueness
      or multiple-solution content (grep-verified for unique, multiple solution, root, basin — no hits).
      κ is its producer.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: more than one solution|not unique|multiple solutions
      paths:
      - crates/reify-mcp/src/tools/chunks/constraints.md
      expect: present
  - name: exemplar-carries-a-false-claim
    binding: 'G6 branch 3 — examples/best_practices/discrete_choice.ri states that real branch-and-bound
      over discrete variables is what CP-SAT will bring. FALSE for its own file: side is a continuous
      Real auto with no finite domain, and CP-SAT requires one via build_variable_domain, so the CP-SAT
      path can never reach it. Rung 2 serves this exemplar, not rung 1.'
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: what CP-SAT will bring
      paths:
      - examples/best_practices/discrete_choice.ri
      expect: absent
  - name: index-edit-is-gated
    binding: capability→producer — best_practices_index_matches_corpus_directory build-gates examples/best_practices/INDEX.md
      against the corpus directory; the row edit is same-diff with the exemplar edit.
    verdict: PASS
- label: μ
  task_id: 6719
  title: 'solution-set-completeness μ: PRD close — terminal stamp, landed leaf IDs, AS-AUTHORED freeze,
    LIVE/AS-AUTHORED map'
  note: 'E3 coarse arm: reused in place as a singleton (`x_e3_reused: true`, status `pending`); still the live
    producer for this capability.'
  capabilities:
  - name: terminal-vocabulary-specified
    binding: capability→producer — the closed three-value terminal vocabulary (SHIPPED / SUPERSEDED /
      WITHDRAWN) and the three-part freeze header are specified in the PRD overlay; in-corpus exemplars
      docs/prds/v0_6/data-carrying-enums.md and docs/prds/kernel-seam-contracts.md.
    verdict: PASS
    delivered_check:
      kind: grep
      pattern: Status.*(SHIPPED|SUPERSEDED|WITHDRAWN)
      paths:
      - docs/prds/v0_6/solution-set-completeness.md
      expect: present
  - name: close-leaf-stays-dispatchable
    binding: anti-inversion — μ depends on every sibling by real add_dependency edges; a cancelled sibling
      counts as satisfied for the edge, per the overlay's cancelled-dependency disposition.
    verdict: PASS
