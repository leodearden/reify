# Capability manifest — builtin-signature-registry

PRD: `docs/prds/v0_6/builtin-signature-registry.md`. Binds each leaf's asserted capabilities to evidence (G3+G6 mechanized). Machine-readable twin: `builtin-signature-registry.capability-manifest.yaml`. All source anchors verified 2026-08-04 against main `5fb3959fbb` (evidence lineage: the 2026-08-03 measured enumeration, `docs/notes/fallback-soundness-analysis-2026-08-03.md` + `docs/notes/fallback-soundness-xref-2026-08-03.json`).

**D3 verification note (2026-08-04, run `wf_53a7db08-66a`)**: the γ workflow's adversary correctly FAILED the α leaf's original probe-based registration premise as VACUOUS — `reify check` exits 0 on an unregistered name (`tests/prd-gate/fixtures/unknown_fn_silent_accept_baseline.ri`), on a bogus match-variant name, and on a mis-annotated payload (controls run 2026-08-04). Resolution: registration/typedness evidence re-homed to source-level wired bindings below; `.ri` fixtures serve as parse/regression baselines only. This vacuity is itself the hole the PRD closes (boundary #8).

| Leaf | Capability | Binding | Verdict |
|---|---|---|---|
| α | parse fns registered+typed today (seed family) | wired — `crates/reify-builtins/src/registry.rs:31` `registry!` rows `ParseLength`/`ParseLengthR`; compiler seam `crates/reify-compiler/src/builtin_registry.rs:43` `registry_result_type`; exhaustive eval arms `crates/reify-stdlib/src/registry_dispatch.rs:37`. **Superseded 2026-08-30 by α itself** — the pre-α evidence was `crates/reify-compiler/src/parse_signatures.rs:22` `PARSE_FN_NAMES` + eval arm `crates/reify-stdlib/src/parse.rs:17` (verified 2026-08-04 against main `5fb3959fbb`), but α deleted `parse_signatures.rs` outright and removed `parse.rs`'s `eval_parse` string matcher, so those anchors are historical, not live. Regression fixture `tests/prd-gate/fixtures/parse_length_match_resolves.ri` stays non-probative for registration (see D3 note) | PASS |
| α | analysis fns registered (seed family) | wired — `crates/reify-builtins/src/registry.rs:31` `registry!` rows `VonMises`/`MaxShear`/`PrincipalStresses`/`SafetyFactor`/`StressInvariants` (5 names); exhaustive eval arms `crates/reify-stdlib/src/registry_dispatch.rs:37`. **Superseded 2026-08-30 by α itself** — the pre-α evidence was `crates/reify-compiler/src/analysis_signatures.rs:38` `ANALYSIS_FN_NAMES` (verified 2026-08-04 against main `5fb3959fbb`), a file α deleted. | PASS |
| α | post-merge corpus byte-identity (PRD §7.3(6)) | measured 2026-09-10 — `reify check` stdout, stderr and exit code byte-identical between main `b42c7dd207` and the merged α branch `34165b4be6` for `examples/fields_analysis.ri` and `tests/prd-gate/fixtures/parse_length_match_resolves.ri`; each binary built from its own tree and run over the SAME staged inputs (stdlib verified identical first), so the comparison isolates the compiler. Re-baselined against MAIN, because post-merge "today's behaviour" is main's post-#6577 behaviour: main's own acceptance tests are GREEN through the registry path — `harness_geometry_solver::solver_elastic_static_stdlib_compile` (13 passed) and `harness_compilation_surface::fea_bracket_minimize_mass_example` (1 passed). The parse fixture stays NON-probative for registration (D3 vacuity finding) | PASS |
| α | seed rows carry #6577's Field forms | wired — `crates/reify-builtins/src/resolvers.rs:67` `field_tensor_arg` + `:117` `reduce_tensor_arg`, the two-form resolver every analysis row routes through. Main landed #6577 INSIDE `crates/reify-compiler/src/analysis_signatures.rs` — the file α deletes — so the merge would have silently regressed it: a `Field` argument must type as `Type::Field`, not a reduced scalar, because eval wraps the field lazily and `value_type_kind_matches` (`crates/reify-eval/src/lib.rs:330`) maps `Value::Field` onto `Type::Field` alone. Ported forward, not re-derived — the source read is byte-identical to `b42c7dd207:crates/reify-compiler/src/analysis_signatures.rs` (verified 2026-09-10). Row-table consequences: `safety_factor` moved `Const` → `ArgAware`; `stress_invariants` stays `Const` (eval has no Field arm for it); the I-REG-7 `Artifact` ledger does **not** move — a wider input shape is not #2884's constrained-input, Pressure-fixed signature, so `EXPECTED_ARTIFACT_ROWS` stays at three | PASS |
| α | `macro_rules` registry substrate | Rust language substrate; strum workspace-dep precedent for derives (`reify-ir` `GeometryOp` discriminants) | PASS |
| α | row-without-eval-arm build failure | producer: this leaf (negative recipe deliverable, doc-comment form per 5055 precedent) | PASS |
| β | `CompiledExpr` lives in reify-ir | wired — `crates/reify-ir/src/expr.rs` (FunctionCall kind; task 3702 lineage) | PASS |
| β | content-hash currently excludes result_type | wired — `crates/reify-compiler/src/expr.rs:3587-3594` (hash = tag + qualified_name + arg hashes) | PASS |
| τ1 | numeric eval arms exist (floor/ceil/round → Int; log10 → Real) | wired — `crates/reify-stdlib/src/numeric.rs:37-42`; sinh/cosh/tanh `crates/reify-stdlib/src/trig.rs:38-40` | PASS |
| τ1 | dimensioned silent-accept pre-state | fixtures `numeric_floor_dimensioned_silent_accept.ri`, `numeric_sinh_dimensioned_silent_accept.ri` (check exit 0 today, D3-verified); probe evidence `floor(2.5mm)` → `Int(0)` (2026-08-03 sweep) | PASS |
| τ1 | two-arg floor call syntax parses | grammar-fixture — `tests/prd-gate/fixtures/numeric_floor_two_arg_parses.ri` (0 ERROR nodes, D3-verified) | PASS |
| τ1 | two-arg floor eval semantics | producer: this leaf (new capability — `floor(x/q)·q`; not asserted as existing) | PASS |
| τ1 | prose deferral to close | wired — `crates/reify-compiler/src/math_signatures.rs:90-92` (uncited deferral comment) | PASS |
| τ2 | orientation/frame eval arms exist | wired — `crates/reify-stdlib/src/orientation.rs` (`orient_*` arms :10-563), `crates/reify-stdlib/src/geometry.rs:512` (`frame_to_frame`) | PASS |
| τ2 | frame_to_frame mistyping pre-state | fixtures `frame_to_frame_resolves.ri` (check exit 0 silent today), `frame_to_frame_transform3_nomatch.ri` (D3-verified) | PASS |
| τ2 | member syntax on nominal returns parses | grammar-fixture — `orient_axis_angle_member_parses.ri`, `orient_to_axis_angle_call_resolves.ri` (D3-verified) | PASS |
| τ2 | orientation ctor registrations upstream | producer: task 5344 (in-progress) — hard `add_dependency` edge wired at decompose | PASS |
| τ2 | nominal-structure-over-Map precedent | wired — `crates/reify-compiler/src/joint_signatures.rs` `joint_ctor_result_type` (:128) types Map-valued joint builtins as StructureRef | PASS |
| τ3 | joint family eval arms + compiler table | wired — `crates/reify-stdlib/src/joints.rs:193-719` (`transform_at`, `joint_*`); `joint_signatures.rs:69` (17 names) | PASS |
| τ4 | FEA/flexure/stackup eval arms | wired — `crates/reify-stdlib/src/fea.rs:47-102`, `flexures/*.rs`, `stackup.rs:17-21`, `dfm.rs:32`, `tolerancing.rs:18-19`, `supports.rs:116-134`, `loads.rs:134`, `tensegrity.rs:22-23` | PASS |
| τ5 | mechanism/trajectory eval arms | wired — `crates/reify-stdlib/src/mechanism.rs:42`, `snapshot.rs:449-481`, `sweep.rs:141`, `dynamics/eval.rs:160-164`, `trajectory/mod.rs:70-104` | PASS |
| τ5 | piecewise_polynomial is a stub | wired (as stub) — `trajectory/mod.rs:104` always-Undef; row ledgered + PTODO cite required (INV-SF-5) | PASS |
| τ6 | complex family eval arms | wired — `crates/reify-stdlib/src/complex.rs:65-272` (`re`/`im` aliases + `complex_*` ops) | PASS |
| τ7 | compose is `.ri`-owned | wired — `crates/reify-compiler/stdlib/fields.ri:118` typed generic decl (task 4224); resolution fixture `compose_fn_field_resolves.ri` + rejection controls `compose_one_arg_rejected.ri`, `compose_middle_type_mismatch_rejected.ri` (D3-verified) | PASS |
| τ8 | eval-side consumer maps exist | wired — `crates/reify-eval/src/geometry_ops.rs` (`is_geometry_query_call`:3856, selector map :5314-5352); drift test `crates/reify-eval/src/registry_drift_tests.rs` (task 5055) as migration validator | PASS |
| τ9 | GeometryOp descriptor table to bridge | wired — `crates/reify-ir/src/geometry.rs:569` strum discriminants (4670-4675 program) | PASS |
| π | LSP table to replace | wired — `crates/reify-lsp/src/completion.rs:314` `BUILTIN_FUNCTIONS` (~95 entries; 35 contradict compiler per enumeration) | PASS |
| ψ | value-kind check primitive exists | wired — `value_type_kind_matches` `crates/reify-eval/src/lib.rs:313`, still PRIVATE (a plain `fn`, not even `pub(crate)`). **Anchor corrected 2026-09-14**: the old `:307-333` named main `823502024d`'s `fn` line plus its `match value` block; ψ's own `mod` hook moves the `fn` line to `:313` and the `Value::Undef => true` sentinel arm to `:326`. reify-eval deps reify-compiler + reify-stdlib (Cargo.toml); ψ adds reify-builtins as a **dev**-dep only, so `cargo tree -e no-dev -p reify-eval` and `crates/reify-eval/engine_hash_closure.txt` are unchanged | PASS |
| ψ | executed static-vs-runtime parity harness | **delivered** — `crates/reify-eval/src/registry_parity_tests.rs`, hooked at `crates/reify-eval/src/lib.rs:118`; run `cargo test -p reify-eval --lib registry_parity` (12 tests, 12 passed / 0 failed, measured 2026-09-14). Membership is DERIVED from `reify_builtins::rows()` filtered on `BindingKind::EvalBuiltin`, so each τ's rows join with no edit to the module; argument data is compile-forced by an exhaustive `match EvalBuiltinId` with no `_` arm (the I-REG-2 forcing shape `registry_dispatch::dispatch` uses). The verdict is THREE-way — `Matches` / `Vacuous` / `Diverges` — because `value_type_kind_matches` accepts `Value::Undef` for every type (`crates/reify-eval/src/lib.rs:326`), so a two-way harness is defeated by exactly the buggy-eval-body residue §3 decision 12 names. Asserts through the public `reify_stdlib::eval_builtin` (`row.name` passed as a VARIABLE — I-REG-1 clean, no name literal in the file). **Amended 2026-09-15** (review pass, 13 tests / 13 passed): (a) the verdict is driven by BOTH of `value_type_kind_matches`' trivial-accept paths, not just one — a declared `Type::Error` short-circuits its anti-cascade guard to `true` BEFORE the value is inspected, so it classifies `Vacuous` alongside `Value::Undef`; unreachable on α's total resolvers, but exactly where a τ resolver wired to `E_BuiltinArgShape` diagnostics (§3 decision 5) starts landing. (b) `representative_probes` returns a LIST of probes per row (each a `Vec<(Value, Type)>`), so a τ row can register a second argument SHAPE alongside its first instead of the exhaustive-`match` freezing in one shape per row forever; a row is reported at its most severe probe (`worst_probe`), because a shape that matches does not certify a shape never compared | PASS |
| ψ | exemption ledger is EMPTY, and enforced both ways | measured 2026-09-14, not stubbed — all 7 seed rows classify `Matches` under the executed sweep, so `PARITY_EXEMPTION_LEDGER` has no entries. The PRD's named seed entry (`piecewise_polynomial`, §4/§9) cannot be present yet: that name has no registry row and is still answered by the legacy string arm at `crates/reify-stdlib/src/trajectory/mod.rs:107`; it joins as a `Vacuous` entry when τ-mechanism/trajectory migrates. The empty ledger has teeth because `adjudicate` enforces BOTH directions — an unledgered non-`Matches` row fails, and so does a stale entry whose row now passes — so it cannot be padded with entries that assert nothing. Non-vacuity OBSERVED by 3-leg mutate-run-revert (wrong declared type; buggy eval body with the table untouched; an argument-less probe). **Amended 2026-09-15**: the module header records each leg's EDIT and expected verdict CLASS, no longer the verbatim failure text — that transcription embedded pass/fail tallies which are a function of the module's test count and went stale the moment this pass added a test, with nothing in the tree able to detect it. Two further legs observed at this pass and likewise recorded by class, not transcript: deleting `classify`'s `Type::Error` guard (RED, `left: Matches` / `right: Vacuous`) and inverting `worst_probe` so it reports a row's BEST probe (RED) — in both runs every OTHER test stayed green, confirming each new guard is the only thing holding its rule | PASS |
| ψ | PRD open question 5 resolved (harness placement) | **in-crate reify-eval module, not `crates/reify-compiler/tests/`.** Deciding argument: `value_type_kind_matches` is private (`crates/reify-eval/src/lib.rs:313`) and I-REG-4 requires asserting against THAT function with no second derivation — an integration test is a separate crate and cannot reach it, and making it `pub` would widen reify-eval's API solely to host a test. The cost of the alternative is already in-tree: `crates/reify-compiler/tests/harness_type_checking/mul_div_static_runtime_parity.rs:177` re-authored its own `value_kind_matches_type` for precisely this reason. Three corroborating grounds (the sibling `registry_drift_tests` precedent, no new production dependency edge, no new gate artifact) are in the module header. Recorded here so the next τ author does not re-litigate it; `builtin-signature-registry.md` is deliberately NOT edited, because every sibling τ leaf shares that file | PASS — Q5 answered |
| ψ | **residue that ψ does NOT close** — `Type::Field` half of the ArgAware rows | Recorded 2026-09-15 so the next τ author does not read the harness wider than it is. `reduce_tensor_arg` (`crates/reify-builtins/src/resolvers.rs`) has TWO branches — a `Type::Field` arm (task 6577) and a concrete fall-through — so `von_mises` / `max_shear` / `principal_stresses` / `safety_factor` each declare a `Field<D, ..>` result for a Field argument. Every ψ probe is concrete, and a Field probe CANNOT be added to this harness: `reify_stdlib::eval_builtin` has no Field handling at all (`crates/reify-stdlib/src/analysis.rs` contains zero `Value::Field`), because the lazy `wrap_tensor_field` that produces one lives in `crates/reify-expr/src/analysis.rs` behind reify-expr's evaluator ladder — so a Field probe here would evaluate to `Value::Undef` and certify nothing. The §3 decision 12 residue therefore stays OPEN for the Field half of those four rows; it is reify-expr's to close, not ψ's. What IS covered is one side of the pair only: the resolvers' `Type`-level Field contract has unit tests in `crates/reify-builtins/src/resolvers.rs` (ported from 6577), which is type algebra with no execution in it. Filed as follow-up (ticket `tkt_0RTNCFGC08S8W935JZPBZ0CR8J`); ψ's `representative_probes` already returns a LIST so that shape can be registered when a leaf routes Field evaluation through this path | PASS — ceiling RECORDED (the residue itself stays open, by scope, not by waiver) |
| ψ | **drift-guard: no registration needed** — VERIFIED, not assumed | The trigger (`.claude/skills/prd/project.md:170`) is scoped to a new gate-resident `crates/*/tests/*.rs` binary or a new `tests/infra/test_*.sh`; ψ ships an in-crate `#[cfg(test)] mod`, which is neither. Per-anchor, measured 2026-09-14 on `task/6013`: `tests/infra/run-all-classification.manifest` — 238 rows, **0** matching `crates/` (its header scopes it to `tests/infra/test_*.sh`, and discovery at `run-all-classification-lib.sh:177` globs `"$_dir"/test_*.sh` only); no wallclock budget file exists and `tests/infra/test_no_new_wallclock_upper_bounds.sh:107` iterates `"$dir"/*.sh` only; `.config/nextest.toml` contains **0** occurrences of `partition` and already routes `package(reify-eval)` into the occt test-group at `:224` — adding a priority override there would turn `tests/infra/test_nextest_slow_priority.sh` assertion D (`:259-277`, "the `crates/<pkg>/tests/<binary>.rs` source file exists on disk") RED, because an in-crate lib module has no such file | PASS |
| ω | fallback site + pre-state | wired — `crates/reify-compiler/src/expr.rs:3568-3584`; baseline fixture `unknown_fn_silent_accept_baseline.ri` (check exit 0 today, verified 2026-08-04); flip gated on task 5997 (hard edge) | PASS |
| λ | boundary rows are deliverables | producer: this leaf (integration gate; §8 table is the signal source) | PASS |
| ρ | exemplar auto-compile gate exists | wired — `crates/reify-compiler/tests/examples_smoke.rs`; `examples/best_practices/INDEX.md` present | PASS |

No binding resolves to a blocking class (`declared-only` / `test-only` / `producer-downstream` / `producer-absent` / `fixture-ERROR` / `bound≤floor` / `rejection-absent`). The one D3 FAIL set was adjudicated by re-homing evidence (see note), not by waiving it.
