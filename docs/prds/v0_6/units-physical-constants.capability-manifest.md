# Capability Manifest — `units-physical-constants`

Binds each leaf task's asserted capabilities to evidence (mechanizes gates G3 + G6). All probes run 2026-06-01 against `target/debug/reify` (the real binary; tree-sitter CLI is stale per project G3 note). Any **FAIL** binding blocks queueing — none are FAIL.

| # | Leaf | Capability asserted | Evidence form | Verdict |
|---|---|---|---|---|
| 1 | α | `unit_to_scalar` extends with bare `A`/`mol`/`cd` arms | **wired-on-main**: `unit_to_scalar` (units.rs:442) is the `.or_else` fallback in `expr.rs:770-773`, consulted on every unit literal; existing `kg`/`g`/`K` arms prove the pattern. Additive Rust arms — no orphan. | **PASS** |
| 2 | α | bare `A`/`mol`/`cd` currently unresolved (gap is real) | **numeric/substrate floor**: `1A`/`1mol`/`1cd` ⇒ `error: unknown unit` today; `mA`/`mmol`/`mcd` resolve (prefixed-only emission, si_units.rs `SI_PREFIX_BASES`). | **PASS** (gap confirmed) |
| 3 | β | `e` bare builtin in `constants.rs` | **wired-on-main**: `resolve_builtin_constant` (constants.rs:43) is the live resolver behind bare `pi`/`tau`; adding an arm + `BUILTIN_NAMES` entry is the documented extension contract (constants.rs:14-16). | **PASS** |
| 4 | β | bare `e` parses as identifier, no sci-notation collision | **grammar-fixture**: `structure def E { let a = e  let b = e * 2.0 }` ⇒ no parse error; only `unresolved name: e` (the gap β closes). Guard tests already probe `"e"`. | **PASS** |
| 5 | γ | zero-arg `pub fn -> <dim-alias>` with sci-notation body parses & checks | **grammar-fixture**: `pub fn PLANCK_GOOD() -> Action { 6.62607015e-34 * 1kg * 1m * 1m / 1s }` + `structure def P { let h = PLANCK_GOOD() }` ⇒ `reify check` exit 0 (base units resolve in fn body). | **PASS** |
| 6 | γ | sci-notation literals parse in value position | **grammar-fixture**: `let a = 6.62607015e-34  let big = 6.02214076e23` ⇒ `reify check` exit 0 (task #3087). The `BOLTZMANN_CONSTANT()` decimal-form comment is stale. | **PASS** |
| 7 | γ | composite-dimension aliases resolve (no parens / no `^`) | **grammar-fixture**: `Energy * Time` ✓, `Capacitance / Length` ✓, `Energy / AmountOfSubstance / Temperature` ✓, `Dimensionless / AmountOfSubstance` ✓. Negative: `… / (A * B)` and `T^4` ⇒ syntax error ⇒ powers expanded, denominators left-assoc. `Real / AmountOfSubstance` ⇒ `cannot resolve 'Real'` ⇒ use `Dimensionless`. | **PASS** |
| 8 | γ | electrical/molar bodies compile **only after α** (cross-task dep is real) | **producer-dep**: `J`/`W`/`F`/`H`/`C` ⇒ `unknown unit` in a fn body (registry-less scope, units.rs `unit_to_scalar` table = `{mm,cm,m,in,deg,rad,kg,g,s,K}`). `elementary_charge`/`permittivity`/`permeability` need `A`; `avogadro`/`gas_constant` need `mol` ⇒ γ **depends on** α. Dependency wired. | **PASS** |
| 9 | γ/δ | **numeric floor — return-type is NOT a dimensional checksum** | **anti-false-premise**: `PLANCK() -> Action { … kg·m / s }` (wrong exponent) and `-> Action { 1.0 }` both `reify check` **clean**. ⇒ body correctness MUST NOT rely on the annotation. Mitigation: binary `+`/`-` DO dimension-check (`1m + 1s` ⇒ `dimension mismatch`), so δ's physics-identity cross-checks catch dimension errors, and δ's eval-tolerance assertions catch value errors. | **PASS** (mitigation specified) |
| 10 | δ | `reify eval` produces value + dimension for concrete bindings | **field-population**: `eval` of `let v = 299792458.0 * 1m / 1s` ⇒ `S.v = 299792458 m·s^-1` (real value, not `Undef`; constants are concrete, not solver-driven). | **PASS** |
| 11 | δ | physics identities are dimensionally consistent (premise true) | **numeric floor**: `R = N_A·k_B` (gas constant = Avogadro × Boltzmann) and `ε₀·μ₀·c² = 1` are exact SI/physical identities ⇒ `r_check ≈ 0` and `em_check ≈ 1` are achievable, not aspirational. Tolerance, not equality, per Q3. | **PASS** |
| 12 | δ | example + test consume the surface (anti-orphan integration gate) | **wired-on-main**: `examples/stdlib/constants.ri` + `constants_example_tests.rs` already exist (task #4026) and run in CI via `compile_with_stdlib`; δ extends both. The C-as-integration-gate for this batch. | **PASS** |
| 13 | ε | doc forms match shipped API | **anti-mismatch**: every reconciled §2.4 form is one of the verified-parseable shapes above (bare `pi`/`tau`/`e`; `UPPER_SNAKE()` fns). No `let <name> : Dim = value` top-level-const (Reify has none). | **PASS** |

**Value-provenance ledger (G6).** Each constant's magnitude is pinned to `docs/reify-stdlib-reference.md` §2.4 and CODATA: exact-by-2019-SI (`c`, `boltzmann`, `avogadro`, `planck`, `elementary_charge`, `gas_constant`=N_A·k_B); CODATA-measured doc values retained (`stefan_boltzmann`, `vacuum_permittivity`, `vacuum_permeability`). Anchor: `elementary_charge = 1.602176634e-19` equals the `eV` factor at `si_units.rs:151`. Transcription/exponent errors are caught by the manifest-row-9 mitigation (cross-checks), not by the inert return annotation.
