# Capability Manifest — warm-lane-pool-sizing-lifecycle.md

Mechanizes G3 + G6 per leaf. This PRD is shell/XFS/systemd/git/dark-factory-Python infrastructure — **no `.ri` grammar or semantic substrate**, so the grammar-fixture and numeric-floor (FEA) forms are **N/A**; the load-bearing forms here are **anti-orphan/wired-consumer** and **host-check evidence**, plus the **G6 no-frozen-number** binding (all disk/footprint figures are measured, never test constants). Every capability was verified live on the running pool 2026-07-11.

| Leaf | Asserted capability | Evidence form | Binding | Verdict |
|---|---|---|---|---|
| γ (fast-fix) | `data/queue/write_queue.db*` is tracked and untracking is safe | host-check + wired | `git ls-files data/queue/` = the 3 db files; `.gitignore` lacks `data/queue/*.db*`; `durable_queue.py:143` `mkdir(parents=True, exist_ok=True)` self-creates the dir | **PASS** |
| α | lane ASSIGNED/FREE distinguishable; backing-task status resolvable; recoverable-on-origin classifiable; footprint measurable | host-check | `lslocks` found the single live `cargo` lock (`_lane-22`); `get_statuses` mapped every lane's `task/NNNN` branch; `merge-base --is-ancestor`/`origin/<branch>` classify LANDED/PUSHED/NO; `du`/`df` measure footprint; 4749 `REIFY_LANE_LEAK_STATUS_CMD` seam present | **PASS** |
| α | audit consumer exists (not an orphan) | anti-orphan/wired | consumed by the operator + a health timer (ι open-Q 3) + feeds β budget and θ admission; it is the productized form of the 4749 detector's one-line alarm | **PASS** |
| β | loopback XFS online-grow works; budget is derived not frozen | host-check + G6 | `fallocate`+`losetup -c`+`xfs_growfs` standard on 6.x kernel, `xfsprogs` installed, host `data_lv` 4.5 TB free; the budget formula (§9.2) recomputes from live `df`/`du` — **no GB constant frozen** (D8/P4) | **PASS** |
| δ | free-first `rm -rf <lane>/target` frees divergent extents without staging a clone; source WIP survives | host-check + wired | `target/` is rebuildable; acquire re-seeds from base (cow-seeding D10); source WIP lives on the branch, restored by `reset_lane` (inv T1); free-before-stage avoids 5168's ENOSPC deadlock (inv T2); consumer = DF `release_lane` (η) | **PASS** |
| ε | disk-guard extensible with a soft threshold + sentinel; knobs declarable | host-check + wired | `warm-lane-disk-guard.sh` reads `df -B1 --output=avail,iavail`; `fleet-load-detector.sh` sentinel pattern is the template; `orchestrator.yaml warm_lane_pool` block exists (knobs currently **absent** — this leaf declares them); consumer = DF dispatch loop (θ) | **PASS** |
| ζ | integration gate asserts relations, never a frozen number | G6 | asserts resident-divergent ≤ budget, free recovers on release-thin, soft fires before hard — all *directions/relations* recomputed from live measurement (D8) | **PASS** |
| η (DF) | `release_lane` seam exists to wire | anti-orphan/wired | space-safety β/ε wired `acquire_warm_lane`/`create_worktree`; `release_lane` is in the same pool lifecycle; cross-project edge to δ/ε | **PASS** |
| θ (DF) | dispatch/acquire loop can consult a reify signal | anti-orphan/wired | prior art: `fleet-load-detector.sh` (host signal → DF admission loop) + `warm-lane-disk-guard.sh` (hard-floor exit-75 → DF requeue); cross-project edge to ε/α | **PASS** |

**No FAIL bindings.** Anti-orphan check: every reify primitive (α audit, β sizing, δ thin, ε oracle) names a live consumer (operator/timer, DF `release_lane`, DF dispatch loop) — none is a producer-orphan. Anti-inversion: no capability asserted at the wrong layer (audit/thin/oracle are all pool-mount-level shell, matching where the pool lives). No frozen numeric threshold (G6): the mean-divergent (~122 GB) and 56-vs-6 TB arithmetic are measured *context*, not test constants; β/ζ recompute from live `df`/`du`.
