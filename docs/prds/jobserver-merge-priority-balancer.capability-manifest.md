# Capability manifest — jobserver-merge-priority-balancer

Mechanizes G3 (substrate) + G6 (premise) per leaf. Any FAIL binding blocks queueing.
This is an **infrastructure** PRD (python daemon + systemd unit + shell); the reify
product-signal forms (field-population `Value::Undef`, grammar fixtures) are N/A —
the operative evidence forms are **wired-on-main** (the mechanism sits in the verify
pipeline's execute path / a live systemd unit, not test-only) and **measured-not-guessed**
numerics (PRD §5.C: every tuned constant is an *output* of the ε harness, never a
frozen guess — the G6 discipline this PRD applies to infra).

Substrate verification date: 2026-06-10 (all greps re-run at decompose).

## Shared substrate (G3) — verified once, cited by all leaves

| Substrate | Evidence | Status |
|---|---|---|
| `nproc == 32`, seed 32 | `nproc` → 32; `jobserver-canary.sh:20` `SEEDED=32`; unit `printf "%032s"` | PASS |
| cargo consumes `CARGO_MAKEFLAGS=--jobserver-auth=fifo:<path>` | `scripts/verify.sh:416-423` (current single-pool mechanism; path is an opaque string to cargo) | PASS |
| python3 `fcntl`/`termios.FIONREAD` + `O_RDWR\|O_NONBLOCK` | `scripts/jobserver-canary.sh:22-31` already does FIONREAD via python3 | PASS |
| `DF_VERIFY_ROLE ∈ {task, merge}` stamped on the merge path | `verify.sh:173,300-360` branches on it; `orchestrator.yaml:31-49` documents the orchestrator injecting `merge`; pre-existing, **no DF change** | PASS |
| FIFO-custodian systemd pattern | live `reify-jobserver.service` `ExecStart=… exec 7<>fifo; …; exec sleep infinity`, `PartOf=orchestrator-reify.service`, `Restart=on-failure`; generated by `setup-dev.sh:373-391` | PASS |
| GNU-make jobserver **implicit token** (every cargo runs ≥1 rustc without drawing the pool) | standard jobserver protocol semantics (cargo inherits make's); this is task's hard floor under absolute priority | PASS |
| `orchestrator.yaml verify_env` currently injects `CARGO_MAKEFLAGS` (the asymmetry δ removes) | `orchestrator.yaml:84` | PASS (exists → δ's edit is real) |
| Infra drift-test oracles ζ must update | `tests/infra/test_occt_flock_gate.sh:246-258` (Test 17, 60m outer timeout), `tests/infra/test_release_mode_in_test_command.sh`, `test_release_scoped_scope.sh`; band-aid lineage comment `verify.sh:680-690` | PASS |
| `scripts/jobserver-balancer.py` | does **not** exist — α *creates* it (producer-in-batch, upstream of every consumer) | PASS (not a fiction; queued as α) |

No grammar/parser substrate (no `.ri` surface) — grammar-fixture check N/A batch-wide;
`grammar_confirmed=true` on all tasks (no grammar work queued).

## Per-leaf bindings

### α — dual-FIFO seeding + balancer daemon (custodian)
- **Capability:** new `scripts/jobserver-balancer.py` seeds merge+task FIFOs to the baseline split, holds both `O_RDWR` (custodian, C5), runs the control loop with the **spin-grab transfer primitive and a minimal donate-idle tick**; `reify-jobserver.service` `ExecStart` rewritten to run it (via `setup-dev.sh` unit generation).
- **Substrate (G3):** all shared-substrate rows above. **PASS.**
- **DAG-direction (G6 branch 3) — resolved at decompose:** α's PRD signal asserts *idle-donor migration*, while §9's β bullet lists the full C4 policy including donate-idle. To keep the producer upstream (anti-inversion), α's filed scope **explicitly includes** the non-blocking spin-grab transfer primitive + a minimal donate-idle tick (this is the natural reading of α "runs the loop" + its migration signal); β refines the loop to the full C4 policy. Producer of every capability in α's signal = α itself. **PASS.**
- **Wired-on-main evidence:** the daemon is the `ExecStart` of a live systemd unit (`reify-jobserver.service`), and its FIFOs are consumed by every cargo the verify pipeline runs — not test-only. At α's close: `systemctl --user start reify-jobserver` → both FIFOs exist, `FIONREAD` sums to nproc; scripted idle-donor load shows the FIONREAD shift. **PASS** (δ is queued as the verify.sh-side consumer with dep edges via ε/η).
- **Numeric floor:** the baseline split value is a *placeholder pending ε* (any valid partition works for α's sum invariant; merge-favored per §4 C4). No guessed bound asserted. **PASS.**

### β — balancer policy (C4)
- **Capability:** full C4 policy — sense via FIONREAD, donate-idle → demanded, contention-ratchet task→merge (absolute merge priority), ε-buffer give-back, idle baseline-reset; single-threaded, spin-grab within a transfer (§5.B).
- **Substrate (G3):** α's daemon (dep edge α). **PASS.**
- **Premise (G6):** *contested→task ratchets monotonically to 0* — holds by construction: non-blocking spin-grab catches task-token releases; merge at `free(merge)==0` never donates back (C4 give-back fires only at `free(merge)>0`); monotone. *Task survives at 0 pool tokens* — the implicit-token row (shared substrate): every cargo always runs ≥1 rustc without drawing the pool. *Bounded ratchet, not instant preemption* is explicitly accepted (§4 C4 / §5.B) — no latency number is asserted by the signal. **PASS.**
- **Wired-on-main:** policy lives in the same live daemon (α's unit). Signal is an integration test over synthetic FIFO states — acceptable for an **intermediate** task (consumers ε and η are the real-load proof; η is the C-as-integration-gate leaf). **PASS.**
- **Numeric floor:** ε buffer + poll interval are ε's *outputs*, not asserted here. **PASS.**

### γ — two-pool canary
- **Capability:** `scripts/jobserver-canary.sh` rewritten to the C2 **sum** invariant (`FIONREAD(merge)+FIONREAD(task) == nproc` when idle); reseed = balancer-service restart; idle-only guard kept.
- **Substrate (G3):** existing canary + timer (`setup-dev.sh:393-417`, FIONREAD helper at `jobserver-canary.sh:22-31`); α's two FIFOs (dep edge α). **PASS.**
- **Premise (G6):** *a `32/0` idle split does NOT trigger reseed* — true by construction of the sum check (T-a boundary test pins it: a skewed-but-sum-correct state is legitimate). **PASS.**
- **Wired-on-main:** runs from the live `reify-jobserver-canary.timer`. **PASS.**

### δ — verify.sh role→FIFO selection + ownership move
- **Capability:** `apply_env` selects merge/task FIFO by `DF_VERIFY_ROLE` with **per-role `-p` guard** (absent FIFO → `CARGO_MAKEFLAGS` unset → cargo private pool, today's degraded mode); `CARGO_MAKEFLAGS` deleted from `orchestrator.yaml verify_env` (single source = C3).
- **Substrate (G3):** `apply_env` + `-p` guard pattern exist (`verify.sh:402-423`); `DF_VERIFY_ROLE` plumbed (`verify.sh:300-332`); the verify_env entry to delete exists (`orchestrator.yaml:84`). **PASS.**
- **Wired-on-main:** `apply_env` is in the execute path of every verify invocation (task + merge + hooks). Oracle signal = `--print-plan` env block (the established infra-test idiom, cf. `test_occt_flock_gate.sh`); the oracle test must create the role FIFOs if absent (the `-p` guard otherwise prints the unset comment). **PASS.**
- **No dep on α by design** (§9 preamble + §11): either landing order degrades gracefully to the private pool; the live window is closed by the host unit switch at α deployment.

### ε — empirical split + timeout tuning harness
- **Capability:** harness drives just-task / just-merge / mixed **real** cargo loads (warm + cold sccache); measures `/proc/stat` busy fraction, per-pool FIONREAD time series, merge + worst-case-cold-task wall clocks, exit-124 count; outputs tuned baseline split, poll interval, ε buffer, and re-derived timeout budgets; committed tuning report.
- **Substrate (G3):** α (daemon) + β (policy) + δ (role selection) via dep edges; `/proc/stat` + FIONREAD samplers are trivial host facilities. **PASS.**
- **Premise (G6 branch 1) — measured, not guessed:** the utilization threshold and all timeout budgets are **derived from the harness's own measurements** (PRD §5.C/§10.3), never frozen a priori into a RED test — the harness shape is *measure → derive → assert-with-margin*. Achievability basis for ≈nproc utilization: the single-pool baseline already achieves it in the just-task regime today, and dual-pool only *partitions* nproc with donate-idle restoring full availability within one poll tick. **Baseline A/B (feeds η):** the harness runs its regimes against the **current single-pool service first** (baseline capture, before the host unit switch), then against the deployed balancer — giving η a named, same-instrument comparison instead of a vibes baseline. **Escape valve (§10.4):** if implicit-only cold task compile can't fit any sane timeout, that is a surfaced *finding* (revisit absolute priority / lean on warmer-builds), not a silent stall — the signal cannot wedge on an impossible premise. **PASS.**
- **Numeric floor:** asserts `measured > floor` only (utilization ≥ derived threshold; task timeout ≥ measured worst case + margin). No bound is asserted below an analytical floor. **PASS.**

### ζ — timeout re-derivation wired + drift tests updated
- **Capability:** ε's measured budgets applied to `verify.sh add_test_passes` (debug 60m / release 75m constants at `verify.sh:691-695` replaced by derived values); `test_occt_flock_gate.sh` Test 17 + `test_release_*` updated in lockstep; #4447-lineage band-aid retired.
- **Substrate (G3):** the constants + asserting tests exist (shared-substrate row). **PASS.**
- **Premise (G6):** *cold task verify at implicit-only completes within budget* — budget := ε's measured worst case **plus margin**, so `bound > floor` **by construction** (the floor was measured by ε; dep edge ε). This retires the guessed-timeout band-aid class (esc-4178/4447) rather than adding to it. **PASS.**
- **Wired-on-main:** verify.sh execute path + `tests/infra/run_all.sh` auto-discovery. **PASS.**

### η — end-to-end mixed-load acceptance gate (CRITICAL leaf)
- **Capability:** real merge verify concurrent with N task verifies on the deployed dual-pool host: (a) box ≈ fully utilized; (b) merge wall-clock improved vs the single-pool baseline; (c) no spurious task exit-124; (d) merge reaches full token allocation under contention.
- **Substrate (G3):** everything upstream via dep edges (α β γ δ ζ); ε's baseline A/B data is the comparison source for (b) (dep via ζ→ε). **PASS.**
- **DAG-direction (G6 branch 3):** every capability (a)–(d) is delivered by η's transitive dependency closure — daemon+policy (α/β), role routing (δ), canary safety net (γ), honest timeouts (ζ←ε). Nothing is owed by a downstream task. **PASS.**
- **Premise (G6):** (b)'s baseline = ε's same-instrument single-pool capture (named source, not historical anecdote — though the 2026-06-04 48→24 incident corroborates); (a)/(d) follow from C4 if β's tests hold — η is exactly the real-load check that they do. This is the **C-as-integration-gate**: the user-observable proof (§1 surfaces 1–3) lives here, where the full chain exists. **PASS.**
- **Anti-fake-done:** the signal is measured behaviour of the production pipeline under real load — not a synthetic-input unit test (the C-02/C-07 shape this gate exists to kill). **PASS.**

## Cross-PRD seams (G4 re-check)

| Seam | Owner | Status |
|---|---|---|
| `DF_VERIFY_ROLE=merge` stamping | dark-factory (pre-existing; verified live) | wired — no DF change, no dep edge needed |
| `CARGO_MAKEFLAGS` verify_env vs apply_env | **this PRD** (δ) | queued (δ) — both files reify-owned |
| Balancer re-seed on orchestrator restart | reify (`setup-dev.sh`, inherits `PartOf=`) | queued (α) |
| OCCT semaphore merge-priority | future OCCT PRD (named in §6/§7) | out of scope; does not bind below cap≈31 |
| Warm merge worktree | `warmer-builds-merge-verify.md` | orthogonal sibling; composes; no edge |

No reciprocal-ownership ambiguity; no new contested pair (the overlay's three are kernel/grammar seams, untouched).

## Summary

All bindings **PASS**. No FAIL. Batch clear to queue. One decompose-time premise
repair is recorded inline (α/β donate-idle attribution — α ships the transfer
primitive + minimal donate-idle so its migration signal is self-produced; β owns the
full C4 policy). The numeric discipline is structural: every tuned constant is an ε
*output* asserted with margin, and η's improvement claim has a named same-instrument
baseline (ε's pre-switch single-pool capture).
