# Cross-driver divergence survey — DRAFT for discussion (not yet ruled)

Status: survey output, 2026-08-26, against main `041554eae5`. Produced by a 6-agent
survey (CLI drivers / GUI path / LSP+MCP / library seams / provenance archaeology /
consolidation) for the spec-conformance program. Every classification is a PROPOSAL;
rows marked Decision-needed=YES await Leo's ruling. Do not build on this until the
discussion round closes. Source reports: session scratchpad (ask Claude to re-copy
if needed).

# Reify cross-driver divergence table — consolidated from 5 survey reports

Consolidated 2026-08-26 against main `041554eae5`. Sources: cli-drivers, gui-path, lsp-mcp, library-seams, archaeology surveys; duplicates merged (each row lists the merged source-report IDs). Spot-checks re-run this session confirm the load-bearing claims: `mcp_context.rs:96/430/580` bare `compile()`, `gui/src-tauri/src/engine.rs:6943` `code: None`, `main.rs:988-998` build solver comment, `module_dag.rs:438` bare import parse, `check_module_path_decl` called only from `crates/reify-cli/src/main.rs:205` (+ compiler-internal), and the η refusal living in `build_outputs_with_result` (engine_build.rs:5197) while GUI `EngineSession::export` calls plain `engine.build()` (engine.rs:2704-2726).

Driver key: **check/eval/test/build/report/explain/doc** = CLI subcommands; **GUI** = EngineSession evaluate path; **Lezer** = GUI editor grammar; **LSP**; **lib** = library APIs incl. test-support ladder; **TS** = tree-sitter grammar; **mcp** = `reify mcp-server`.

Classification: intended-role-difference (IRD) / suspected-unintended (SU) / clearly-bug (CB). Rows ordered by how load-bearing the pending decision is for the conformance suite: open rulings with the widest blast radius first, then already-ruled defects (decisions taken, suite just encodes them), then narrow/governed items.

## Table 1 — Cross-driver divergences

| ID | Surface | Drivers involved | Observed difference | Evidence | Provenance | Classification | Decision-needed |
|----|---------|-----------------|--------------------|----------|------------|----------------|-----------------|
| D1 | `auto`-param (solver) resolution | eval/report/explain/GUI vs build/check/test/LSP/mcp | The same `auto` param prints a solved number under eval/report/explain/GUI and stays INDETERMINATE/undef under build, check, test, LSP, and mcp-server. | main.rs:1412-1430, :988-998; test_runner.rs:106-113; lsp diagnostics.rs:301-303; task #6631, #4132 | build half: commit `37bafd9acc` **INVERTS** recorded ruling esc-4458-87 (2026-06-10 SCOPE-WIDEN) — ACCIDENT with receipts in #6631; check/LSP halves: SILENT (no record) | SU / HIGH | **YES** |
| D2 | Which drivers run the constraint pass and gate on it | check vs eval vs test vs build vs report/explain | A violated constraint fails check/build (exit 1) but is invisible to eval on non-geometry modules (exit 0), never evaluated by report/explain (exit 0), and structurally indeterminate-⇒-pass under test. | main.rs:1650-1685 (eval), :1527-1532 (report), :852-880 (test); probe: `bracket_violating.ri` → check 1 / eval 1 / build 1 / **test 0 / report 0**; non-geometry violated scratch → eval **0** | eval routing task 4145 documents exit-widening but not the violation asymmetry — SILENT side effect; report posture DELIBERATE (comment); test drop SILENT | SU / MED | **YES** |
| D3 | Kernel-backed measurement (ReprWithin, GD&T Conforms, DFM, geometry queries, OpenVDB) | check vs eval vs build vs GUI vs LSP | check runs targeted kernel arms and build always attaches a kernel (+OpenVDB for isosurface), while eval attaches per `module_has_geometry` but never OpenVDB, and GUI viewport/LSP never measure these constraints at all. | main.rs:598-651, :999-1014, :1650-1666; no `ensure_openvdb_kernel`/`tessellate_realizations`/`set_capture_repr_tol` hits in gui/src-tauri or reify-lsp | ReprWithin-in-GUI DELIBERATE (precision PRD :307); OpenVDB scoping task δ/5002 CLI-only — SILENT for eval/GUI; GD&T/DFM-in-GUI: no record — SILENT | SU / MED (partially ruled) | **YES** |
| D4 | Purpose selection / purpose-injected constraints | check vs all other drivers | `--purpose` exists only on `reify check`, and since `engine.check()` skips purpose-injected constraints, every other driver (GUI, LSP, all other CLI, MCP) silently never checks them. | main.rs:307-374, :693-824 (esp. :695-699); GUI invoke_handler main.rs:983-1017 has no purpose surface | Purpose sequence hand-assembled in CLI; no record deciding other drivers should lack it — SILENT | SU / MED | **YES** |
| D5 | Module-header rule (`module` decl vs filename; spec §7.1/§7.2) | CLI (all subcommands) vs GUI/LSP/mcp/lib | A mismatched `module` header is Error+exit 1 under every CLI subcommand and silently fine in the GUI, LSP, mcp-server, and library tier. | main.rs:204-208 (only non-compiler call site, verified); module-and-visibility-hardening.md:261-262 | Commit `3077d2cfbd` (2026-05-30) deliberately scoped to "CLI site"; PRD left GUI/dirty-buffer an **open question** — half DELIBERATE, half SILENT | SU / MED | **YES** |
| D6 | RepresentationWithin export-refusal (η safety gate); export surface | GUI export vs build `-o` / declarative outputs | A design that `reify build -o` refuses to write (unenforced representation bound) can plausibly be exported from the GUI, whose export also lacks `.3mf` and the declarative `: Output` mode. | engine.rs:2704-2726 calls plain `engine.build()` (verified); refusal lives in CLI Mode-A main.rs:1067-1111 + `build_outputs_with_result` engine_build.rs:5197 (verified); precision PRD :620-630 | η's surface list (task #6170 program) never included GUI imperative export — SILENT omission | CB / MED (code-read; not runtime-verified) | **YES** |
| D7 | `relate_solve` at-auto poses in live geometry | build/GUI-export vs GUI live viewport vs check | Solved poses reach STEP via `build`/GUI export but never the GUI live viewport (identity fallback) and never check, so the viewport shows different geometry than the exported file. | engine_build.rs:3979 (`build_with_geometry_output` only); geometry_ops.rs:11388; #6631 addendum ("apparently untracked") | SILENT — no owning task exists per #6631's own record | SU / HIGH | **YES** |
| D8 | MCP tool semantics per host | mcp (CLI context) vs GUI (Tauri context) | The same MCP tool names return materially different results per host: CLI context gives constraint status always `"unknown"`, discards eval diagnostics, strips codes, and omits guarded-group params; GUI context returns real state. | mcp_context.rs:108/242-262/282-283/330-347/439/605 vs gui mcp_context.rs:127-177; no cross-context parity test | "for now … we'll upgrade" stub comment (mcp_context.rs:334-336) never revisited — ACCIDENT (stalled stub) | SU / HIGH | **YES** (is CLI mcp-server a supported driver, or deprecate?) |
| D9 | Parse-error severity/policy | CLI vs lib orchestrator vs test-support (vs LSP) | One parse error is a pre-compile abort (exit 1) at the CLI, a Warning with best-effort compile inside the library orchestrator, a re-mapped Error in test-support, and a span-diagnostic early-return in the LSP. | main.rs:193-198; pre_pass.rs:18-31; helpers.rs:317-349; lsp diagnostics.rs:121-129 | Each policy locally DELIBERATE (pre_pass.rs:20-23; helpers.rs:331-337; task #4681); never reconciled — no cross-driver record | IRD / LOW | **YES** |
| D10 | Compile-time `auto:` type-param feasibility checker | check/eval/GUI vs LSP/lib-default/test-ladder | Same source can be `E_AUTO_TYPE_PARAM_AMBIGUOUS` under the stub checker (LSP, library default, test ladder) and a unique-survivor success under the real `SimpleConstraintChecker` (CLI, GUI). | lib.rs:221-243; main.rs:200; gui engine.rs:843-846; lsp analysis.rs:97; helpers.rs:236-239 | Checker-injection task 4437 (done, `f525934654`+) wired CLI+GUI deliberately; LSP/test-ladder left on stub with no recorded decision — SILENT | SU / MED | **YES** |
| D11 | Diagnostic egress composition (codes/spans/rewrites/visibility) + exit conventions | CLI vs GUI panel vs LSP vs mcp | The same diagnostic set reaches users as code-less span-less text (CLI), code-stripped spans (GUI panel + MCP, `code: None`), or code-bearing rewritten spans with silent Indeterminates and LSP-only freshness codes (LSP) — plus `doc`'s unique exit-2 usage convention and eval-only undef `note:` lines. | engine.rs:6943 (verified), 2891; convert.rs:152-254; main.rs:210-212, :1944; lsp diagnostics.rs:223-511 | GUI code-strip predates DiagnosticCode adoption (`3e5c76debd` 2026-04-03), never revisited — ACCIDENT; LSP rewrites DELIBERATE per-feature (task 5078); doc exit-2 DELIBERATE (task 2361, `0f10ccfee6`) | Mixed: code-strip SU / HIGH; rest IRD / MED | **YES** |
| D12 | Multi-file import resolution + cfg surface | check+GUI load_file vs eval/build/test/report/explain/doc/LSP/GUI-buffer/mcp | The same importing entry resolves under check and GUI `load_file` but reports imported structures unknown (exit 1) under every other driver; `--cfg` exists only on check and the GUI hard-codes host-default cfg. | main.rs:173-215 vs module_dag.rs:911-998; gui engine.rs:926-1077, :944-950; probes on `resolution_multifile/`, `importing_entry_unknown_structure.ri` | check went multi-file as a **side effect** of cfg (`383c6f47c1`); GUI deliberately (task 3228); the split itself never ruled — SILENT accretion, since RULED a defect (resolution-unification PRD, Leo 2026-07-24; tasks 5516-5529 pending) | CB / HIGH (ruled) | NO (fix chartered; residual: GUI cfg-selector is ruled-deliberate v1) |
| D13 | Error-severity exit gating under check | check vs eval/build | `reify check` prints `error:` lines yet exits 0 (only compile diags + 2 bolt-ons gate), while eval/build exit 1 on any Error diagnostic. | main.rs:546-552, :671-690; probe `m5_geometry_flange.ri` → 2 errors, exit 0; INV-SF-2 | Task 4458 retrofitted eval/build only — SILENT at introduction; ruled a defect 2026-07-24 (hard-gate + allowlist, #5403/#5404 pending) | CB / HIGH (ruled) | NO |
| D14 | check false-green vs eval on identical file | check vs eval | check prints "All constraints satisfied." exit 0 while eval emits `error: failed to compile geometry operation…` exit 1 on the identical file, because check's kernel arm discards `build()` diagnostics and its kernel gate is narrower than eval's. | check-diagnostic-truthfulness.md:18-28; main.rs:598-651 vs :1650; tasks #5748, #6048 | Accretion of check's "lightweight" heritage — ACCIDENT; ruled + PRD-owned (2026-07-28) | CB / HIGH (ruled) | NO |
| D15 | Stdlib prelude at compile under mcp-server | mcp vs every other driver | `reify mcp-server` compiles all three sites with the no-prelude bootstrap `compile()`, so stdlib types/traits/units/enums are unresolved-name Errors that no other driver emits (while eval-side values still partly resolve). | mcp_context.rs:96/430/580 (verified); lib.rs:194-205; empirical probe (9 spurious Errors vs check clean) | Original 2026-03-23 stub (`7b0af76331`); task 2176 (done) fixed LSP+CLI but missed these sites; parse later upgraded (task 2525), compile not — ACCIDENT | CB / HIGH | NO (fix or formally deprecate — flag remediation choice) |
| D16 | Builtin signatures advertised vs compiled | LSP vs compiler | 35 LSP-advertised builtin signatures contradict the compiler (wrong arity, return types, param order; 7 of 13 `orient_*` missing). | builtin-signature-registry PRD :5/:98; task #5704 (cancelled→subsumed) | Prose signature strings never machine-checked — ACCIDENT; structural fix (registry-generated LSP) in flight | CB / HIGH | NO |
| D17 | Method calls with arguments | TS grammar vs evaluator/library | `xs.map(\|v\| v+1)` is a parse error in the grammar while `reify-expr`'s method-call arms exist and are unreachable from any `.ri` source. | grammar.js:1583-1587; task #6406 | Evaluator capability landed without its grammar — SILENT accretion (discovered 2026-08-21) | SU / HIGH | **YES** (which side moves) |
| D18 | Value+unit display formatting | CLI vs GUI vs LSP | Three independently-evolved formatting functions render the same value differently per surface, with an in-code "known-divergent, do not fix here" marker. | gui engine.rs:6630-6640; display-unit-preference.md §2b/§7c; #5234 (seam done, L4 rewiring outstanding) | ACCIDENT, deliberately quarantined pending unification | SU / HIGH | **YES** (confirm L4 status; ratify quarantine into suite meanwhile) |
| D19 | Editor accept/reject grammar | Lezer vs TS/compiler | The editor grammar is a governed subset: 327/330 corpus files clean, one pinned CST divergence (`1_000_000` lexes as quantity), third-dialect pins, and 2 files pending #5495. | reifyGrammarCorpus.test.ts:16-25, :3996-4011, :4649-4813; reify.grammar:1835-1843 | DELIBERATE — ledger + `CLEAN_FLOOR` ratchet (tasks 5907/5927/5941 done); drift half was ACCIDENT, remediated | IRD / HIGH | **YES** (only to adopt the ledger as the suite's sanctioned-divergence mechanism) |
| D20 | Test-support ladder as a language driver | lib (test-support) vs all product drivers | The in-process ladder compiles a language no product driver ships: no stdlib at compile yet full stdlib at eval, Mock checker defaulting every constraint Satisfied, module identity `"test"`, and prelude-blind enum lowering on plain rungs. | helpers.rs:96-239; mocks.rs:61-68; engine_admin.rs:787-796 | DELIBERATE conveniences per rung, never ruled as a conformance surface — SILENT in aggregate | IRD / MED | **YES** (label non-conformant, or build a check-faithful rung) |

### Per-row notes (Table 1)

- **D1.** The one proven ruling-inversion in the census: Leo resolved esc-4458-87 choosing to keep `configured_eval_engine(...).with_solver(production())` in `cmd_build`; a context-free re-dispatched implementer committed the opposite, and the surviving comment's "intentional" is that commit's own text (#6631, watcher transcript + Graphiti edges cited there). Downstream symptom probed in #6631: check reads as PASS (indeterminate), build & GUI realize the *default* (phantom geometry), eval solves the value but realization still uses the default. check's solver absence has **no recorded rationale anywhere** and interacts badly with `--strict` (which exists to fail indeterminates). The suite cannot pick expected `auto`-param outcomes per driver until this is ruled.
- **D2.** Mechanism: `build() ⊇ check()` but `eval() ⊉ check()`, so eval's violation surfacing is an accident of the task-4145 geometry routing. `test` additionally never prints or gates `TestResult.diagnostics` (loop reads only status), and geometry-dependent `@test` constraints can only be Indeterminate (kernel-free, deliberate isolation per test_runner.rs:106-113) — the "meaningless pass" class. `report`'s kernel-free rollup is comment-deliberate but its violation-blindness is unexamined. Decision shape: define per-driver whether the constraint verdict is part of that driver's contract.
- **D3.** Fold-in: eval lacking OpenVDB means an isosurface module degrades under `reify eval` where `reify build` succeeds (task δ/5002 comment scopes itself to cmd_build without mentioning eval). GUI repr-within Indeterminate is ruled ("Revisit if a use case contradicts"); extend-or-rule-separately for GD&T/DFM.
- **D5.** Includes the module-identity sub-issue: CLI names modules by file stem (dotted-stem caveat), test-support hardcodes `"test"`, LSP uses URI stem — fixtures carrying real `module` headers pass tiers non-uniformly unless the suite normalizes.
- **D8.** Distinct from D15: D15 is the CLI context's compile being wrong in itself; D8 is the parity claim across the two `ReifyToolContext` implementations, with nothing pinning them together.
- **D11.** Cheapest high-value fix inside this row is the GUI `code: None` strip (one projection function). The rest is a ratification exercise: pick which of message/code/span/exit-code is the conformance object — no driver emits all four.
- **D12.** Also covers: LSP-internal asymmetry (navigation is import-aware while diagnostics call the same imports unresolved), GUI buffer-loads single-file, GUI 1-hop template merge, entry-only source_map — all documented v1 limitations with RU-leaf owners. The only open residue: whether any command *should* stay single-file (RU D-12-adjacent) — the PRD's default is no.
- **D13.** RU's η parity harness deliberately asserts diagnostic-set equality, not exit-code equality (D-11), because this seam is owned by #5386/#5403 — the suite should mirror that split of assertions.
- **D19.** The Lezer ledger (pinned EXPECTED_CLEAN + per-production rationale in-grammar) and E2's `EXEMPTION_LEDGER` (Table 2) are the two in-tree precedents for machine-checked sanctioned divergence; the conformance suite likely wants exactly this shape as its exemption mechanism.

## Table 2 — Intra-pipeline parity seams (single-driver internal, not cross-driver)

| ID | Seam | Observed difference | Evidence | Provenance | Classification | Decision-needed |
|----|------|--------------------|----------|------------|----------------|-----------------|
| S1 | Compile-time vs eval-time overload resolution | Two independent implementations of one predicate (compiler: 4 disjuncts; eval mirror: 3, missing D4 `type_carries_type_param`); drift yields bare `Value::Undef` for calls the compiler resolved. | type_compat.rs:1020-1026, :1195-1202; reify-expr lib.rs:1533-1573; #5992, #5686; esc-4093-152, esc-4231-120/126 | Structural ACCIDENT (dep-graph forces a verbatim mirror); guarded by comment convention only until #5992 | SU (accidental-by-construction) / HIGH | YES (parity test + ledger shape) |
| S2 | check: entry-vs-import compile asymmetry | Within one `reify check` run, imports are parsed prelude-blind (enum lowering differs), compiled with no stdlib prelude and the stub checker, and their own diagnostics — incl. Error-severity header mismatch — are silently swallowed (probe: exit 0, zero output). | module_dag.rs:438, :474-486, :894-910, :938-943; probe `module_header_mismatch/entry.ri` | Accretion; stub-checker half DELIBERATE+pinned; rest ACCIDENT, chartered (RU β/5516, θ/5522, ξ/5528) | CB / HIGH (ruled) | NO |
| S3 | Static vs runtime builtin typing | 121 builtins ride a terminal first-arg type fallback; 93 of 231 statically mistyped incl. wrong values (`floor(2.5mm)` → `Int(0)`). | builtin-signature-registry.md:5; fallback-soundness-analysis-2026-08-03.md; #5371/#5997 | Accretion — ACCIDENT; registry program in flight | CB / HIGH | NO |
| S4 | Compile-prelude vs eval-prelude | `Engine::new` always loads the full stdlib as its eval prelude regardless of what compile saw, so no-prelude-compiled modules (mcp-server, library plain rungs) evaluate against a full-stdlib function table — compile diagnostics and eval values disagree about the same names. | engine_admin.rs:787-796; reify-eval lib.rs:1513-1520; mcp probe (errors + fully-evaluated values) | SILENT — no record reconciling the two prelude decisions | SU / MED | YES |
| S5 | Compile-time vs eval-time visibility (non-pub cross-file) | check's compile-time existence check seeds `known` with all prelude templates regardless of `pub`, so only the eval-phase backstop catches a non-pub use (error text with exit 0). | sub_component_validation.rs:57-65; #5386 (pending) | ACCIDENT (missing filter), found 2026-07-24; fix split #5403/#5523 | CB / HIGH (ruled) | NO |
| S6 | Omega function fallback | A call to a nonexistent ≥1-arg function compiles with zero diagnostics (check exit 0) while the 0-arg boundary warns — makes "check accepts / eval fails" shapes trivially constructible. | fixtures `omega_nonexistent_fn_{silent_accept,zero_arg_warns}.ri` (cite omega-fallback-deletion PRD pre-state) | Accretion; deletion PRD in flight | CB / HIGH (ruled) | NO |
| S7 | cfg gating vs import-warning channel | Under check, a cfg-gated-OUT import is correctly skipped by the DAG but still draws the cfg-blind `import "X" not resolved by this entry point` warning. | entities_phase.rs:198-211 (task 2226 wording, predates cfg); probe on `conditional_compilation/main.ri` | ACCIDENT (warning site never updated when DAG skip landed); contradicts η/5521 boundary #4 wording | SU / HIGH | YES (small; will surface when η lands) |
| S8 | Prelude collision policies | Four policies for one concept: units last-wins+warn, aliases first-wins+warn, functions first-wins-silent, templates first-wins+warn. | prelude_context.rs:32-74 (task 2776); RU PRD §1 | Accretion — ACCIDENT; normalization (incl. ruled breaking units flip) owned by RU θ/5522 | CB / HIGH (ruled) | NO |
| S9 | Static vs runtime Mul/Div types | Runtime widens non-divisible Int/Int division by divisibility; static stays Int — the one seeded row in a machine-checked exemption ledger (unledgered divergence panics). | mul_div_static_runtime_parity.rs:27-28, :203-206; #5063 done; INV-COMP-3 (docs/invariants.md:38) | DELIBERATE, ledgered — the house pattern | IRD / HIGH | YES (only to import the ledger row into the suite as-is) |

## (a) Surveyed but clean (checked, no divergence found — absence of a Table-1 row means checked)

- **Parsing (product drivers):** every Rust driver — all 7 `.ri` CLI subcommands, GUI EngineSession, LSP, mcp-server — parses entry files through the same `parse_with_stdlib` → tree-sitter path (lib.rs:263-271); no accept/reject divergence among product drivers for entry files. (Import-side parse is S2; Lezer is D19.)
- **`#no_prelude`:** honored inside the shared orchestrator (`effective_prelude`, lib.rs:499-500 / pre_pass.rs:48-61) — uniform everywhere; parse-side enum seeding ignoring the pragma is also uniform.
- **Stdlib content:** exactly one embedded stdlib (`stdlib_loader.rs`, incl. generated `std.si_units`); no variant-vs-variant divergence exists — the only axis is stdlib-vs-none (D15/D20).
- **Kernel selection:** the lex-min BRep pick (`engine_admin.rs:893-918`) is shared by every kernel-using driver; no divergence in *which* kernel.
- **`#deterministic` lowering:** shared compiler code (annotations.rs:106-111); no per-driver divergence found.
- **Orchestrator-internal stages** (lints, phases, purposes-merge, hashing inside lib.rs:478-736): single shared implementation — structurally cannot drift between drivers.
- **gui-debug MCP listener (port 3939):** verified to be an observation surface over the same EngineSession pipeline (same `load_file_into_engine` funnel, #5193), not a fourth pipeline.
- **`reify dev inspect-node` and `reify cache *`:** confirmed not `.ri` drivers at all.
- **GUI line/col converter divergence:** bounded, declared unreachable in production, pinned at EOF (engine_tests.rs:2757-2775) — deliberate and closed.
- **Resolved-and-pinned precedents (formerly divergent, now clean):** MCP severity casing CLI-vs-GUI (#2638 done, cross-transport regression guards); `minimize`/`maximize where` grammar-vs-compiler (ruled REJECT 2026-08-25, #6575 done, spec amended, compiler landed `dc22d897a3`); Lezer catch-up drift 7/329→327/330 (#5907/#5927/#5941 done, ratcheted).

**Dropped as not language-visible** (surveyed, excluded per rules): persistent FEA cache wiring differences incl. the GUI sweep-without-use comment-vs-code contradiction (performance only — worth a hygiene task, not a conformance row); GUI file-watcher recompiling only on entry-file changes (session staleness); `--verbose` meaning kernel-provenance on build vs cache-stats on eval (CLI UX); eval's OCCT STEP banner leaking to stdout (output hygiene).

## (b) What the survey could NOT determine

- **GUI export η-bypass at runtime (D6):** code-read confirmed this session (plain `engine.build()`, refusal only in `build_outputs_with_result` + CLI Mode-A), but never runtime-verified — no fresh GUI build existed during the surveys.
- **Probe-binary freshness is contested:** the cli-drivers report claims `target/release/reify` was rebuilt within ~12h of HEAD; the gui-path report records mtime 2026-07-28. The reports' probe *results* agree with code reads and with the RU PRD's own 2026-07-24 probes, but any probe-only claim without a code citation carries this caveat.
- **check's solver-free posture intent (D1):** exhaustively searched; no record found either way — "SILENT" is a determination of absence-of-record, not proof no decision was ever made verbally.
- **GUI live-viewport pose gap (D7):** sourced from #6631's addendum and fused-memory observation `84501fa6…`, not independently probed (needs a GUI runtime session).
- **Warm-edit incremental path parity:** the GUI's `edit_check`/warm-state/demand machinery has **no second implementor** to diff against, so full-recompile-vs-incremental equivalence could not be cross-checked — a single-implementor conformance risk rather than an observed divergence.
- **Non-host cfg targets:** all probes ran on linux host-default; `--cfg target=wasm` was probed only for symmetry under check — behavior of other drivers under non-host cfgs is untested (moot until RU γ/δ land, but unverified).
- **Purpose semantics outside check (D4):** no other driver has a purpose surface, so cross-driver purpose *behavior* could not be measured — only the surface's absence.
- **display-unit-preference L4 status (D18):** whether the call-site rewiring has landed since #5234's seam was not re-verified; the in-code known-divergent markers were still present at survey time.
- **Task statuses** are as of 2026-08-26 fused-memory reads; pending tasks (5516-5529, 5403/5404, 5748, 6631, 6406, 5495, 5992, 5686, 5386) may have moved by reading time.
- **LSP under real multi-file workspaces:** the diagnostics-vs-navigation asymmetry was established by code read; end-to-end editor behavior on a multi-root workspace (open-buffer preference, stale cross-file state) was not probed.
## Errata (2026-08-26, post-filing)

- **D6**: already tracked — filing was curator-combined into existing pending task **#6190**
  (priority raised to high; runtime-verify-then-close framing merged in).
- **D7**: already tracked — existing pending task **#6646** covers it exactly; #6631's
  "apparently untracked" addendum was stale.
