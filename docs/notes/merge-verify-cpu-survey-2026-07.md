# Merge-verify CPU survey — findings baseline (2026-07-19)

Program: merge-verify-cpu-reduction. Release-gate milestone: task **5254**.
Provenance: 12-agent mining of `data/orchestrator/runs.db`, `data/verify-logs/` (~230 failed-gate diagnostics), DF source, and live config. Fused-memory observation `4e480523-746e-4538-aa78-a61a30f43857`.
Committed 2026-07-19 post red-main recovery (e9ca7875ed).

## Cost baseline

- Passing merge gate: **median ~44–49 min wall** (p90 ~68), ~27 gates/day = ~12.7 wall-h/day; **2.26 gates per landed merge** (14d); failed gates = 24% of gate hours.
- Test phase (green run 5163, 3015s): debug nextest build 681s + exec 566s; release build 700s + exec 285s; reify-cli prebuild 327s; reify-audit prebuild 178s; run_all infra ~390s; gui vitest median 80s. Lint phase (separate process): clippy median ~4 min (max 36 min).
- **Compile+link ≈ 55–65% of gate CPU.** 1,103 integration-test binaries (reify-eval 397, reify-compiler 290, reify-cli 73, reify-syntax 61, reify-kernel-occt 48); link ~0.5–1.5s each (rust-lld; mold rejected +26%, `docs/notes/linker-rustlld-vs-mold-bench.md`), memory-serialized at 1–3 GiB RSS/link (`scripts/verify.sh:1183-1200`).
- Debug pass: 20,280 tests / 1,145 binaries. Release pass: ~10,400 tests / ~500 binaries over the 9 crates in `scripts/release-sensitive-crates.txt`.
- Slowest tests: `no_stale_undef_invariant_gate` corpus shards (~890s summed, content-shifting), `buckling_multi_case` ~500s/3, adaptive-refinement cantilever 119–254s, `process_dfm_thickness_example` ~300s/3, occt→gmsh conformance 116s; release-only: `euler_column_pin_pin` 299s/5, `modal_analysis_e2e` 179s/5.
- CORRECTION (2026-07-19, DF 2822): the laptop remote runner has been running a NARROWER verify for merge-deciding lands (--scope branch, test-only, debug — not the full gate), and passing remote verifies are archived nowhere (verify_runner.py:1058-1064); this is DF gate-hole #2 and supersedes this survey's earlier claim that the remote host runs the same merge gate.
- Frequency streams (14d): task-role verifies ~146/day; post-rebase `workflow_verify` ~52/day (since 07-14, ~86 wall-h/14d forecast); speculative gates 55 runs/41h (17 failed); same-SHA dupes 24 (all explained: M1 bounded infra-transient retries, M2 cold-shadow trust re-verifies, train/spec attribution; **no lock failure** — merge lock is an order lock; deliberately no green cache: `merge_queue.py:11374` skip_verify unconditionally False).

## Failure attribution (230 failed-gate diagnostics, 06-24→07-19)

fails/CPU-h: run_all infra **1228** ≫ node lane 204 > debug nextest 131 ≈ prebuilds 127 > lint 60 (24 genuine) > release nextest 21. Current step order already near-optimal (contract: `tests/infra/test_verify_failfast_order.sh`); only supportable swap = lint block before release nextest (~22s/failed gate). **Dynamic ordering rejected**: weekly failure-profile ranking is stable; oracle-over-static ≤15s/failed gate.

## Flake RCA verdicts (per-family, all evidence-backed)

- `_merge-verify` worktree-deleted-mid-run: 48 strict local instances since 06-25 (+2 laptop twins 5034/5187) = 24–27% of failed local gates in the shared-worktree era. TWO limbs: (i) actor-vs-actor (seed/thin/GC/reset clobbering a live verify) — CLOSED by DF 2685 (07-17; lock-path convergence onto `<lane_dir>.lock`, `git_ops.py:1866-1922`, reset holds the flock `git_ops.py:7798-7812`); (ii) **restart-collateral — STILL OPEN, the dominant limb (44/48)**: lease is process-lifetime-bound, no startup survivor barrier; of 64 restarts dispatching a merge verify within 90s, **62 failed (96.9%)**; two post-2685 recurrences (5164 07-18, 5071 07-19). Contributing: `verify_use_cgroup_scope` OFF for reify (`config.py:2481`; `verify.py:3294`) so GNU-`timeout` setpgid children escape killpg (`verify.py:2846-2849`); contended-lease fail-open after 10s (`git_ops.py:1902-1911`); laptop CLI still on divergent `.merge_verify.lock` (`cli.py:495-503`). Open fixes (NO existing task): startup survivor barrier (cwd/fd/maps scan + reap before first merge dispatch), enable `verify_use_cgroup_scope` for reify (+DF startup scope sweep), contended-lease timeout ⇒ defer dispatch not unprotected run, laptop lock convergence.
- `test_verify_semaphore_e2e` (41): 21 = laptop host-constant assertion (fixed 4901, 06-30); 15 = worktree collateral; 4+1 = branch-skew/recursion (fixed cd12443277, 5144). **Zero true contention flakes. Test is green since 07-16. Do not file a de-flake task.**
- `test_no_new_wallclock_upper_bounds` (24): all deterministic detections; ~18 true positives, 6/run lexeme false-positives (config constants + prose "wall"); 23/24 landed on innocent branches (blame-locus inversion). Three offenders (4861, 4923, 5141) **landed through the gate while the guard was red** — DF landing-discipline hole. 5158's recorded "latent two-task interaction" RCA was empirically FALSIFIED.
- `test_occt_flock_gate` (23): 21 = one deterministic quoting bug × ambient `REIFY_GATE_EXCLUDE_HEAVY` (fixed 4965/4966/4971, 07-03); **live flake: Tests 14/15/22 holder-spawn race** (`sleep 0.2` grace, `test_occt_flock_gate.sh:166-168,213-214,601-615`) — flaked again 07-19 (5252 ledger event). Fix: causal barrier (flock -n probe) like Tests 19/21A.
- `test_run_all_classification` (21): 18 = correct drift catches. **Live Jul-19 red: 5247/5249 added infra tests without manifest rows via the TRIVIAL-PASS HOLE** — `scripts/verify-pipeline-guard.sh requires-full-gate` returns rc=1 for new `tests/infra/test_*.sh`, so the full gate never ran. Rows already on branch task/5252. Sweep retries it forever as `env_transient` (DF categorizer bug) — heavy-coverage sweep dead since 07-19 11:45.
- `test_run_all` (20) / `test_run_all_ambient_isolation` (7): all deterministic-in-context (ambient env leak fixed 4961+cd12443277; SIGPIPE fixed 09b2f3e69e) or collateral; ambient_isolation is 100% derivative of test_run_all (recommend baseline-red SKIP decoupling).
- `test_reify_audit_ptodo` (15): 14 collateral + 1 correct catch (4636). Hermetic already; add `NEW_IN_LIVE` echo at `test_reify_audit_ptodo.sh:251` for diagnosability.
- `test_cpu_admit` (13): 13/13 collateral; fully fixture-injected already. No action.
- `test_verify_scope` (12): 4 collateral, 3 correct catches (fixed), **5 LIVE deterministic false positives: stale MG-B5 sentinel** (`test_verify_scope.sh:831-834` assumes reify-ir never release-sensitive; 5166 legitimately added it). Fix via `/unblock 5166` (high-confidence proposal already on the task).
- `test_cpu_load_governance` (7): all predate the 04997 host-exclusive reclassification + 5125 merge-tier exclusion. No leak. Measurement tests can NEVER be gate-resident on a saturated host (empirically settled); logic tests use fixture PSI (cpu-admit pattern).
- tree-sitter/shim/reaper/throughput: dominated by collateral; real one-offs fixed (4866 plan-capture, 5148 ps-poll). Small hermeticity items: tree-sitter adopt `plan_capture_lib.sh` at `:359/:382`; proc_reaper pre-clean marker suffix (`:945`), load-scaled handshakes, loud lib-missing guards.
- **FLAKY ledger is amnesiac**: defaults to the running checkout's `data/verify-logs/flaky-ledger.jsonl` (`run_all.sh:549-558`) = wiped on every lane reseed; exists nowhere on the host. Phase-2.5 retry+ledger only covers `pool` bucket. Fix: DF exports `REIFY_RUN_ALL_FLAKY_LEDGER` to the main checkout's archive path.
- **DF categorizer**: `_classify_environmental` guard-3 precedence (`verify_classify.py:125-126,225-252`) labels any output with lock/slot/semaphore tokens + "timed out" (whole-output co-occurrence) as `semaphore_timeout` BEFORE per-tool dispatch; a reify full-gate log always matches (187 lock-token + 118 timed-out lines in a sample). **Precision 0/31** in the 06-21→07-19 window (22 real test failures, 9 compile/env, zero genuine timeouts — merge role BYPASSES the semaphore, `lib_test_semaphore.sh:92`, so the label is near-impossible there). Burn: 8 pure-wasted same-SHA full-gate retries (identical failing sets both attempts, all labeled semaphore_timeout) = 2.08 h wasted / 4.61 h occupancy, all 07-18→07-19. Fix vehicle: **DF 2821 (in-progress 07-19)** — positive-anchor on wrapper-emitted markers (the module's own docstring proposal, `verify_classify.py:198-203`); plus deterministic-red circuit-breaker at the retry seam (`merge_queue.py:1800-1812`, reuse `parse_per_test_results` + a `^FAILED \S+\.sh` matcher) if 2821 ships classifier-only. **A↔B interaction**: restart-collateral currently self-heals via the misclassification's free retry (5164/5071) — the classifier fix must land with DF 2756's `_VERIFY_ENV_BROKEN_RE` (`verify_classify.py:160-163`) extended to the `couldn't read <file>` / `verify.sh: No such file` collateral shapes, or A-collateral starts hard-blaming innocent branches.

## Consolidation trade-off (ratified)

Selection is crate-level (`-p`) or per-test; only **7 binaries** are named by filtersets/overrides (6 heavy atoms in `scripts/heavy-test-filter-lib.sh:48` + `representation_within_assertion` in `.config/nextest.toml:91-122`; occt group matches by *package*). Optimum: **within-crate harnesses capped ~10–20 kLOC**; keep the 7 standalone; reify-eval 397→~15–25, reify-compiler 290→~10–15, small crates 1 each; net ~50–80 binaries, ~95% link elimination, worst-case test-edit recompile ~1 min. Guards that resolve binary names to disk: `test_heavy_filter_atoms.sh`, `test_nextest_slow_priority.sh`, `test_verify_offline_partition.sh`, `test_verify_gate_exclude_heavy.sh`, `test_run_offline_deep.sh`, `test_verify_role_prio.sh`.

## Eval-decomposition state (fused-memory, 07-19)

M=4933 done (verdict: A/B/D CANCEL on CPU, P/S PROCEED; non-CPU escape clause explicit); A=4934 done; **B=4935 pending** (dep 5072); **D=4936 pending**; P=4937 done; S=4938 done; 5026/5027 done; 5028 deferred bookmark. NOT covered anywhere: (a) release-sensitivity leaf extraction (reify-eval 8 src + 4 test files; trivial members: reify-core `src/source_location.rs:274`, reify-stdlib `src/loop_closure_solver.rs:1513`, reify-runtime `src/warm_startable_assert.rs:97`, reify-expr 1 file, reify-mesh-morph `src/diagnostics.rs:527,560`); (b) lib compile-unit shrink beyond B; (c) **ENGINE_VERSION_HASH hashes all of Cargo.lock** (`crates/reify-eval/src/engine_hash_algo.rs:99-106`, `build.rs:90-96`; task 3484) — any dep bump invalidates the FEA cache + triggers full recompile + ~794 relinks.

## Coverage gaps (verified 07-19)

- Offline deep lane (`DF_VERIFY_ROLE=offline`, `scripts/run-offline-deep.sh`): **never executed** — DF worker exists (`orchestrator/offline_lane.py`) but `git.offline_lane_enabled` + `git.persistent_offline_deep_worktree` default False (`config.py:1334,1350`), absent from `dark-factory-orchestrator.yaml`.
- Heavy filterset IS covered by the main-tip background sweep (role=background, since 5210 landed 07-14; `verify.sh:564-567` scopes heavy-exclusion to task|merge) — but sweep currently wedged (see above).
- Unconditionally-`#[ignore]`d convergence tests: run **nowhere** (`--run-ignored all` is offline-role-only, `verify.sh:576-577`).
- Host-infra cold lane (`run_all --scope host-infra`, 4 tests): **never ran** (Lane-X lock `/tmp/reify-lane-x-1000.lock` has never existed; gated on `offline_lane_infra_enabled`, `config.py:1363`).

## retry_failed_only substrate (verified)

`merge_request` (`dark-factory/escalation/src/escalation/server.py:1093`) has the `verified_green` caller-vouched-bool precedent. DF already parses per-test results (`merge_shadow.py:171-230 parse_per_test_results`, nextest+libtest formats). cargo-nextest **0.9.136** has experimental `-R/--rerun <run-id|latest|zip>` under `NEXTEST_EXPERIMENTAL_RECORD=1` (reruns failed + not-started + new — the SOUND semantics; bare failed-only `-E` filter is UNSOUND because gate runs fail-fast-cancel, e.g. `5570/9823 tests run`). Shadow-compare gotcha: `on_result` stores the final VerifyResult as warm per-test baseline (comment above `merge_queue.py:1815`) — subset retry must merge attempt-0 + retry maps. run_all has no member-subset knob (only `--scope host-infra`); Phase 2.5 already re-invokes members individually. Savings: ~2.5 h/14d direct + head-of-line latency + makes retry budget >1 affordable.
